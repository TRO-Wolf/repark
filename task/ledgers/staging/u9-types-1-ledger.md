# Unit ledger — WO U9-TYPES-1 · MAP, TIMESTAMP_LTZ, VOID (v3 unknown), UUID read

**Date:** 2026-09-25 · **Branch:** `feat/u9-types-1` · **Base:** `60eaa729`
(`origin/main`) **Model:** Claude Opus 5.5 (`claude-opus-5-5`) · **Policy:** [../../../AGENTS.md](../../../AGENTS.md).
**Path:** STANDARD. **risk_tier: standard.**

**Retires:** this ledger moves to `../completed/` when the unit's last commit lands.

**Why now.** Four TYPES cells of the 2026-09-25 scoreboard run answered wrong on the Spark door:
`TY-TIMESTAMP-LTZ` and `TY-UNKNOWN-VOID` refused their column type at CREATE, `TY-UUID-READ`
refused `ALTER TABLE … ADD COLUMN u UUID`, and `TY-MAP` failed its INSERT on the empty map
literal `map()` (`Function 'map' expected at least one argument but received 0`).

**What Spark does (measured 2026-09-25; every measurement is committed as
`python/repark/tests/u9_types_1_spark_oracle.json`, generator
`target/probe-u9-types-1/build_oracle.py` + `merge_oracle.py` over `steps.py`; the first sweep
is `target/probe-u9-types-1/spark.json`, 204 probes; the verifier round r2 of 2026-09-25 added 13 steps, 208 in all, 149 EQUAL, recorded to `obs-spark-r2.json` / `obs-repark-r2.json` and merged into the observation files for the new keys only; round r3 added 41 `map/assign/*` and `map/cmp/*` steps, 249 in all, 163 EQUAL, Spark recorded to `obs-spark-r3.json` for the new keys only and RePark re-recorded on every group to `obs-repark-r3.json`; the probe of the comparison forms is `target/probe-u9-types-1/r3/cmp_probe.py`, answers `spark-cmp.json`, `repark-cmp-before.json`, `repark-cmp-after.json`).** A `TIMESTAMP_LTZ` column is Iceberg
`timestamptz` on v2 and v3, whatever `spark.sql.timestampType` says, and reads as Spark
`timestamp`; `TIMESTAMP_LTZ '…'` is a session-zone instant literal. `map()` is an empty
`map<void,void>` that coerces to the target map type. A `VOID` column is Iceberg `unknown` on
v3 only (v1/v2 refuse `Invalid schema for v<N>: - Invalid type for c: unknown is not supported
until v3`), reads NULL and describes as `void`. A `uuid` column (Spark cannot declare one in
SQL: `[UNSUPPORTED_DATATYPE] Unsupported data type "UUID"`) reads and writes as a canonical
lower-case `string`.

## Clauses

| Clause | Statement | Proof obligation | Verdict | Evidence |
|---|---|---|---|---|
| C-001 | A `TIMESTAMP_LTZ` column is Iceberg `timestamptz` on format v2 and v3: top level, a struct field, an array element and a map value, at CREATE and at `ADD COLUMN`, also under `spark.sql.timestampType=TIMESTAMP_NTZ`. It reads as Spark `timestamp` in `cols`, `DESCRIBE`, `dtypes` and `printSchema`, and `ALTER COLUMN c TYPE TIMESTAMP_LTZ` is a no-op commit. | Metadata schema and every schema surface on the facade; the Rust door schema. | PROVEN | Facade replay `ltz/v2/md`, `…/cols`, `…/describe`, `…/dtypes`, `…/printschema`, `ltz/v3/*`, `…/struct-md`, `…/addcol-md`, `…/alter-to-ltz`, `ltz/ntzconf/md` (the `c` field; `d` is R-5); Rust `a_timestamp_ltz_column_is_an_iceberg_timestamptz_on_v2_and_v3`. Cell `TY-TIMESTAMP-LTZ` replays EQUAL on every observation. |
| C-002 | `TIMESTAMP_LTZ '…'` (any case, with or without a space) is Spark's session-zone instant literal: type `timestamp`, the wall read in `spark.sql.session.timeZone` (UTC and America/New_York), usable in `VALUES`, comparisons and `CAST(… AS STRING)`; the double-quoted `TIMESTAMP_LTZ "…"` is the same literal. A `timestamp_ltz` identifier that is not followed by a string literal keeps its meaning, and `CAST(… AS TIMESTAMP_LTZ)` is unchanged. | Rows and types on the facade and the Rust door; token-planner unit pins. | PROVEN | Facade replay `ltz/v2/literal-cols`, `…/literal-rows`, `…/insert-ltz-literal`, `…/where-ltz-literal`, `ltz/ny/insert-ny-literal`, `ltz/ny/where-ltz`, `ltz/ntzconf/literal-cols`, `ltz/literal/double-quoted`, `…/double-quoted-cols`; Rust `a_timestamp_ltz_typed_literal_is_a_session_zone_instant`, `a_double_quoted_timestamp_ltz_literal_is_the_same_instant`, `a_timestamp_ltz_word_that_is_not_a_typed_literal_keeps_its_meaning`, `spark_rewrites::timestamp_ltz_literal` tests. |
| C-003 | Reads and writes of a `TIMESTAMP_LTZ` column answer as Spark: rows, NULL, `IS NOT NULL`, range and string-literal comparisons, `ORDER BY` both ways, `CAST` to `STRING`, a `DATE` insert, a DataFrame `writeTo(...).append()`, the `.files` bounds and counts, `days` / `hours` / identity + `months` partitioning and the `.partitions` table, and the session-zone display under America/New_York. | Rows per shape on the facade; the Rust door write, filter and `days` spec. | PROVEN | Facade replay of the remaining `ltz/*` steps; Rust `a_timestamp_ltz_column_writes_reads_and_partitions_by_day`. |
| C-004 | The ANSI door keeps refusing the Spark spelling `TIMESTAMP_LTZ` in a column definition (`Unsupported SQL type TIMESTAMP_LTZ`) and creates no table. | Exact text on the ANSI door. | PROVEN | `crates/repark-sql/tests/ansi_door_u9_types.rs::the_spark_timestamp_ltz_spelling_refuses_on_the_ansi_door`. |
| C-005 | Every `ltz/*` step that does not answer as Spark carries its residue record (R-1..R-5, R-16) with both answers, and the replay holds RePark to that record. | The replay's residue arm; the residue-table cross-check. | PROVEN | `test_every_step_answers_as_spark_measured_or_as_its_dated_residue[ltz]`, `test_every_residue_names_a_ledger_residue_and_differs_from_spark`. |
| C-006 | `map()` (any case, back-quoted or bare, no arguments, unqualified) is Spark's empty map: type `map<void,void>`, value `{}`, `size` 0, a Python `dict`; it writes into a `MAP<…>` column through `INSERT … VALUES` (beside `map('k', 1)` and `NULL`), `INSERT … SELECT`, `CAST(map() AS MAP<…>)`, a nested `MAP<STRING, MAP<…>>` value, `CASE`, `UNION ALL` and `map_concat`, and as the assigned value of `UPDATE … SET`, `MERGE … WHEN MATCHED THEN UPDATE SET`, `WHEN NOT MATCHED THEN INSERT … VALUES` and `WHEN NOT MATCHED BY SOURCE THEN UPDATE SET`. A qualified `s.map()` and `map(k, v, …)` are unchanged. | Rows and types on the facade and the Rust door; AST unit pin. | PROVEN | Facade replay `map/v2/insert`, `map/v3/insert`, `map/literal/*` (EQUAL steps), `map/nested/insert`, `map/v2/insert-select-empty`, `…/insert-empty-typed`, `map/literal/backtick`, `…/backtick-cols`, `map/dml/*`; Rust `an_empty_map_call_is_an_empty_map_of_void`, `a_back_quoted_empty_map_call_is_an_empty_map_of_void`, `update_and_every_merge_clause_assign_the_empty_map`, `keyword_lower::the_probe_finds_only_ns_casts_and_empty_map_calls`, `store_assign::a_map_assigns_when_its_key_and_value_assign`, `a_map_column_takes_literals_the_empty_map_and_null`, `keyword_lower::an_empty_map_call_lowers_to_a_map_of_two_empty_arrays`. Cell `TY-MAP` replays EQUAL on every observation. |
| C-007 | A `MAP<STRING, INT>` column answers as Spark on v2 and v3: metadata `map<string,int>`, `cols` / `DESCRIBE` / `dtypes` / `printSchema`, rows, `IS NOT NULL`, `c['k']` in a filter and a projection, `element_at`, `map_keys` / `map_values`, `size`, `CAST` to `MAP<STRING, BIGINT>`, a DataFrame `writeTo(...).append()` of dicts, the `.files` counts, `ADD COLUMN` of a map, `ALTER COLUMN c.value TYPE BIGINT`, and a `STRUCT` or `MAP` value type. | Rows per shape on the facade. | PROVEN | Facade replay of the EQUAL `map/*` steps. |
| C-008 | The ANSI door keeps its own spellings: `MAP(ARRAY['k'], ARRAY[1])` and the empty `MAP(ARRAY[], ARRAY[])` write into a `MAP(VARCHAR, INTEGER)` column, and the Spark spelling `MAP()` refuses `Function 'map' expected at least one argument but received 0`. Every `map/*` step that does not answer as Spark carries its residue record (R-6..R-13). | Exact rows and text on the ANSI door; the replay's residue arm. | PROVEN | `crates/repark-sql/tests/ansi_door_u9_types.rs::the_ansi_empty_map_is_map_of_two_empty_arrays_and_the_spark_spelling_refuses`; `test_every_step_answers_as_spark_measured_or_as_its_dated_residue[map]`. |
| C-011 | A map assigned by `UPDATE … SET`, MERGE `WHEN MATCHED THEN UPDATE SET` or `WHEN NOT MATCHED THEN INSERT` refuses where its key or value cannot store-assign, with Spark's text: `[INCOMPATIBLE_DATA_FOR_TABLE.CANNOT_SAFELY_CAST] Cannot write incompatible data for the table ``: Cannot safely cast `c`.`key` "STRING" to "INT". SQLSTATE: KD000` (`MAP<INT, STRING>` from `map('1', 'x')`), `` `c`.`value` "STRING" to "INT" `` and `` `c`.`value` "DATE" to "INT" `` (`MAP<STRING, INT>`), and `[INCOMPATIBLE_DATA_FOR_TABLE.CANNOT_FIND_DATA] Cannot write incompatible data for the table ``: Cannot find data for the output column `c`.`value`.`b`. SQLSTATE: KD000` (a struct value missing `b`); nothing is written. UPDATE and MERGE answer the same text. | Exact text on the Rust door; class, condition, SQLSTATE and body on the facade; rows unchanged. | PROVEN | Rust `update_and_merge_refuse_a_map_key_or_value_that_cannot_store_assign`, `update_cast::a_map_key_or_value_that_cannot_store_assign_names_its_path_as_spark_does`, `…::a_map_struct_value_missing_a_field_cannot_find_its_data`, `…::a_map_whose_key_and_value_store_assign_has_no_message`; facade replay `map/assign/*` (the `rows` steps EQUAL, the refusals held by R-18 for the message prefix only). |
| C-012 | A map operand of `=`, `<>`, `<`, `<=`, `>`, `>=`, `<=>`, `IN` or `ORDER BY` refuses on the Spark door, in a SELECT and in an UPDATE or DELETE `WHERE`, with Spark's `[DATATYPE_MISMATCH.INVALID_ORDERING_TYPE] Cannot resolve "(c = map(a, 1))" due to data type mismatch: The `=` does not support ordering on type "MAP<STRING, INT>". SQLSTATE: 42K09` (`<>` names `=` as Spark does; `IN` names `in`, `ORDER BY` names `sortorder` and `c ASC NULLS FIRST`), and `SELECT DISTINCT` over a map column refuses `[UNSUPPORTED_FEATURE.SET_OPERATION_ON_MAP_TYPE] The feature is not supported: Cannot have MAP type columns in DataFrame which calls set operations (INTERSECT, EXCEPT, etc.), but the type of column `c` is "MAP<STRING, INT>". SQLSTATE: 0A000`; the UPDATE and DELETE write nothing. | Exact text on the Rust door; class, condition, SQLSTATE and body on the facade. | PROVEN | Rust `a_map_operand_refuses_comparison_ordering_and_distinct_as_spark_does`; facade replay `map/v2/where-eq`, `map/v2/order`, `map/cmp/*` (held by R-6 for the prefix and Spark's plan tail; MERGE `ON` and `GROUP BY` are R-19). |
| C-009 | A `VOID` column on a format-v3 table is Iceberg `unknown`: CREATE and `ADD COLUMN` commit, `INSERT … VALUES (0, NULL)` writes (no parquet column, as Java's `TypeToMessageType` emits none), the column reads NULL, describes as `void` (`cols`, `DESCRIBE`, `dtypes`), a non-NULL value refuses `CANNOT_SAFELY_CAST`, and v1 / v2 refuse `Invalid schema for v<N>: - Invalid type for c: unknown is not supported until v3` (cell `TY-UNKNOWN-VOID`). | The `void/*` steps replay EQUAL. | PROVEN | 2026-09-25/26 (PR2): 23 of 27 `void/*` steps replay EQUAL; the 4 that cannot (the volatile Py4J call id on the v1/v2 refusals, the planning prefix on the value refusal) are held by R-22, R-23 and R-24. Rust `crates/repark-spark/src/tests/u9_void_uuid.rs` pins the mapping, the v3-only fork refusal, the NULL write, the value refusal and the typed-null cast. Spark measured (group `void`, 21 steps 2026-09-25, +6 steps 2026-09-26); R-14 retired. |
| C-010 | A `uuid` column written through the Iceberg API reads and writes as Spark `string` (`cols`, `DESCRIBE`, a Python `str`): a canonical string literal writes, an upper-case one stores lower case, NULL writes, `u = '…'` filters on the canonical text, and a DataFrame append of a `STRING` column writes (cell `TY-UUID-READ`). | The `uuid/*` steps replay EQUAL. | PROVEN | 2026-09-25/26 (PR2): 25 of 30 `uuid/*` steps replay EQUAL, including `CAST(x AS UUID)` keeping Spark's `UNSUPPORTED_DATATYPE` refusal byte for byte; the 5 that cannot (the RePark-extension `UUID` spelling commit, the volatile task wrappers on invalid literals and files metrics, the `u_bucket` name) are held by R-25, R-26, R-27 and R-28. The presentation lives in `UuidTextSchemaProvider` (`crates/repark-iceberg/src/catalog/uuid_text_schema.rs`), which flips the fork's own `with_uuid_as_string` switch on every catalog-path provider; identity DML that references a uuid column takes the fork path (`selection_refs_needs_fork`). Rust `u9_void_uuid.rs` pins the mapping, the flag, the cast refusal text, the no-op `ALTER … TYPE STRING` and the DELETE/UPDATE smoke. Spark measured (group `uuid`, 12 steps 2026-09-25, +18 steps 2026-09-26); R-15 retired. |
| C-013 | Every RePark-owned write and read path presents a `uuid` column as Spark `string` through one schema presentation (`crates/repark-iceberg/src/catalog/uuid_presentation.rs`): MERGE joins on `t.u = s.u` and `t.u = '…'`, inserts and `UPDATE SET u = s.u` an upper-case string that stores lower case; `INSERT OVERWRITE t SELECT …` (whole table), `INSERT OVERWRITE t PARTITION (p = 2) SELECT …` and `writeTo(t).overwritePartitions()` of a `STRING` column commit and store lower case; `SELECT _file IS NOT NULL, _pos IS NOT NULL, u` and `_partition.p, u` return the text; `DESCRIBE t u` and `SHOW TABLE EXTENDED` say `string`. | Groups `uuid-write` and `uuid-overwrite` replay EQUAL; one Rust pin per path red before the fix. | PROVEN | 2026-09-26 (round-1 fixer, verifier V-002 / V-003 / V-004): 22 of 22 steps EQUAL (Spark measured 2026-09-26 by `target/probe-u9-types-2/measure.py`); `crates/repark-spark/src/tests/u9_uuid_void_writes.rs` (`merge_joins_inserts_and_updates_through_uuid_text`, `insert_overwrite_both_forms_store_uuid_text_lower_case`, `metadata_column_projection_presents_uuid_text`, each red at the old `FixedSizeBinary(16) = Utf8` / `cannot store-assign` refusal) and the kernel pins `crates/repark-iceberg/src/catalog/tests/uuid_presentation.rs`. `DESCRIBE t u` / `SHOW TABLE EXTENDED` measured on both engines 2026-09-26 (`target/probe-u9-types-2/sib.py`). |

## Mutation record (2026-09-25)

Each line was broken, the named tests ran, and the file was restored.

| # | Mutation | Red |
|---|---|---|
| M1 | drop the `"timestamp_ltz"` arm of `iceberg_named_primitive` | Rust `a_timestamp_ltz_column_is_an_iceberg_timestamptz_on_v2_and_v3`, `a_timestamp_ltz_column_writes_reads_and_partitions_by_day`; facade `[ltz]` (CREATE refuses) |
| M2 | stop calling `plan_timestamp_ltz_literal_regions` from `plan_keyword_regions` | Rust `a_timestamp_ltz_typed_literal_is_a_session_zone_instant`, `a_timestamp_ltz_column_writes_reads_and_partitions_by_day`; facade `[ltz]` (`…/literal-cols`) |
| M3 | drop the `is_empty_map_call` arm of `lower_expression` | Rust `an_empty_map_call_is_an_empty_map_of_void`, `a_map_column_takes_literals_the_empty_map_and_null`, `an_empty_map_call_lowers_to_a_map_of_two_empty_arrays`; facade `[map]` |
| M4 | drop the `lower_empty_map_calls` call before the identity-DML path of `execute_passthrough_inner` (r2) | Rust `update_and_every_merge_clause_assign_the_empty_map` (`Function 'map' expected at least one argument but received 0`) |
| R3 | red-first before the r3 fixes (verifier V-001..V-003): `update_cast.rs`, `merge/insert.rs` and the `spark_ast.rs` hook restored to `1141a63f` | Rust `update_and_merge_refuse_a_map_key_or_value_that_cannot_store_assign` (`UPDATE … SET c = map('1', 'x')` committed) and `a_map_operand_refuses_comparison_ordering_and_distinct_as_spark_does` (`WHERE c = map('a', 1)` answered rows); 4 of the 6 `u9_map` tests stayed green |
| R2 | red-first before the r2 fixes (verifier V-001, V-003, V-005) | Rust `update_and_every_merge_clause_assign_the_empty_map`, `a_back_quoted_empty_map_call_is_an_empty_map_of_void`, `a_double_quoted_timestamp_ltz_literal_is_the_same_instant`; after the MERGE lowering the MERGE step still refused `MERGE UPDATE SET cannot store-assign column c: source type Map(… Null …) is not ANSI-store-assignable …` until `ansi_store_assignable` gained Spark's map arm (key and value each store-assignable) |
| M5 | break the `"void"` arm of `iceberg_named_primitive` (`"voix"`) | Rust `void_is_unknown_on_v3_and_refuses_below_v3` (CREATE refuses) |
| M6 | flip `with_uuid_as_string(true)` to `false` in `UuidTextSchemaProvider::table` | Rust `uuid_is_a_string_column_end_to_end` (provider advertises binary) |
| M7 | move the `== SQL ==` window one char (`saturating_sub(32)` to `(31)`) in `uuid_cast.rs` | Rust `cast_to_uuid_refuses_with_sparks_text` (byte mismatch) |
| M8 | drop the uuid-to-string no-op arm of `schema_change_from_alter_column` | Rust `alter_uuid_to_string_commits_without_changing_the_type` (fork refuses) |

## Coverage

```yaml
COVERAGE_ATTESTATION:
  pr_unit: u9-types-1
  categories:
    - id: AT-1
      status: ATTACKED
      evidence: Every clause comes from the Spark measurement committed as the oracle; the TY cells replay through the scoreboard harness and each oracle step replays through the facade, EQUAL or held by its residue record.
      artifacts: [python/repark/tests/u9_types_1_spark_oracle.json, python/repark/tests/test_u9_types_1.py]
    - id: AT-2
      status: ATTACKED
      evidence: Each type is measured at CREATE (v1/v2/v3 where they differ), ADD COLUMN, nested positions, literals and NULLs, reads, filters, ordering, casts, DataFrame appends, partition transforms, ALTER COLUMN TYPE, the files table and a non-UTC session zone. PR2 adds void printSchema/cast-rows/files-metrics/CoW-delete and uuid dtypes/printSchema/order/bucket-part/alter-to-string/invalid-literals/append/delete/update/files-metrics.
      artifacts: [target/probe-u9-types-1/steps.py, crates/repark-spark/src/tests/u9_timestamp_ltz.rs, crates/repark-spark/src/tests/u9_void_uuid.rs]
    - id: AT-3
      status: ATTACKED
      evidence: Refused steps carry class, condition, SQLSTATE and text; a refused CREATE leaves no table (the replay reads the next step against the same catalog).
      artifacts: [python/repark/tests/test_u9_types_1.py, crates/repark-sql/tests/ansi_door_u9_types.rs]
    - id: AT-4
      status: N/A
      justification: No commit, isolation or concurrency path changes; the unit maps type names and rewrites literals before planning.
    - id: AT-5
      status: N/A
      justification: No privileged action, credential, deserialization or path handling; the literal rewrite swaps one keyword token and re-parses through the door's parser.
    - id: AT-6
      status: ATTACKED
      evidence: The type mapper and canonicalize layer suites (create_table, keyword_lower, spark_rewrites, spark_literals) stay green; a timestamp_ltz identifier and a cast target keep their meaning. PR2 adds the void/uuid mapper arms, the cast gate and the alter-column-type extraction with their suites.
      artifacts: [crates/repark-spark/src/spark_rewrites/timestamp_ltz_literal.rs, crates/repark-spark/src/tests/u9_timestamp_ltz.rs, crates/repark-spark/src/void_type.rs, crates/repark-spark/src/uuid_cast.rs, crates/repark-spark/src/alter_column_type.rs, crates/repark-spark/src/tests/u9_void_uuid.rs]
    - id: AT-7
      status: ATTACKED
      evidence: The literal planner is one linear pass over tokens already produced by canonicalize; statements without a quote keep the borrowed fast path.
      artifacts: [crates/repark-spark/src/spark_rewrites/timestamp_ltz_literal.rs]
    - id: AT-8
      status: ATTACKED
      evidence: No new dependency, crate edge or code comment; spark_literals.rs stays at 999 lines (one call renamed), router.rs untouched. PR2 adds no dependency (the provider flip uses trait upcasting, not as-any) and no crate edge; alter.rs ratchets 1353 down to 1272 with its census mirrors, router.rs still untouched.
      artifacts: [crates/repark-spark/src/spark_literals.rs, crates/repark-spark/src/spark_rewrites/mod.rs, crates/repark-spark/src/alter.rs, crates/repark-spark/src/alter_column_type.rs, crates/repark-iceberg/src/catalog/uuid_text_schema.rs]
    - id: AT-9
      status: ATTACKED
      evidence: The registry gains one row per cell; residues cite the registry rows they belong to (TZ-6, TZ-7). PR2 moves TY-UNKNOWN-VOID and TY-UUID-READ to EQUAL with dated notes, the UUID-spelling extension and the R-22..R-28 residues.
      artifacts: [docs/spark-sql-iceberg-parity.md]
    - id: AT-10
      status: ATTACKED
      evidence: Each load-bearing line was broken and its named tests ran red (mutation table). PR2 adds M5 (void arm), M6 (uuid flag), M7 (cast window) and M8 (uuid no-op), each red on its named Rust test before restore.
      artifacts: [target/probe-u9-types-1/red-M1.txt, target/probe-u9-types-1/red-M2.txt, target/probe-u9-types-1/red-M3.txt]
  complete: true
```

## Residues

| # | Residue |
|---|---|
| R-1 | Pre-existing (a bare `TIMESTAMP` column answers the same): a STRING literal in `INSERT … VALUES` into a `TIMESTAMP_LTZ` column writes it (`ltz/v2/side-insert-string`, `ltz/v3/side-insert-string`, and the rows of `…/side-select`), where Spark refuses `[INCOMPATIBLE_DATA_FOR_TABLE.CANNOT_SAFELY_CAST] Cannot write incompatible data for the table … Cannot safely cast `c` "STRING" to "TIMESTAMP". SQLSTATE: KD000`. |
| R-2 | Declared (registry TZ-6, dated note 2026-09-16): the SQL door refuses the `TIMESTAMP_NTZ` type name, so `TIMESTAMP_NTZ '…'` into an LTZ column and `CAST(c AS TIMESTAMP_NTZ)` answer `[UNSUPPORTED_TIMESTAMP_NTZ] … SQLSTATE: 0A000` where Spark writes and casts (`ltz/v2/side-insert-ntz`, `ltz/v3/side-insert-ntz`, `ltz/v2/cast-ntz`). |
| R-3 | `ALTER COLUMN c TYPE TIMESTAMP_NTZ` / `STRING` refuse with Iceberg's `Cannot change column type: c: timestamptz -> timestamp` (`-> string`) as `PySparkException` `DataInvalid => …`, where Spark raises `Py4JJavaError` wrapping `SparkException: Unsupported table change: …` (`ltz/v2/alter-to-ntz`, `ltz/v2/alter-to-string`). |
| R-4 | Ruled (registry TZ-7, Q12): `collect()` of an LTZ column in a non-UTC session answers the session-zone wall (`2023-12-31T19:00:00` for `2024-01-01T00:00Z` under America/New_York), where Spark answers the instant in the Python host zone (`ltz/ny/select`; the host is UTC in the harness). |
| R-5 | Declared (registry TZ-6 residual, Q10): under `spark.sql.timestampType=TIMESTAMP_NTZ` a bare `TIMESTAMP` column is still `timestamptz`, where Spark stores `timestamp` (`ltz/ntzconf/md`, field `d`; the `TIMESTAMP_LTZ` field `c` answers as Spark). |
| R-6 | Text only since r3 (2026-09-25, C-012): the map comparison, `IN`, `ORDER BY` and `DISTINCT` refusals match Spark's class, condition, SQLSTATE and message body, but the facade message keeps RePark's `Error during planning: ` prefix and lacks Spark's `; line 1 pos N;` and analyzed-plan tail (`map/v2/where-eq`, `map/v2/order`, `map/cmp/eq-empty`, `…/eq-cols`, `…/ne`, `…/lt`, `…/null-safe-eq`, `…/in`, `…/projection`, `…/order-desc`, `…/distinct`, `…/update-where`, `…/update-where-literal`, `…/delete-where`). Before r3, `WHERE c = map('k', 1)` answered `[[0]]`, `ORDER BY c` answered rows and `UPDATE … WHERE c = map()` committed. |
| R-7 | `CAST(<map> AS STRING)` refuses `Unsupported CAST from Map(…) to Utf8View`, where Spark renders `{k -> 1}`, `{a -> 2, b -> null}`, `{}` (`map/v2/cast-string`). The complex-to-string cast family is wider than maps: `CAST(array(1, NULL) AS STRING)` renders `[1, ]` for Spark's `[1, null]` and a struct refuses where Spark renders `{1, x}` (`target/probe-u9-types-1/spark-extra.json`). |
| R-8 | Refusals whose condition and SQLSTATE match but whose text does not: `CAST('{k -> 1}' AS MAP<…>)` quotes `Utf8("{k -> 1}")` for Spark's `"{k -> 1}"`, and both it and the `c.key` type change carry RePark's `Error during planning: ` prefix without Spark's position and plan tail (`map/v2/cast-from-string`, `map/v2/alter-key-type`). |
| R-9 | Invalid map values in `INSERT … VALUES` refuse with DataFusion's text as `PySparkException`: `map(1, 'a')` into `MAP<STRING, INT>` (`Cannot cast string 'a' to value of Int32 type`), `map(NULL, 1)` (`map key cannot be null`), `map('d', 1, 'd', 2)` (`map key must be unique, duplicate key found: d`), where Spark raises `INCOMPATIBLE_DATA_FOR_TABLE.CANNOT_SAFELY_CAST` / `KD000` and `INVALID_INLINE_TABLE.FAILED_SQL_EXPRESSION_EVALUATION` / `42000` (`map/v2/insert-wrong-type`, `…/insert-null-key`, `…/insert-dup-key`). |
| R-10 | Partitioning by a map refuses with the fork's `DataInvalid => Cannot partition by non-primitive source field: 'map'.` where Spark raises `ValidationException: Cannot partition by non-primitive source field: map<string, int>` (`map/v2/partition`, `map/v2/create-partitioned`). |
| R-11 | `ALTER COLUMN c TYPE MAP<STRING, BIGINT>` refuses `ALTER COLUMN `c` TYPE to a non-primitive is not supported`, where Spark commits the value promotion (`map/v2/alter-map-type`; `ALTER COLUMN c.value TYPE BIGINT` answers as Spark). |
| R-12 | A field access after a subscript, `c['k'].a`, refuses `Dot access not supported for non-string expr`, where Spark answers the field (`map/struct/field`); `array(named_struct('a', 1))[0].a` refuses the same way where Spark answers `1` (`spark-extra.json`). |
| R-13 | `map()` does not unify with a typed map inside `coalesce`, `if`, `array(…)` or a `VALUES` table (DataFusion's coercion has no `Map<Null, Null>` arm), where Spark answers `map<string,int>` (`map/literal/coalesce`, `…/coalesce-cols`, `…/if`, `…/array-of-maps`, `…/values`). `CASE`, `UNION ALL`, `map_concat` and INSERT targets answer as Spark (C-006). Dated 2026-09-25 (verifier V-002): `SELECT element_at(map(), 'a') AS e` refuses `AnalysisException` `Error during planning: Failed to coerce arguments to satisfy a call to 'element_at' function: coercion from Map("entries": non-null Struct("key": non-null Null, "value": Null), unsorted), Utf8 to the signature UserDefined failed. …`, where Spark answers `NULL` typed `void` (`map/literal/element-at`). |
| R-14 | RETIRED 2026-09-26 (PR2 answers Q1): RP-51 carries F-UNKNOWN-WRITE-1 (#356), so the fork writes `unknown` like Java (omits the Parquet column, all-null `readable_metrics`) and refuses it below v3 with the Java-mirrored `Invalid schema for v<N>` text. The Spark door maps `VOID` to `unknown` on both SQL doors; 23 of 27 `void/*` steps are EQUAL and the rest are held by R-22, R-23 and R-24. Original hold text kept for the record: the door refused `VOID` at CREATE and `ADD COLUMN` and `CAST(NULL AS VOID)`, so every later step answered from the missing table. |
| R-15 | RETIRED 2026-09-26 (PR2 answers Q2): RP-51 carries F-UUID-STRING-1 (#355). The presentation lives in `UuidTextSchemaProvider`, which flips the fork's own `with_uuid_as_string` switch on every catalog-path provider — no RePark-side type wrapper. The Spark door accepts the `UUID` spelling on CREATE / ADD COLUMN as an Iceberg `uuid` column (RePark extension, orchestrator ruling 2026-09-25); `CAST(x AS UUID)` keeps Spark's `UNSUPPORTED_DATATYPE` refusal byte for byte. 25 of 30 `uuid/*` steps are EQUAL and the rest are held by R-25, R-26, R-27 and R-28. Original hold text kept for the record: the door refused `UUID` everywhere while Spark read an API-created column as `string`. |
| R-22 | Dated 2026-09-26: `VOID` on v1 / v2 refuses at CREATE. Spark answers `Py4JJavaError` wrapping `java.lang.IllegalStateException: Invalid schema for v<N>` with a volatile `oNN.sql` call prefix (o56 on 2026-09-25, o45 on 2026-09-26); RePark has no JVM, so the fork refuses the commit with its Java-mirrored `DataInvalid => Invalid schema for v<N>: - Invalid type for c: unknown is not supported until v3` text, carried as `PySparkException`. The Java core text matches; the class and the wrapper line differ (`void/v2/create`, `void/v1/create`). |
| R-23 | Dated 2026-09-26 (text only): a non-NULL value into a `VOID` column refuses with Spark's class, condition, SQLSTATE and message body, but the facade keeps RePark's `Error during planning: ` prefix (`void/v3/insert-value`). |
| R-24 | Dated 2026-09-26: the ADD COLUMN door of R-22 — the fork refuses the v2 schema update with the same Java-mirrored text (`void/v2/addcol-alter`). |
| R-25 | Dated 2026-09-26 (RePark extension, orchestrator ruling 2026-09-25): Spark refuses the `UUID` spelling in SQL (`[UNSUPPORTED_DATATYPE]`, `ParseException`); RePark commits it as an Iceberg `uuid` column, documented in the TY-UUID-READ registry row and the design map (`uuid/sql/create`). |
| R-26 | Dated 2026-09-26: the `bucket(4, u)` spec over a `uuid` column commits, writes and reads on both engines; only the generated field name differs (`u_bucket_4` on Spark, `u_bucket` on RePark — the established `col_bucket` convention, unchanged by this unit) (`uuid/part/md`). |
| R-27 | Dated 2026-09-26: an invalid uuid literal refuses at the task. Spark answers `Py4JJavaError` with a volatile stage wrapper around `java.lang.IllegalArgumentException: Invalid UUID string: <value>` (stages 18.0/19.0 on 2026-09-26); RePark refuses loud with the fork's `DataInvalid => Invalid UUID string: <value>` text as `PySparkException`. The `Invalid UUID string: <value>` core matches (`uuid/v2/insert-invalid`, `uuid/v2/insert-int`). |
| R-28 | Dated 2026-09-26: `.files` `readable_metrics.u` over a `uuid` column. Spark fails `ClassCastException: Cannot cast java.util.UUID to java.lang.CharSequence` inside a volatile `Py4JJavaError` wrapper (stage 30.0); the fork answers byte bounds, which RePark surfaces and the replay holds (`uuid/v2/files-metrics`). |
| R-16 | Dated 2026-09-25 (verifier V-004; pre-existing for `TIMESTAMP 'garbage'`): an invalid `TIMESTAMP_LTZ` typed literal fails at execution as a cast, `SELECT TIMESTAMP_LTZ 'garbage' AS x` answering `PySparkException` `Execution error: [CAST_INVALID_INPUT] The value 'garbage' of the type "STRING" cannot be cast to "TIMESTAMP" because it is malformed. … SQLSTATE: 22018`, where Spark raises `ParseException` `[INVALID_TYPED_LITERAL] The value of the typed literal "TIMESTAMP_LTZ" is invalid: 'garbage'. SQLSTATE: 42604` (`ltz/literal/garbage`). The literal planner only swaps the keyword token and has no timestamp-string parser to refuse with Spark's class before the cast. |
| R-17 | Dated 2026-09-25 (verifier V-006, no code; registry TY-TIMESTAMP-LTZ, ANSI-door paragraph): on the ANSI door `CREATE TABLE … (c TIMESTAMP(6) WITH TIME ZONE)` stores Iceberg `timestamp` (naive, not `timestamptz`), `arrow_typeof(c)` and `DESCRIBE` answer `Timestamp(µs)` with no zone, and `INSERT … VALUES (0, TIMESTAMP '2024-01-01 00:00:00+05:00')` stores the UTC wall `2023-12-31T19:00:00`; `TIMESTAMP WITH TIME ZONE` without a precision refuses `CREATE TABLE: column `d` is TIMESTAMP with nanosecond precision (9), which this engine cannot honor …`. Measured with a throw-away probe test in `crates/repark-sql/tests/ansi_door_u9_types.rs` (removed). The Spark door's `TIMESTAMP_LTZ` column is `timestamptz` (C-001). |
| R-18 | Dated 2026-09-25 (r3, verifier V-002; text only): the refusing map assignments (C-011) match Spark's class, condition, SQLSTATE and message exactly after RePark's `Error during planning: ` prefix, which the facade keeps (`map/assign/*/update`, `…/merge-matched`, `…/merge-insert`, 12 steps). |
| R-19 | Dated 2026-09-25 (r3, verifier V-003): two map-ordering forms do not pass the Spark door's planned-statement walk. `MERGE INTO t x USING (SELECT map('a', 1) AS m) y ON x.c = y.m WHEN MATCHED THEN UPDATE SET note = 'hit'` commits and updates the matching row, where Spark refuses `[DATATYPE_MISMATCH.INVALID_ORDERING_TYPE] Cannot resolve "(c = m)" due to data type mismatch: The `=` does not support ordering on type "MAP<STRING, INT>". SQLSTATE: 42K09` (`map/cmp/merge-on` and the rows after it, `map/cmp/rows`); the MERGE executor plans its join outside the passthrough and `merge.rs` sits at its 970-line ceiling. `SELECT c, count(*) FROM t GROUP BY c` fails `PySparkException` `Arrow error: Not yet implemented: Row format support not yet implemented for: [SortField { … data_type: Map(…) }]`, where Spark 4.1.2 groups by the map and answers `[[{a: 1}, 1], [{}, 1]]` (`map/cmp/group`). |
| R-20 | Dated 2026-09-25 (r3, verifier V-004, no code; for the facade collect-conversion owner): a struct inside a map value collects on the facade as a Python `dict` (`{'a': 1, 'b': 2}`), where PySpark answers `Row(a=1, b=2)`; the values are equal (verifier probe keys `st_ok/upd/rows`, `st_ok/merge/rows`). No oracle step records it: the replay's recording observes the same normalized value on both engines (`map/struct/select` and the `map/assign/*/rows` steps are EQUAL), while the verifier's probe keeps nested values raw and sees the `Row` on PySpark. |
| R-21 | Dated 2026-09-25 (fixer, fnp8 error cells 52 and 53): with `map()` lowered to Spark's empty `map<void,void>` (C-006), `SELECT transform_keys(map(), (k, v) -> k) AS r` planned and then refused `[NULL_MAP_KEY] Cannot use null as map key. SQLSTATE: 2200E` on an empty map, because the shared null-key check refused any `Null`-typed key array whatever its length. It now refuses exactly when a key is null (`logical_null_count`), so `transform_keys`, `transform_values`, `map_filter` and `map_zip_with` over `map()` answer `{}` as Spark's `collect()` does (`fnp8_error_oracle.json` rows 52 and 53, ANSI on and off; Spark's `ValueError: A null type field may not be non-nullable` is PySpark's Arrow export, kept apart by `test_live_fnp8_empty_map_collect_diagnostic`), and a produced null key, `NULL` or a typed null, still refuses with that text. Pins: Rust `sql_door_map_kernels_over_the_empty_map_answer_the_empty_map`, `sql_door_transform_keys_to_a_null_key_still_refuses`; `fnp8_repark_errors.json` keys `52-sql` and `53-sql` now record RePark's collected empty map. Still divergent: `F.expr` does not lower `map()` (the lowering runs on the Spark SQL door only), so keys `52-expr` and `53-expr` keep `Error during planning: Function 'map' expected at least one argument but received 0`. |
