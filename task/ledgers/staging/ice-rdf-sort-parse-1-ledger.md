# Unit ledger — ICE-RDF-SORT-PARSE-1 · CALL rewrite_data_files strategy and sort_order (IPI-43)

**Date:** 2026-09-20 (round 1 built the code and pins; round 2 filed this ledger; round 3 closes V-001..V-005) · **Branch:** `fix/ipi-43-rdf-sort-zorder` · **Base:** `d89f8130` · **Model:** muse-spark-1.3-contributor · **Policy:** [../../../AGENTS.md](../../../AGENTS.md).
**Path:** STANDARD. **risk_tier: standard.**

**Retires:** this ledger moves to `../completed/` when the unit's last commit lands.

**Why now.** `strategy => 'sort'` and `sort_order => …` on `CALL … rewrite_data_files` were blanket refusals. They now parse and route onto the fork's `RewriteStrategy::{SortByTableOrder, Sort, ZOrder}`, which the pinned fork rev already carries (round-1 commit `20ac235d`). The three inventory cells this unit closes — `P-RDF-SORT`, `P-RDF-SORT-TABLE-ORDER`, `P-RDF-ZORDER` — record the same output row, the same `files_after`, the same rows and the same metadata on Spark, so a rewriter that ignored `strategy` and `sort_order` and bin-packed would turn all three EQUAL while doing nothing. The pins therefore read each rewritten data file straight out of parquet and assert the row order inside it, against the 7-cell Spark oracle the recorder beside them measured on live Spark (`ice_rdf_sort_parse_1_spark_oracle.json`).

**Not in this unit:** the `branch` argument (the fork builder has no branch parameter, so accepting it would ship a silently ignored argument; IPI-31 owns it); non-`zorder` transform sort terms (a registered CALL-door refusal, `RDF-SORT-TRANSFORM-1`, not a parse failure); `STATUS.md`; version numbers.

## PROPOSITION LEDGER — ICE-RDF-SORT-PARSE-1 — 2026-09-20

| Clause | Proposition (checkable) | Proof obligation | Verdict | Evidence / open question |
|---|---|---|---|---|
| C-001 | The shared identity grammar parses a bare name, `ASC` and `DESC` with Java's direction-tied NULLS defaults (a bare name and `ASC` map to NULLS FIRST, `DESC` maps to NULLS LAST); an explicit NULLS FIRST or NULLS LAST overrides the default; a term list keeps its order and dotted names survive (`st.a DESC, id, b ASC NULLS LAST`). | The three grammar tests in `crates/repark-spark/src/tests/sort_order_parse.rs`. | **OPEN** | `bare_direction_picks_javas_tied_null_order`, `explicit_null_order_overrides_the_tied_default` (covers lowercase `desc nulls last`) and `a_list_keeps_its_order_and_dotted_names` are committed on the branch. Closing needs a mutation run no round did. |
| C-002 | Malformed identity terms classify as `OrderParseError` variants for the calling door to render — `Empty`, `QuotedName`, `MissingName`, `Transform` with the transform name, `BadNulls` with name and got word, `Trailing` with name and got word. The pin is the variant only, never a rendered string: ALTER keeps its own text and CALL renders Java's `Unable to parse sortOrder`. | `malformed_terms_are_classified_for_the_door_to_render` asserts variants over six inputs. | **OPEN** | Committed on the branch; the test pins the error class, not the message, so neither door's wording is pinned here. Closing needs a mutation run no round did. |
| C-003 | `parse_zorder_columns` answers `Columns` on every spelling (`zorder(id, data)`, `ZORDER(id, data)`, spaced `ZOrder ( id , data )`), merges several `zorder(…)` terms into one column list, and forwards an empty `zorder()` as `Columns([])` so that `Cannot ZOrder when no columns are specified` comes from the fork alone; it answers `Absent` for identity terms, a bare `zorder` name and transform terms, and `Mixed` when identity and z-order terms share one `sort_order`. | The two scan tests in `crates/repark-spark/src/tests/sort_order_parse.rs`. | **OPEN** | `zorder_is_recognised_in_every_spelling` and `zorder_scan_separates_absent_from_mixed` are committed on the branch. Closing needs a mutation run no round did. |
| C-004 | `strategy => 'sort'` with `sort_order => 'id DESC NULLS LAST'` sorts each rewritten file into Spark's measured order (rewritten 5, added 2; cat x `[8, 7, 6, 4, 3, 1]`, cat y `[5, 2]`); the procedure records nothing on the table (its default sort order stays empty, pinned against the recorded post-rewrite sort state, and the new snapshot's operation is Spark's `replace`); the sort and z-order cells differ inside the file, so a bin-packing no-op fails; `strategy` routes through the positional door too (named and positional `sort` refuse on an unsorted table with the fork's full message by equality, positional `binpack` runs, a third positional refuses on arity); `tests/call.rs` carries the 1303 to 1287 shrink-only ratchet. | Rust `call_rdf_sort_explicit_order_writes_descending_ids` plus `call_rewrite_positional_strategy_routes_to_the_rewriter`; Python `test_rdf_sort_explicit_order` plus `test_rdf_sort_is_not_a_no_op`; the `test_cap_1_source_file_line_cap.py` mirror row. | **OPEN** | Committed on the branch: the Rust pin fuses five files into one with ids `[8, 5, 3, 2, 1]` and asserts zero sort-order fields; the not-a-no-op pin asserts the oracle tells sort from z-order before rewriting; the mirror row pins `tests/call.rs` at 1287. Round 3: V-001 re-pins the positional refusal to the full message by equality (line-neutral, the ratchet still reads 1287); V-005 adds the sort-state and `replace` assertions to `_assert_cell`. Closing needs a mutation run no round did. |
| C-005 | `strategy => 'sort'` with no `sort_order` falls back to the table's own order: a table `WRITE ORDERED BY (id DESC NULLS LAST)` rewrites into the same in-file orders and the same output row as the explicit spelling, keeps that default order (still `id DESC NULLS LAST` against the recorded post-rewrite sort state), and commits Spark's `replace` operation. | Rust `call_rdf_sort_without_sort_order_uses_the_table_order`; Python `test_rdf_sort_table_order` against the oracle `tableorder` cell. | **OPEN** | Committed on the branch: the Rust pin fuses to one file with ids `[8, 5, 3, 2, 1]`; the oracle `tableorder` cell matches the `sort` cell file for file. Round 3 (V-005): `_assert_cell` pins the recorded sort state and `replace`, with a shape check that the recorded default is `id DESC NULLS LAST`; both sides sort the order list by order id because the fork lists the default first where Java lists id-ascending. Closing needs a mutation run no round did. |
| C-006 | `sort_order => 'zorder(id, data)'` interleaves and lands where Spark lands (rewritten 5, added 2; cat x `[1, 3, 4, 6, 7, 8]`, cat y `[2, 5]`), records nothing on the table (pinned against the recorded post-rewrite sort state), and commits Spark's `replace` operation. | Python `test_rdf_zorder` against the oracle `zorder` cell. | **OPEN** | Committed on the branch. Round 3 (V-005): `_assert_cell` pins the recorded sort state and `replace`. Closing needs a mutation run no round did. |
| C-007 | On the two-single-character-STRING fixture, `zorder(p, q)`, `zorder(q, p)` and `id DESC` land in three different measured orders (`[5, 8, 3, 4, 1, 7, 2, 6]`, `[5, 8, 4, 3, 2, 1, 7, 6]`, `[8, 7, 6, 5, 4, 3, 2, 1]`); the packet's BIGINT plus STRING fixture does not discriminate because the string dominates the interleave. The flat fixtures are read partition-free: `_file_ids_flat` selects no partition column because unpartitioned `$files` has none until IPI-20 lands. | Python `test_rdf_zorder_is_column_order_sensitive` against the three oracle `zdisc` cells, asserting the oracle discriminates all three first; `_file_ids_flat` is the reader (round-1 commit `1205af7d`, packet §11 A-1). | **OPEN** | Committed on the branch. Closing needs a mutation run no round did. |
| C-008 | A bare `id DESC` with no NULLS clause puts the NULL last at runtime (`[8, 5, 3, 1, null]`, rewritten 5, added 1), pinning the direction-tied default outside the parser's unit tests. | Python `test_rdf_sort_bare_desc_puts_nulls_last` against the oracle `nulls` cell, asserting the trailing id `is None`. | **OPEN** | Committed on the branch. Closing needs a mutation run no round did. |
| C-009 | Every refusal is Java's or the fork's text under Java's class. Sorting an unsorted table with no order raises `IllegalArgumentException` with the fork's full message by equality (`Cannot sort data without a valid sort order, table '…' is unsorted and no sort order is provided`), and compacts nothing; the class comes from a declared remap, a three-needle message match applied ONLY at the `action.execute()` call site in `run_rewrite`, re-raising `error.message()` so no `DataInvalid => ` kind prefix survives, while the unknown-strategy and binpack arms in `resolve_strategy` run before execute and are untouched. `binpack` plus any `sort_order`, identity or z-order spelling, is `Cannot set rewrite mode, it has already been set to BIN-PACK`, raised before options validate. An unknown z-order column and an empty z-order raise the fork's full texts verbatim by equality (the unknown-column text carries the struct suffix). Mixed identity and z-order terms raise `Cannot mix identity sort columns and a Zorder sort expression`, checked before the strategy dispatch. A non-`zorder` transform term is the registered `RDF-SORT-TRANSFORM-1` refusal (`UnsupportedOperationException`). Layout options are accepted exactly where Java accepts them (z-order takes all four) and refused with `Cannot use options […]` naming the resolved rewriter (`BIN-PACK`, `SORT`) everywhere else. | Rust `call_rdf_sort_on_unsorted_table_surfaces_the_fork_refusal`, `call_rdf_binpack_with_sort_order_matches_javas_rewrite_mode_message`, `call_rdf_zorder_refusals_come_from_the_fork`, `call_rdf_sort_order_parse_refusals_match_java`, `call_rdf_layout_options_follow_the_rewriter`; Python `test_rdf_sort_on_unsorted_table_refuses`, `test_rdf_binpack_with_sort_order_refuses`, `test_rdf_zorder_refusals_are_the_forks`, `test_rdf_sort_order_transform_is_a_declared_refusal`, `test_rdf_mixed_sort_terms_refuse`. | **OPEN** | Committed on the branch: the remap is `is_fork_validation_refusal` (round-1 commit `2de66930`), which also closed the unsorted-table refusal surfacing as base `PySparkException`. Round 3: V-001 re-raises `error.message()` (commit `e76f9e1e`, accessor verified in the pinned fork checkout) and re-pins every fork refusal to the full message by equality in Rust and Python; V-002 and V-003 re-pin the two facade rows and the maintenance-call row to the same equality. The pre-existing unknown-strategy pin tests are unchanged on the branch; refusal-before-options and mix-before-dispatch orderings are code-read (`resolve_strategy` precedes `parse_rdf_options`, the `Mixed` arm precedes the strategy match) with no dual-violation pin. Closing needs a mutation run no round did. |
| C-010 | An omitted `strategy` with a `sort_order` sorts rather than bin-packs: an unknown field (`nope ASC`) raises `Cannot find field`, and a valid order fuses the files into Spark's measured `zdisc_id_desc` order. | Rust `call_rdf_sort_order_without_strategy_is_consulted`; Python `test_rdf_sort_order_without_strategy_still_sorts` against the oracle `zdisc_id_desc` cell. | **OPEN** | Committed on the branch. Round 3 (V-002): the facade bare-`sort_order` pin is re-pinned from the old refusal to the new measured success (Spark-recorded all-zero row and file order). Closing needs a mutation run no round did. |
| C-011 | Default (no strategy argument) and explicit `binpack` compact exactly as before this unit: output row `[5, 2, >0, 0, 0]`, per-file record counts `[6, 2]`, all eight ids present. | Python `test_rdf_binpack_unchanged`, the regression pin for the untouched bin-pack path. | **OPEN** | Committed on the branch. Closing needs a mutation run no round did. |

## Round-1 defects closed (evidence on the branch)

1. `2de66930` (fix, IPI-43): fork sort and z-order validation refusals raise `IllegalArgument`. `is_fork_validation_refusal` in `crates/repark-spark/src/call/rewrite_data_files.rs` matches three needles — `Cannot sort data without a valid sort order`, `Cannot ZOrder when no columns are specified`, `Cannot find column '` — and the match guards ONLY the `action.execute()` call site in `run_rewrite`, re-raising through `illegal_argument_error` with the fork text verbatim so the door reports Java's class as well as Java's text. `resolve_strategy` runs before execute, so its unknown-strategy arm (`DataFusionError::Plan`, `unsupported strategy`) and its binpack arm (already `illegal_argument_error`) are untouched. The same commit closed the unsorted-table refusal surfacing as base `PySparkException`: the Python pin now asserts `IllegalArgumentException`, which subclasses `PySparkException` (`crates/repark-python/src/exceptions.rs`), so old `except PySparkException` catches still catch.
2. `1205af7d` (test, IPI-43): partition-free file reader for the flat fixtures. `_file_ids_flat` in `python/repark/tests/test_ice_rdf_sort_parse_1.py` selects `file_path, record_count` only, stamps `partition: None`, and sorts by ids, per packet §11 A-1. It reads the C-007, C-008 and C-010 fixtures and the flat C-009 refusal fixtures.
3. The unsorted-table refusal that surfaced as `PySparkException` is the symptom item 1 closed: before the remap the fork's validation error crossed the boundary as the base class; after it, the door raises `IllegalArgumentException` with the fork text verbatim, pinned in Rust by message needles and in Python by class plus needles.

## Round-3 findings closed (V-001..V-005) — 2026-09-20

The verification critic on `2de66930` returned NEEDS_REMEDIATION; round 2 filed the ledger but
did not touch the findings. Each row below says how the finding was closed and where.

| Finding | How closed | Where |
|---|---|---|
| V-001 (P1) | The remap re-raises `error.message()` instead of `error.to_string()`, so the `DataInvalid => ` kind prefix is gone and the door answers the bare fork text; the accessor was verified public in the pinned fork checkout (`3ed905c6`, `crates/iceberg/src/error.rs`), so no `Display` split was needed. Every refusal assertion for the three fork messages pins the FULL message by equality in Rust (`External error: …` with table identifier and struct suffix) and Python (`str(exc)`). The needle match is untouched. | Fix `e76f9e1e`: `crates/repark-spark/src/call/rewrite_data_files.rs`; pins in `crates/repark-spark/src/tests/call_rdf_sort.rs`, `crates/repark-spark/src/tests/call.rs` (line-neutral, ratchet still 1287), `python/repark/tests/test_ice_rdf_sort_parse_1.py`. |
| V-002 (P1) | Both facade pins rewritten to the new measured behavior, names kept: the bare-`sort_order` case asserts the CALL succeeds with the Spark 4.1.2 recorded row (all zeros on the single-file fixture), the unchanged file with id 1, and no new snapshot; the `strategy => 'sort'` on unsorted case asserts `IllegalArgumentException` with the exact V-001 fork message. Neither test deleted nor xfailed. | `b56fbb55`: `python/repark/tests/test_rewrite_data_files_options.py`. |
| V-003 (P2) | The maintenance-call pin asserts the exact class (`IllegalArgumentException`) and the exact fork message by equality on both the named and the positional spelling. | `12e83ed4`: `python/repark/tests/test_maintenance_call.py`. |
| V-004 (P2) | `RDF-SORT-1` carries FIXED 2026-09-20 (ICE-RDF-SORT-PARSE-1): the repark bullet states the routed behavior, the pin bullet names the unit's pins, the rationale states what replaced the refusal, and the `V3-COV-5` cross-reference gains its dated retirement note. New DECLARED row `RDF-SORT-TRANSFORM-1` files the CALL-door transform-term refusal with the two pins that hold it. | `d0996905`: `docs/spark-sql-iceberg-parity.md`. |
| V-005 (P2) | `_assert_cell` pins the recorded post-rewrite sort state (`sort-orders` plus default id) and the current snapshot's `replace` operation, with a shape check that the recorded tableorder default is `id DESC NULLS LAST`. The recorder carries all three per cell, re-recorded from live Spark 4.1.2 with every pre-existing key byte-identical. Both sides sort the order list by order id: the fork lists the default first where Java lists id-ascending, a serialization order the rewrite does not move (verified pre/post on the engine). A gate run caught `snapshots[-1]` as the wrong head — the fork serializes `snapshots` in hash order, a different order on each of three measured runs — so both readers resolve the head by `current-snapshot-id`; the re-record came back identical. | `0e004959` plus the step-7 fix commit: `python/repark/tests/test_ice_rdf_sort_parse_1.py`, `python/repark/tests/_record_ice_rdf_sort_parse_1_oracle.py`, `python/repark/tests/ice_rdf_sort_parse_1_spark_oracle.json`. |

## Declared divergences

The PR body declares two judgement calls as divergences. A build lane cannot read the PR body (no `gh` from the lane), so the sentences below quote the branch's own artifacts — the round-1 commit messages, the code, and the pinning map rows — and they stand as clause text in C-007 and C-009, not as footnotes here.

1. The sort and z-order validation refusals are re-raised through `illegal_argument_error` with the fork text verbatim, by a three-needle message match applied ONLY at the `action.execute()` call site in `run_rewrite`; the unknown-strategy and binpack arms run before execute and are untouched. (C-009)
2. The flat fixtures are read partition-free: `_file_ids_flat` selects no partition column because unpartitioned `$files` has none until IPI-20 lands, per packet §11 A-1. (C-007)

## Observed, out of unit

- **Closed round 3 (V-002, commit `b56fbb55`).** Two pre-existing Python pins in `python/repark/tests/test_rewrite_data_files_options.py` still expect the pre-unit refusal and contradict C-004 and C-010: `test_rewrite_sort_order_refuses_loud` expects `sort_order.*not supported`, but a bare `sort_order` now sorts; `test_rewrite_sort_strategy_refuses_loud` expects `sort.*not supported`, but `strategy => 'sort'` on an unsorted table now raises `IllegalArgumentException` with the fork's unsorted-table text. Round 2 left both untouched; round 3 rewrote both to the new measured behavior.
- **Closed round 3 (V-004, commit `d0996905`).** The CALL-door map row names a registry row `RDF-SORT-TRANSFORM-1` for the transform-term refusal, but no such row exists under `docs/` on this branch. Round 3 filed it.
- The retired `RDF-SORT-1` row keeps its 2026-08-31 Spark bullet, which says a bare `sort_order` still binpacks on Spark — while this unit (packet A-2, settled) routes a bare `sort_order` to the sort rewriter per the Java `checkAndApplyStrategy` transcription. Round 3 recorded Spark 4.1.2 bare-`sort_order` only on a single-file fixture (all-zero row either way, inconclusive on sort-vs-binpack), so the tension is observed, not resolved; settling it needs a multi-file bare-`sort_order` cell on live Spark. Flagged for the orchestrator.
- The fork lists `sort-orders` default-first where Java lists id-ascending (round-3 measurement, V-005). Content-identical; the pins sort by order id. Owned by the fork, not this unit.
- The fork serializes metadata.json `snapshots` in hash order (round-3 measurement: three runs, three orders), where Java appends oldest-first. An array position is not the head; both round-3 metadata readers resolve by `current-snapshot-id`. Owned by the fork, not this unit.
- The engine names its first metadata file `00000-<uuid>.metadata.json` and writes `version-hint.text` only once commits land; a reader that trusts the hint glob or a lexicographic max reads stale metadata past nine versions. Both round-3 metadata readers resolve versions numerically. The sibling `_metadata` helper in `test_write_order_dist_1.py` shares the latent shape; flagged for the orchestrator.

## Open questions (why every verdict is OPEN)

Each clause closes by mutation: break the implementation the pin guards (drop the remap arm, swap a NULLS default, route `zorder(q, p)` as `zorder(p, q)`, record the order on the table, …) and watch the named pin redden. Round 2 ran no mutation and no cargo; round 3 ran the Rust and Python suites but no mutation either, so nothing here is claimed PROVEN and no mutation record is filed.
