# Unit ledger — U8 WRITE-SQL PR1 · REPLACE WHERE, the INSERT PARTITION clause, bucketed INSERT

**Date:** 2026-09-24 · **Branch:** `feat/u8-write-sql` · **Base:** `fb41309f`
(`origin/main`) **Model:** Claude Opus 5.5 (`claude-opus-5-5`) · **Policy:** [../../../AGENTS.md](../../../AGENTS.md).
**Path:** STANDARD. **risk_tier: standard.**

**Retires:** this ledger moves to `../completed/` when the unit's last commit lands.

**Why now.** Three scoreboard cells of the 2026-09-24 run answered wrong on the Spark door:
`W-INSERT-OVERWRITE-WHERE` did not parse, `W-INSERT-PARTITION-CLAUSE` answered DataFusion's
`Partitioned inserts not yet supported`, and `W-INSERT-BUCKETED` leaked DataFusion's
`Projections require unique expression names`. The third is not a bucket problem: a cast keeps
its input's name in DataFusion, so `SELECT id, CAST(id AS STRING)` repeats `id` in any source.

**What Spark does (measured 2026-09-24; every measurement is committed as `python/repark/tests/u8_write_sql_spark_oracle.json` (sha256 237118c9…, generator `target/probe-u8-r3fix/build_oracle.py`, 584 cases), keys `pr1/…`; originally `target/probe-u8-pr1/spark.json`, `spark2.json`,
`spark3.json`, `spark4.json`, 128 probes).** `INSERT INTO t REPLACE WHERE p q` is
`OverwriteByExpression`, which Iceberg runs as `OverwriteFiles.overwriteByRowFilter(p)` with
the query's files added and no check that they match `p`. A file whose rows match `p` only in
part refuses `ValidationException: Cannot delete file where some, but not all, rows match
filter …`. An empty query commits `delete`, even when nothing matches; a literal `false`
predicate commits `append`. The predicate converts by Spark's V2-filter rules or refuses
`IllegalArgumentException: Cannot convert Spark predicate to Iceberg expression: …`.
`REPLACE WHERE` is only legal right after `INSERT INTO [TABLE] <name>`. The `PARTITION`
clause on `INSERT INTO` fills each static key with its value cast from its string form and
takes a dynamic key from the query; only identity partition columns are accepted. A bucketed
`INSERT … FROM range(20)` writes 20 rows in four files, one per bucket.

## Clauses

| Clause | Statement | Proof obligation | Verdict | Evidence |
|---|---|---|---|---|
| C-001 | `INSERT INTO t REPLACE WHERE cat = 'x' SELECT 9, 'z', 'x'` on a `cat`-partitioned table leaves `[[2,b,y],[9,z,x]]` and commits `overwrite` with added-records 1, deleted-records 2, total-records 2, added-data-files 1, deleted-data-files 1, changed-partition-count 1. | Rows, summary and file layout on the facade and the Rust door; the scoreboard cell. | PROVEN | `test_replace_where_replaces_the_matching_partition`; Rust `replace_where_on_a_partition_replaces_its_rows_with_the_query`. Cell `W-INSERT-OVERWRITE-WHERE` replays EQUAL on the whole observation. |
| C-002 | The spellings Spark accepts write the same rows: `INSERT INTO TABLE`, lower case, a comment inside `REPLACE WHERE`, `VALUES` (one or two rows), a parenthesized query, a CTE query, a cast column, `!=`, `NOT`, `NOT IN`, `LIKE 'x%'`, `<=>`, a literal on the left, a query that reads the target, and query rows outside the predicate (written unchecked). `IS NOT NULL`, `BETWEEN`, `>=` and `IN` over every row replace the whole table. | One row pin per spelling. | PROVEN | `test_replace_where_accepts_the_spellings_spark_accepts`, `test_replace_where_reads_its_source_and_writes_it_unchecked`; Rust `replace_where_accepts_the_spellings_spark_accepts`, `replace_where_reads_the_target_before_it_replaces_it`, `replace_where_writes_rows_outside_the_predicate_unchecked`. Probes RW-*, RW2-*, RW4-values-multi. |
| C-003 | The snapshot follows what the commit changes: no match → `overwrite` add-only; empty query → `delete` (also when nothing matches, `changed-partition-count` 0, and on a table with no snapshot); `true` → `overwrite` of every row; `false` → `append`; an unpartitioned `id >= 1` → `overwrite` of the one file. | Operation and counts per case. | PROVEN | `test_replace_where_snapshot_follows_what_the_commit_changes`; Rust `replace_where_snapshot_operations_follow_what_the_commit_changes`, `replace_where_on_an_unpartitioned_table_replaces_whole_files_only`; iceberg `an_empty_source_commits_a_delete_even_when_nothing_matches`, `an_empty_source_on_a_table_without_snapshots_commits_a_delete`. |
| C-004 | Refusals commit nothing and carry Spark's condition, SQLSTATE and text: `UNSUPPORTED_FEATURE.OVERWRITE_BY_SUBQUERY` / `0A000`; `INVALID_NON_DETERMINISTIC_EXPRESSIONS` / `42K0E` (`"(rand() < 2)"`); `IllegalArgumentException` `Cannot convert Spark predicate to Iceberg expression: UPPER(cat) = 'X'` and `…: null` for `cat = NULL`; `INSERT_COLUMN_ARITY_MISMATCH.NOT_ENOUGH_DATA_COLUMNS` / `21S01`; `TABLE_OR_VIEW_NOT_FOUND` / `42P01`; `ParseException` `PARSE_SYNTAX_ERROR` / `42601` near `'REPLACE'` after `OVERWRITE`, a column list, `BY NAME` or `PARTITION`, and near `'BY'` after the predicate; and the partial-file message `Cannot delete file where some, but not all, rows match filter …` (class and filter spelling are R-1). | Exact class + condition + SQLSTATE + message pins, table unchanged. | PROVEN | `test_replace_where_refusals_match_spark_and_commit_nothing`, `test_replace_where_that_splits_a_file_refuses_like_iceberg`; Rust `replace_where_refusals_carry_spark_text_and_commit_nothing`, `misplaced_replace_where_is_a_spark_parse_error`; iceberg `untranslatable_predicates_refuse_with_the_spark_message`, `a_reference_in_another_case_fails_the_iceberg_field_lookup`, `a_filter_that_splits_a_file_refuses_before_any_commit`. |
| C-005 | `INSERT INTO t.branch_b1 REPLACE WHERE …` commits on the branch and leaves `main` unchanged. | Rows of `main` and of the branch. | PROVEN | `test_replace_where_on_a_branch_leaves_main_alone`; Rust `replace_where_writes_to_a_named_branch_only`. Probe RW-branch. |
| C-006 | `INSERT INTO t PARTITION (cat = 'q') SELECT 9, 'z'` appends `[9, z, q]` with one `append` snapshot (added-records 1, added-data-files 1, changed-partition-count 1), and so do `VALUES`, `TABLE`, an upper-case key, the dynamic `PARTITION (cat)` forms, both column-list orders and `BY NAME`. A two-key spec takes static and dynamic keys in any order. | Rows and summary per spelling. | PROVEN | `test_a_static_partition_value_fills_the_missing_column`, `test_two_key_specs_take_static_and_dynamic_values_in_any_order`; Rust `a_static_partition_value_fills_the_missing_column`, `two_key_specs_take_static_and_dynamic_values_in_any_order`. Cell `W-INSERT-PARTITION-CLAUSE` replays EQUAL. |
| C-007 | A static value is its Spark string form cast to the column type: `id = '7'` and `id = 8` → BIGINT, `cat = 5` → `'5'`, `cat = true` → `'true'`, `cat = NULL` → NULL, `d = '2024-01-05'` → DATE. | Rows per value. | PROVEN | `test_static_values_follow_spark_string_casts`; Rust `static_values_follow_spark_string_casts`. Probes PC-str-into-bigint, PC-int-into-*, PC-null, PC2-bool-into-string, PC2-date-part, PC4-date. |
| C-008 | Clause refusals commit nothing: `NON_PARTITION_COLUMN` / `42000` for a non-partition column, an unknown name, an unpartitioned table and a `bucket` source; `ParseException` `DUPLICATE_KEY` / `23505` on `INSERT INTO` and `INSERT OVERWRITE`; `CAST_INVALID_INPUT` / `22018` (`'abc'`, `'7.5'` into BIGINT, on append and overwrite; class R-3); `INSERT_COLUMN_ARITY_MISMATCH` naming the static column at its table position (`9`, `cat` and `9`, `z`, `cat`, `q`) and, for a dynamic-only clause, the query's columns; `STATIC_PARTITION_COLUMN_IN_INSERT_COLUMN_LIST` / `42713`. | Exact class + condition + SQLSTATE + message pins. | PROVEN | `test_partition_clause_refusals_match_spark_and_commit_nothing`; Rust `partition_clause_refusals_carry_spark_text_and_commit_nothing`, `a_bad_static_value_refuses_as_an_illegal_argument`, `a_dynamic_only_clause_keeps_the_spark_arity_refusal`. Probes PC-*, PC2-dyn-short, PC4-static-in-list. |
| C-009 | A `PARTITION` insert into `t.branch_b1` commits on the branch only; `INSERT OVERWRITE` and `INSERT INTO` with an invalid DATE value refuse `CAST_INVALID_INPUT` (Spark's text, byte-equal); a static `INSERT OVERWRITE … PARTITION (cat = 'x')` still replaces that partition. Supersedes the refusal text of ice-overwrite-mode-1/C-013 (noted in that ledger). | Branch rows; exact refusal; overwrite rows. | PROVEN | `test_partition_inserts_reach_branches_and_validate_overwrite_values`, `test_invalid_static_date_refuses_with_spark_cast_invalid_input`; Rust `a_partition_insert_writes_to_a_named_branch_only`, `static_value_is_cast_to_a_date_partition_and_an_invalid_value_refuses` (re-pinned). Probes PC2-branch, PC3-*. |
| C-010 | On a `write.spark.accept-any-schema` table a `PARTITION` append resolves by name through U6's door: `SELECT 9, 'z'` refuses `Field 9 not found in source schema`, `VALUES (9, 'z')` refuses `Field col1 …`, an extra alias refuses `Field extra …`, and `SELECT 9 AS id, 'z' AS data` writes `[9, z, x]`. A positional `VALUES` keeps U6's `Field col1` refusal. This retires part (b) of u6-write-refusals residue R-3 for appends. | Class + message pins; rows. | PROVEN | `test_partition_inserts_on_accept_any_tables_resolve_by_name`; Rust `a_partition_insert_on_an_accept_any_table_resolves_by_name`. Probes PC2-accept-any, PC3-accept-any-alias; U6 probes e1–e10. |
| C-011 | A positional INSERT source whose later item repeats an earlier output name (a cast keeps its input's name) plans and writes: `INSERT INTO t SELECT id, CAST(id AS STRING), 'k' FROM range(20)` into `bucket(4, id)` appends 20 rows in four files (`append`, added-data-files 4, changed-partition-count 4) with Spark's per-bucket record counts; aliased sources, `INSERT OVERWRITE` (static and dynamic), identity, truncate and unpartitioned tables answer as Spark does. `BY NAME` keeps its names. | Rows, summaries and file layout. | PROVEN | `test_a_bucketed_insert_from_range_matches_spark_layout`, `test_bucketed_overwrites_and_other_layouts_from_range`; Rust `a_cast_that_keeps_its_input_name_no_longer_collides_in_a_positional_source`, `a_bucketed_insert_writes_one_file_per_bucket`, `the_source_rename_leaves_by_name_inserts_alone`. Cell `W-INSERT-BUCKETED` replays EQUAL. |
| C-012 | Near misses keep their answers: a `WHERE` inside the query (`… FROM t AS replace WHERE cat = 'y'`) is a plain append; plain `INSERT … VALUES` and `INSERT OVERWRITE … VALUES` are unchanged. | Rows and operations. | PROVEN | `test_near_misses_keep_their_answers`; Rust `a_where_inside_the_query_is_not_replace_where`. |
| C-013 | The overwrite-by-filter kernel lives in `repark-iceberg`: `spark_overwrite_filter` (sqlparser predicate → Iceberg filter by Spark's optimizer and V2 rules; the null semantics are C-015) and `commit_overwrite_by_filter_with_summary` (row filter, no added-file validation, isolation and branch as the sibling commits; an empty source still commits `delete`). | Kernel unit pins. | PROVEN | iceberg `spark_translatable_predicates_convert_to_iceberg_filters`, `not_pushes_down_through_comparisons_and_connectives_as_spark_does`, `in_lists_follow_spark_null_semantics`, `a_whole_partition_filter_replaces_its_rows_as_an_overwrite`, `added_rows_outside_the_filter_are_not_validated`. |
| C-014 | The facade's bare-name expansion leaves a `REPLACE WHERE` predicate alone and expands only the query, so a CTE in the query keeps its scope. | The CTE spelling through `spark.sql`. | PROVEN | `test_replace_where_accepts_the_spellings_spark_accepts` (CTE case); before the fix it answered `table 'hc.default.s' not found` under a non-default current catalog. |
| C-015 | *Added round 2 (2026-09-25, critic r1 V-001, V-002); narrowed round 4 (2026-09-25, critic r3 V-006).* A NULL partition key **in its own manifest** (written by its own INSERT, as every `r1/…`, `r2/…` and `r3b/…-split-…` / `…-three-…` probe seeds it) is kept or replaced as Spark keeps or replaces it; a NULL key written by the same INSERT as the matching keys is R-12. `<` and `<=` convert to `notNull AND lt` / `notNull AND ltEq` (Java Iceberg's `lt` never matches a NULL; the fork's evaluator orders NULL first), `>` and `>=` stay bare. `NOT` pushes down as Spark's optimizer pushes it: a comparison flips (`NOT cat < 'y'` → `cat >= 'y'`), `AND`/`OR` swap, `IS NULL` ↔ `IS NOT NULL`, `NOT BETWEEN` → `< low OR > high`. An `IN` list is deduplicated with its NULL counted, and a one-element list folds to `=` / `<>` (Spark's OptimizeIn), so `NOT IN ('y')` and `NOT IN ('y', 'y')` delete the NULL row. Longer `NOT IN` lists keep `notNull AND notIn` over the non-NULL values and keep the NULL row. Measured over 49 predicates on a NULL `cat` key and 7 on a NULL `id` key. | Rows per predicate on the facade and the Rust door, generated from Spark's rows; kernel renderings. | PROVEN | Round 3 (2026-09-25): replayed from the committed oracle — Rust `tests::replace_where_oracle::every_replayed_spark_measurement_answers_as_spark_did` and facade `test_the_replace_where_measurements_replay_as_spark_answered[r1/S1-…, r1/S3-…, r2/C-…, r2/I-…]` read `python/repark/tests/u8_write_sql_spark_oracle.json` (sha256 237118c9…, generator `target/probe-u8-r3fix/build_oracle.py`) and compare rows and outcomes case by case (they replace the transcribed `NULL_CAT_CASES` / `NULL_ID_CASES` tables and the hand-written facade lists); iceberg `spark_translatable_predicates_convert_to_iceberg_filters`, `not_pushes_down_through_comparisons_and_connectives_as_spark_does`, `in_lists_follow_spark_null_semantics`. |
| C-016 | *Added round 2 (critic r1 V-003).* A `REPLACE WHERE` source whose output names repeat writes as Spark does: the width check plans the deduplicated source, so `SELECT id, CAST(id AS STRING), 'x' FROM t WHERE cat = 'x'` leaves `[[1,'1',x],[2,b,y],[3,'3',x]]`, `SELECT 9 AS a, 'z' AS a, 'x'` leaves `[[2,b,y],[9,z,x]]`, and its `UNION ALL` form adds `[10,w,x]`. | Rows on the facade and the Rust door. | PROVEN | Rust `tests::replace_where_sources::a_replace_where_source_with_repeated_names_writes_as_spark_does`; the oracle replays of `r1/S5-…` and `r2/A-dup-…` on both doors. |
| C-017 | *Added round 2 (critic r1 V-005 part 2).* An integer literal outside an `INT` column's range (a `BIGINT`-typed literal; a literal beyond i64 is C-019) folds as Spark's unwrap-cast folds it: a comparison that no value satisfies refuses `Cannot convert Spark predicate to Iceberg expression: null` (`=`, `>`, `>=` above the range; `<`, `<=` below; `IN` of only such literals), one that every non-NULL value satisfies refuses `…: (i IS NOT NULL) OR (null)` (`<`, `<>` above), and an `IN` list drops the literal (`i IN (1, 3000000000)` replaces `i = 1`). | Exact text and rows. | PROVEN | iceberg `an_out_of_range_integer_literal_folds_as_spark_unwraps_the_cast`, `in_lists_follow_spark_null_semantics`; the oracle replays of `r1/S4-…`, `r2/O-int-…`, `r2/O-small-…`, `r2/O-tiny-…` on both doors. |
| C-018 | *Added round 2; wording round 4 (critic r3 V-008).* A `REPLACE WHERE` into a table whose namespace does not exist answers `TABLE_OR_VIEW_NOT_FOUND` / `42P01` (it answered the D7 load text before). The pin holds **RePark's own rendering**: one line, sentences joined by spaces, behind DataFusion's `Error during planning: ` prefix. Spark's text (oracle `r2/N-nons`) joins the same sentences with newlines, so the condition, SQLSTATE and sentences match and the whitespace and prefix do not (R-2). | Rust door pin of the full RePark message (round 3, critic r2 V-005c; it matched a prefix before). | PROVEN | Rust `tests::replace_where_sources::a_replace_where_into_a_missing_namespace_answers_table_or_view_not_found`. |
| C-019 | *Added round 3 (2026-09-25, critic r2 V-001, V-002).* An unreachable literal folds to a constant where Spark folds it, and the statement commits: `i <=> 3000000000` (INT, SMALLINT, TINYINT) and `i <=> 99999999999999999999` (BIGINT) are FALSE (the rows stay and the query appends), `NOT (i <=> …)` is TRUE (every row, the NULL key included, is replaced), and `… OR i = 1` / `… AND data = 'a'` simplify through the connectives. A literal beyond i64 (Spark types it DECIMAL(20,0)) folds a range comparison on an INT or BIGINT column to TRUE or FALSE — `i < huge` replaces every row including a NULL key, `i > huge` appends — while `=`, `<>`, `IN` and `NOT IN` keep the C-017 folds. A fractional literal on an integer column selects the rows Spark's unwrap-cast selects: `id < 2.5` and `id <= 2.5` convert to `notNull AND id <= 2`, `id > 2.5` and `id >= 2.5` to `id >= 3` (keeping a NULL key in its own manifest; round 4 narrowing, the same-INSERT case is R-12), `=` / `<>` / `IN` against it fold, and `<=>` is FALSE. *Round 5 (2026-09-25, verifier V-001):* inside a refusal text the comparison renders as Spark's `UnwrapCastInBinaryComparison` renders it — `<` as `< ceil`, `<=` as `<= floor`, `>` as `> floor`, `>=` as `>= ceil`, so `i < 2.5 OR UPPER(data) = 'A'` refuses `Cannot convert Spark predicate to Iceberg expression: (i < 3) OR (UPPER(data) = 'A')` (`<= 2.5` → `(i <= 2)`, `> 2.5` → `(i > 2)`, `>= 2.5` → `(i >= 3)`, `< -0.5` → `(i < 0)`, `<= -0.5` → `(i <= -1)`, `> -0.5` → `(i > -1)`, `>= -0.5` → `(i >= 0)`, `NOT (i < 2.5)` → `(i >= 3)`, on INT and BIGINT) and `i NOT BETWEEN 0.5 AND 2147483647.5` refuses `…: (i < 1) OR ((i IS NULL) AND (null))`; nothing commits. Rounds 3–4 rendered every `<` / `<=` as `<= floor` and every `>` / `>=` as `>= ceil` (same rows, wrong text). | Rows on both doors; kernel renderings; exact refusal text on both doors. | PROVEN | Oracle replays of `r2/O-…`, `r2b/O-…`, `r2fix/L-…`, `r2fix/IN-…`, `r2fix/SM-…`, `r2fix/QI-…`, `r2fix/Q-id-…` and, for the refusal text, `r4/…` (12, `target/probe-u8-r3fix/probe_r4.py`; red on the round-4 kernel: `target/probe-u8-r3fix/red-first-v001-r4.txt`) (Rust `every_replayed_spark_measurement_answers_as_spark_did`, facade `test_the_replace_where_measurements_replay_as_spark_answered`); iceberg `unreachable_literals_fold_to_constants_where_spark_folds_them`, `a_fractional_literal_on_an_integer_column_rounds_as_spark_does` (the filter, unchanged by round 5), `a_refusal_renders_the_first_unconvertible_conjunct_as_spark_does` (the ten `r4` renderings). Mutation M37. |
| C-020 | *Added round 3 (critic r2 V-003).* A refusal renders what Spark renders: Spark splits the optimized predicate into conjuncts and reports the first it cannot convert. An unreachable `=` is `(i IS NULL) AND (null)`, so it splits to `null` (`i = 3000000000 AND data = 'a'`, `data = 'a' AND i = 3000000000`, `cat = NULL AND id = 1`, `i IN (3000000000)`, `i IN (3000000000, NULL)`, `cat IN (NULL)`, `cat NOT IN (NULL)`); an unreachable `<` / `<>` / `NOT IN` is one conjunct `(i IS NOT NULL) OR (null)`; an `OR` keeps its shape (`(null) OR (id = 1)`, `((i IS NULL) AND (null)) OR (data = 'a')`, `(NOT (upper(cat) = 'X')) OR (NOT (cat = 'x'))`); and `cat = 'x' AND upper(cat) = 'X'` reports `upper(cat) = 'X'` only. Re-pins `cat IN (NULL)` (round 2 pinned the statement text). | Exact text on both doors. | PROVEN | iceberg `a_refusal_renders_the_first_unconvertible_conjunct_as_spark_does`, `untranslatable_predicates_refuse_with_the_spark_message`; the oracle replays of `r2fix/IO-…`, `r2fix/CN-…`, `r2/C-…-null…` compare the full message. |
| C-021 | *Added round 3 (critic r2 V-004).* The arity refusal of a repeated-name source names the columns as written while the width check plans the deduplicated source: `SELECT 9 AS a, 'z' AS a` answers `Data columns: \`a\`, \`a\``, the long form `\`a\`, \`a\`, \`x\`, \`a\``, a cast `\`id\`, \`id\``, and the PARTITION door `\`a\`, \`a\`, \`cat\`, \`a\`` (static) / `\`a\`, \`a\`` (dynamic). No `__repark_col_<n>` alias reaches the text. | Exact text on both doors. | PROVEN | Oracle replays of `r2/A-dup-short`, `r2fix/A-dup-…`, `r2fix/P-dup-…` (full-message equality). |

| C-022 | *Added round 4 (2026-09-25, critic r3 V-001, V-002).* A decimal literal on an `INT` / `SMALLINT` / `TINYINT` column unwraps as Spark's `UnwrapCastInBinaryComparison` does. Outside the INT range but inside i64 (after truncation) it folds like an out-of-range integer (C-017), never to a constant: `i < 2147483647.5`, `i <= …`, `NOT (i >= …)`, `i <> …`, `i NOT IN (…)` and `i BETWEEN 1 AND 2147483647.5` refuse `…: (i IS NOT NULL) OR (null)`; `i > …`, `i >= …`, `i = …` refuse `…: null`. A `.0` literal exactly at the INT maximum follows Spark's boundary rules: `i > 2147483647.0` refuses `…: null`, `i <= 2147483647.0` refuses `…: (i IS NOT NULL) OR (null)`, `i < 2147483647.0` converts to `NOT (i = 2147483647)`, `i >= 2147483647.0` to `i = 2147483647`, and `=` / `<=>` / `IN` compare plainly; the minimum mirrors it. A BIGINT column keeps C-019's constant folds and compares a `.0` literal at the i64 boundary plainly. | Kernel renderings; rows and exact text on both doors. | PROVEN | iceberg `a_decimal_literal_on_an_int_column_follows_spark_unwrap_rules` (red on the round-3 kernel: `target/probe-u8-r3fix/red-first-v001.txt`); the oracle replays of `r3c/…` (32), `r3d/…` (11), `r3e/…` (9) on both doors. Mutations M32, M33. |
| C-023 | *Added round 4 (critic r3 V-003).* An arity refusal names every data column as Spark names it: a cast of a column is the column (`\`id\`, \`id\`` for `SELECT id, CAST(id AS STRING)`, also qualified, through a table alias, and beside a `*` that expands to `id`), a cast of anything else is `CAST(<name> AS <TYPE>)`, arithmetic is parenthesized (`(id + 1)`, `(id * 2)`), a literal is its value (`1`, `x`), `substr(data, 1, 1)` keeps Spark's lower-case spelling, `coalesce(id, 0)` / `abs(id)` / `upper(data)` read as written, and an alias wins. A `*` beside a column-named item no longer leaks DataFusion's `Projections require unique expression names`. No `__repark_col_<n>` reaches the text on the REPLACE WHERE or PARTITION door. | Exact text on both doors. | PROVEN | Rust `tests::replace_where_sources::an_arity_refusal_names_every_data_column_as_spark_does` (red on the round-3 router: `target/probe-u8-r3fix/red-first-v003-v005.txt`); the oracle replays of `r2fix/A-…` (6) and `r3fix/N-…` (15, `target/probe-u8-r3fix/probe_r3fix.py`) with full-message equality. Mutations M34, M35. |
| C-024 | *Added round 4 (critic r3 V-005).* A suffix-typed literal in a refused predicate renders as its cast: `id = 1BD` refuses `Cannot convert Spark predicate to Iceberg expression: id = CAST(1 AS DECIMAL(1,0))`, with no `__repark_suffix_literal__` marker. (Spark commits such predicates on an INT column; that is R-11.) | Exact text on the Rust door; the recorded R-11 text on both doors. | PROVEN | Rust `tests::replace_where_sources::a_suffix_typed_literal_renders_as_its_cast_in_the_refusal` (red on the round-3 router: `target/probe-u8-r3fix/red-first-v003-v005.txt`); the oracle replay of `r3/I-typed-BD` against its residue record. Mutation M36. |

VERDICT (2026-09-25, critic r3 disposition): 24 clauses, 24 PROVEN, 0 OPEN, 0 REJECTED.

**Replay contract (round 4, critic r3 V-007; round 5 adds `r4/`).** Both doors replay `python/repark/tests/u8_write_sql_spark_oracle.json` case by case: the table, seed and statements from the case, then the last statement's outcome and the table's rows.

| Door | Test | Prefixes | Cases |
|---|---|---|---|
| Rust | `tests::replace_where_oracle::every_replayed_spark_measurement_answers_as_spark_did` | `r1/S1-`..`r1/S5-`, `r2/C-`, `r2/I-`, `r2/O-`, `r2/A-`, `r2b/O-`, `r2fix/L-`, `IN-`, `SM-`, `IO-`, `QI-`, `Q-`, `CN-`, `P-`, `A-`, `r3/`, `r3b/`, `r3c/`, `r3d/`, `r3e/`, `r3fix/`, `r4/` (26) | 450 (asserted exactly) |
| Facade | `test_the_replace_where_measurements_replay_as_spark_answered[<key>]` | the Rust prefixes plus `r2fix/W-` (the WAP branch session setting is a facade surface) | 451 |

Every refusal asserts the exception class and the full normalized message (condition and SQLSTATE included), except Spark's `Py4JJavaError` cases (R-1, 12 keys), which assert RePark's base class and Iceberg's `Cannot delete file where some, but not all, rows match filter` text. A case whose RePark answer is a named residue carries `residue = {id, repark: {step, rows}}` (45 keys: R-2 14, R-8 2, R-11 10, R-12 19), recorded through the facade by `target/probe-u8-r3fix/record_repark.py`. The replays hold RePark to that recorded answer, and an R-2 case also to Spark's rows, so a residue that moves reds. No key is skipped. Snapshot summaries are **not** replayed: the operation and counts are pinned only by the hand pins (C-001, C-003, C-006, C-011).

## Claims line for U7 (overwrite by condition)

`repark_iceberg::write::{spark_overwrite_filter, commit_overwrite_by_filter_with_summary}`
(`crates/repark-iceberg/src/write/overwrite_filter.rs`) are Spark's `OverwriteByFilter`:
the DataFrame `writeTo(t).overwrite(condition)` can render its condition to Spark SQL, parse it
with the Databricks dialect, convert it with `spark_overwrite_filter` and commit its staged files
with `commit_overwrite_by_filter_with_summary`. The Spark door's executor,
`router::insert_positional::replace_where::execute_replace_where`, shows the staging and the `false` → append arm.

## Mutation record (2026-09-24)

Each line was broken, the named tests ran, and the file was restored
(`target/probe-u8-pr1/mut/M*.txt`; round 2 in `target/probe-u8-r1fix/mut/`, round 3 in `target/probe-u8-r2fix/mut/`, round 4 in `target/probe-u8-r3fix/mut/`).

| # | Mutation | Red |
|---|---|---|
| M1 | drop `.allow_empty_commit()` from the overwrite-by-filter commit | none: the fork counts a set row filter as a change, so the call was dead and is removed; `an_empty_source_commits_a_delete_even_when_nothing_matches` and `an_empty_source_on_a_table_without_snapshots_commits_a_delete` pin the behaviour |
| M2 | validate added files against the filter | `added_rows_outside_the_filter_are_not_validated`, `a_filter_that_splits_a_file_refuses_before_any_commit` |
| M3 | `NOT IN` without the `notNull` conjunct | `in_lists_follow_spark_null_semantics` |
| M4 | a comparison with `NULL` is not refused | `untranslatable_predicates_refuse_with_the_spark_message` |
| M5 | the router never sees `REPLACE WHERE` | 9 of 10 `tests::replace_where` pins |
| M6 | a `false` predicate overwrites instead of appending | `replace_where_snapshot_operations_follow_what_the_commit_changes` |
| M7 | the positional-source rename never runs | `a_cast_that_keeps_its_input_name_no_longer_collides_in_a_positional_source`, `a_bucketed_insert_writes_one_file_per_bucket` |
| M8 | every static value goes to the last column | `static_values_follow_spark_string_casts`, `two_key_specs_take_static_and_dynamic_values_in_any_order` |
| M9 | the static-value cast check never refuses | `a_bad_static_value_refuses_as_an_illegal_argument`, `partition_clause_refusals_carry_spark_text_and_commit_nothing`, `static_value_is_cast_to_a_date_partition_and_an_invalid_value_refuses` |
| M10 | `REPLACE WHERE` is not an owned write head | `replace_where_writes_to_a_named_branch_only` |
| M11 | the router ignores `owned_append` | `a_partition_insert_writes_to_a_named_branch_only` |
| M12 | `INSERT INTO … PARTITION` is not an owned write head | `a_partition_insert_writes_to_a_named_branch_only` |
| M13 | U6's by-name routing excludes every `PARTITION` insert again | `a_partition_insert_on_an_accept_any_table_resolves_by_name` |
| M14 | a repeated clause key is not refused | `partition_clause_refusals_carry_spark_text_and_commit_nothing` |
| M15 | the static-clause arity check never refuses | `partition_clause_refusals_carry_spark_text_and_commit_nothing` |
| M16 | the dynamic-only clause skips the arity check | `a_dynamic_only_clause_keeps_the_spark_arity_refusal` |
| M17 | a subquery predicate is not refused | `replace_where_refusals_carry_spark_text_and_commit_nothing` |
| M18 | a non-deterministic predicate is not refused | `replace_where_refusals_carry_spark_text_and_commit_nothing` |
| M19 | the `REPLACE WHERE` arity check always passes | `replace_where_refusals_carry_spark_text_and_commit_nothing` |
| M20 | the facade expands the whole `REPLACE WHERE` body | `test_replace_where_accepts_the_spellings_spark_accepts[… WITH s AS …]` |
| M21 | *round 2 (2026-09-25):* `<` converts to a bare `lt` again | Rust `a_null_partition_key_is_kept_or_replaced_as_spark_does`, `a_null_identity_key_on_a_long_partition_follows_spark` (both replaced in round 3 by the oracle replay) |
| M22 | a one-element `IN` list does not fold | iceberg `in_lists_follow_spark_null_semantics`, `not_pushes_down_through_comparisons_and_connectives_as_spark_does` |
| M23 | `NOT <` does not flip to `>=` | iceberg `not_pushes_down_through_comparisons_and_connectives_as_spark_does` |
| M24 | the out-of-range fold text is never used | iceberg `an_out_of_range_integer_literal_folds_as_spark_unwraps_the_cast`, `untranslatable_predicates_refuse_with_the_spark_message` |
| M25 | the width check plans the raw `REPLACE WHERE` source (V-003) | Rust `a_replace_where_source_with_repeated_names_writes_as_spark_does` |
| M26 | a missing namespace is not a missing table | Rust `a_replace_where_into_a_missing_namespace_answers_table_or_view_not_found` |
| M27 | *round 3 (2026-09-25):* a range comparison against a literal beyond i64 folds to the INT texts again | iceberg `unreachable_literals_fold_to_constants_where_spark_folds_them` |
| M28 | `(i IS NULL) AND (null)` is not split into conjuncts | iceberg `a_refusal_renders_the_first_unconvertible_conjunct_as_spark_does`, `an_out_of_range_integer_literal_folds_as_spark_unwraps_the_cast`, `untranslatable_predicates_refuse_with_the_spark_message` |
| M29 | the arity refusal names the deduplicated source | Rust `every_replayed_spark_measurement_answers_as_spark_did` (`r2/A-dup-short`) |
| M30 | `<=>` against an unreachable literal folds to `null` again | Rust `every_replayed_spark_measurement_answers_as_spark_did` |
| M31 | a fractional literal rounds `>` down instead of up | iceberg `a_fractional_literal_on_an_integer_column_rounds_as_spark_does` |
| M32 | *round 4 (2026-09-25):* a `.0` literal at the INT boundary compares plainly | iceberg `a_decimal_literal_on_an_int_column_follows_spark_unwrap_rules` |
| M33 | a decimal literal outside the INT range folds to a constant again | iceberg `a_decimal_literal_on_an_int_column_follows_spark_unwrap_rules`, `a_refusal_renders_the_first_unconvertible_conjunct_as_spark_does`, `an_out_of_range_integer_literal_folds_as_spark_unwraps_the_cast` |
| M34 | a column-named item beside `*` is not aliased | Rust `an_arity_refusal_names_every_data_column_as_spark_does` |
| M35 | `substr(…)` is named by its SQL display | Rust `an_arity_refusal_names_every_data_column_as_spark_does` |
| M36 | the refusal renders the predicate with its suffix-literal marker | Rust `a_suffix_typed_literal_renders_as_its_cast_in_the_refusal` |
| M37 | *round 5 (2026-09-25):* a fractional `<` renders `<= floor` again (the round-4 text) | iceberg `a_refusal_renders_the_first_unconvertible_conjunct_as_spark_does`; Rust `every_replayed_spark_measurement_answers_as_spark_did` (`r4/…`) — `target/probe-u8-r3fix/red-first-v001-r4.txt` |

## Coverage

```yaml
COVERAGE_ATTESTATION:
  pr_unit: u8-write-sql
  categories:
    - id: AT-1
      status: ATTACKED
      evidence: Every clause comes from the 128-probe Spark measurement or the scoreboard record. The three cells replay EQUAL on the whole observation. The probe matrix answers 115 of 128 probes as Spark does (rows, error condition, snapshot summaries); the other 13 are R-1 (11 class-only), R-2 (RW2-ts) and R-4 (RW-table). Before this unit 11 of the first 122 probes matched (`repark.json`, `repark2.json`, pre-change wheel).
      artifacts: [python/repark/tests/test_ice_write_sql_1.py, crates/repark-spark/src/tests/replace_where.rs, crates/repark-spark/src/tests/partition_append.rs]
    - id: AT-2
      status: ATTACKED
      evidence: Predicates cover every operator Spark converts plus the untranslatable, null, subquery, non-deterministic and mis-cased forms; sources cover SELECT, VALUES, CTE, parenthesized and self-reading queries; targets cover partitioned, unpartitioned, bucketed, unseeded, branch and accept-any tables. Misplaced REPLACE WHERE and a WHERE inside the query are pinned.
      artifacts: [crates/repark-iceberg/src/write/overwrite_filter/tests.rs, crates/repark-spark/src/tests/replace_where.rs]
    - id: AT-3
      status: ATTACKED
      evidence: Every refusal asserts the snapshot count is unchanged. The partial-file refusal comes from the fork before any commit. The static-value cast is checked before any source runs.
      artifacts: [crates/repark-spark/src/tests/partition_append.rs, python/repark/tests/test_ice_write_sql_1.py]
    - id: AT-4
      status: ATTACKED
      evidence: The overwrite commit takes the table's isolation level and validate_from_snapshot like its sibling commits; REPLACE WHERE and PARTITION inserts are owned write heads, so the branch sniff rewrites them to the branch selector instead of a temp view.
      artifacts: [crates/repark-iceberg/src/write/overwrite_filter.rs, crates/repark-spark/src/write_to_branch.rs]
    - id: AT-5
      status: N/A
      justification: No privileged action, credential, deserialization or path handling. Rewritten SQL re-parses through the door's own parser; static values render through sqlparser's literal display.
    - id: AT-6
      status: ATTACKED
      evidence: Rows, snapshot summaries and file layouts are pinned; the INSERT-adjacent Rust modules (976 tests) and facade suites stay green apart from the re-pinned C-013 text of ice-overwrite-mode-1.
      artifacts: [crates/repark-spark/src/tests/overwrite_mode.rs, python/repark/tests/test_ice_overwrite_mode_1.py]
    - id: AT-7
      status: ATTACKED
      evidence: The two new recognizers return on a substring miss before tokenizing, so statements without `replace` or `partition` pay one lower-case scan. A PARTITION append adds one table load and one source plan.
      artifacts: [crates/repark-spark/src/router/insert_positional/replace_where.rs, crates/repark-spark/src/router/insert_positional/partition_append.rs]
    - id: AT-8
      status: ATTACKED
      evidence: The converter and the commit are one kernel in repark-iceberg; the door reuses the owned append, the stage-then-commit helpers and U6's naming. No new dependency or crate edge, no code comments, router.rs at 996 lines, session_core.py on its exact baseline.
      artifacts: [crates/repark-iceberg/src/write/overwrite_filter.rs, crates/repark-spark/src/router.rs]
    - id: AT-9
      status: ATTACKED
      evidence: The registry gains DML-7 and DML-8, DML-1's cast sentence and ID-3's positional exemption; the superseded ice-overwrite-mode-1/C-013 text is marked in its ledger.
      artifacts: [docs/spark-sql-iceberg-parity.md, task/ledgers/staging/ice-overwrite-mode-1-ledger.md]
    - id: AT-10
      status: ATTACKED
      evidence: Each load-bearing line was broken and its named test ran red (table above).
      artifacts: [crates/repark-spark/src/tests/replace_where.rs, crates/repark-iceberg/src/write/overwrite_filter/tests.rs]
  complete: true
```

## Residues

| # | Residue |
|---|---|
| R-1 | Iceberg validation refusals keep RePark's class: the partial-file refusal is `PySparkException` `DataInvalid => Cannot delete file where some, but not all, rows match filter id = 2: …`, where Spark raises `Py4JJavaError` wrapping `ValidationException` and spells the filter `ref(name="id") == 2`. A mis-cased predicate column refuses Spark's `Cannot find field 'CAT' in struct: …` text as `AnalysisException`. The same class residue as u6-write-refusals R-7. Round 2 (2026-09-25): a `<` / `<=` filter now renders its `notNull` conjunct (`(data IS NOT NULL) AND (data < "b")`, Spark `ref(name="data") < "b"`); the rows match (sweep S2-*, eight probes). |
| R-2 | The untranslatable-predicate refusal quotes the statement's spelling: `(id + 1) = 3` reads `id + 1 = 3`, `CAST(id AS string)` reads `CAST(id AS STRING)`, `UPPER(cat)` reads as written, and Spark's `LIKE '%x' ESCAPE '\'` reads `LIKE '%x'` (the six oracle keys `r2fix/Q-…upper…`, `r2fix/Q-and-or-arith`, `r2fix/Q-like-suffix-and` carry RePark's text as their R-2 residue record). A `TIMESTAMP` literal in the predicate refuses as untranslatable (probe RW2-ts); Spark converts it in the session zone. A `DATE` literal or date string converts. *Added round 4 (2026-09-25, critic r3 V-004, V-008, V-009; probes `xr-probe/r3/probe_r3.py`, oracle keys `r3/…`), each held by its residue record.* Both refuse, the text differs: `d = 1000000` on DECIMAL(5,2) (`r3/D-eq-oor`) — Spark `…: CAST(d AS decimal(9,2)) = 1000000.00`, RePark `…: d = 1000000`; `d < 99999999999999999999` (`r3/D-lt-huge`) — Spark `…: CAST(d AS decimal(22,2)) < 99999999999999999999.00`, RePark `…: d < 99999999999999999999`; `d = 1.505` (`r3/D-eq-3dp`) — Spark `…: null`, RePark `…: d = 1.505`; `cat = 1`, `cat < 2`, `cat IN (1, 2)` on STRING (`r3/S-eq-int`, `S-lt-int`, `S-in-int`) — Spark `…: CAST(cat AS long) = 1` (`< 2`, `IN (1, 2)`), RePark `…: cat = 1` (`cat < 2`, `cat IN (1, 2)`); `d = 'abc'` on DATE (`r3/DT-eq-bad`) — Spark `DateTimeException` `[CAST_INVALID_INPUT] The value 'abc' of the type "STRING" cannot be cast to "DATE" because it is malformed. … SQLSTATE: 22018`, RePark `IllegalArgumentException` `…: d = 'abc'`; `d = 1` on DATE (`r3/DT-eq-int`) — Spark `[DATATYPE_MISMATCH.BINARY_OP_DIFF_TYPES] Cannot resolve "(d = 1)" … ("DATE" and "INT"). SQLSTATE: 42K09`, RePark `AnalysisException` without a condition, `spark_expr_semantics` / `caused by` / `Error during planning: [DATATYPE_MISMATCH.CAST_WITH_FUNC_SUGGESTION] Cannot resolve "CAST(Int32(1) AS DATE)" …` (the analyzer's error chain, outside this unit). C-018's missing-namespace text is RePark's one-line rendering behind `Error during planning: `, Spark's (`r2/N-nons`) is newline-separated. |
| R-3 | `CAST_INVALID_INPUT` raises `IllegalArgumentException`; Spark raises its subclass `NumberFormatException`, which the facade does not define. |
| R-4 | `INSERT INTO t REPLACE WHERE … TABLE a.b.c` fails the door's pre-existing parse of a three-part `TABLE` query (plain `INSERT INTO t TABLE a.b.c` fails the same way). |
| R-5 | Pre-existing and unchanged: a plain positional `INSERT … SELECT` of the wrong width still answers `Column count doesn't match insert query!` (u6-write-refusals R-4). The Spark text is produced only on the REPLACE WHERE and PARTITION paths, which already load the table. |
| R-6 | `REPLACE WHERE` has no native-door spelling; the ANSI door answers its parse error (the DML-6 shape). A native steer is a follow-up. |
| R-7 | `REPLACE WHERE` under `spark.wap.id` is unmeasured. Narrowed 2026-09-25: under `spark.wap.branch` the write lands on the branch and `main` is unchanged, pinned by the facade replay of `r2fix/W-rw-wap` (main rows and `branch_wb` rows). |
| R-8 | Dated 2026-09-25 (critic r1 V-004, probe S1-notlike, XR-not-like). `REPLACE WHERE cat NOT LIKE 'y%'` on a table with a NULL `cat` key: Spark 4.1.2 fails `SparkException: [INTERNAL_ERROR] Eagerly executed overwrite failed. You hit a bug in Spark or the Spark plugins you use. … SQLSTATE: XX000` and commits nothing; RePark commits the overwrite as `not(startsWith)`, which also deletes the NULL-key row (rows `[[2,b,y],[9,z,x]]`). Spark's answer is a Spark bug, so it is not emulated. `NOT LIKE` without a wildcard folds to `<>` and matches Spark (S1-notlike-exact). |
| R-9 | Dated 2026-09-25 (critic r1 V-005 part 1, oracle keys `r1/S6-…`). A `REPLACE WHERE` target that is not an Iceberg table: Spark answers ``[_LEGACY_ERROR_TEMP_1011] Writing into a view is not allowed. View: `tv`.; line 1 pos 0`` for a temporary view and ``[_LEGACY_ERROR_TEMP_1012] Cannot write into v1 table: `spark_catalog`.`default`.`default_pq`.; line 1 pos 0`` for a `USING parquet` table. Through the facade a bare temp-view name is qualified to the current catalog first, so RePark answers `TABLE_OR_VIEW_NOT_FOUND` for ``hc.default.tv`` (a plain `INSERT INTO tv` answers `table 'hc.default.tv' not found`, the same pre-existing gap). A DataFusion session table reached on the Rust door answers RePark's own ``INSERT INTO … REPLACE WHERE requires an Iceberg table, got `<name>` ``. Round 3: that Rust-door text is pinned by `tests::replace_where_sources::a_replace_where_into_a_session_table_answers_the_iceberg_only_refusal`. |
| R-11 | Dated 2026-09-25 (critic r3 V-004; probe `xr-probe/r3/probe_r3.py`, oracle keys below, each held by its residue record). Spark commits and RePark refuses `IllegalArgumentException` `Cannot convert Spark predicate to Iceberg expression: …` for a suffix-typed or scientific literal on an INT column — `i < 1.5E0` (`r3/I-sci-lt`, RePark `i < CAST(1.5 AS DOUBLE)`), `i = 1E0` (`I-sci-eq`, `i = CAST(1 AS DOUBLE)`), `i = 1L` (`I-typed-L`, `i = CAST(1 AS BIGINT)`), `i = 1D` (`I-typed-D`, `i = CAST(1 AS DOUBLE)`), `i = 1BD` (`I-typed-BD`, `i = CAST(1 AS DECIMAL(1,0))`), `i = 1Y` (`I-typed-Y`, `i = CAST(1 AS TINYINT)`); for a DECIMAL(5,2) key — `d = 1.5` (`D-eq`), `d = 2` (`D-eq-int`), `d IN (1.5, 2)` (`D-in`); and for a DATE key against a timestamp-shaped string — `d = '2024-01-02 10:00:00'` (`DT-eq-ts-str`). Spark's rows are in each case's `rows`; RePark's table is unchanged. The kernel does not convert a typed or scientific literal, a literal against a DECIMAL key, or a timestamp-shaped string against a DATE key; converting them as Spark's unwrap-cast does is a follow-up. |
| R-12 | Dated 2026-09-25 (critic r3 V-006; probes `xr-probe/r3/probe_r3.py`, `probe_r3b.py`). A NULL partition key written **by the same INSERT** as the keys a `<` / `<=` filter matches: Spark replaces the NULL-key row too, RePark keeps it (its `notNull AND lt` is Spark's answer only when the NULL key sits in its own manifest, C-015). Measured on `i < 1.5`, `i <= 1.5`, `i NOT BETWEEN 0.5 AND 1.5` (INT, `r3/I-frac-…`), `i < 1.5` (BIGINT, `r3/L-frac-lt`), `d < '2024-01-03'` (DATE, `r3/DT-lt-str`) and seven `<` / `<=` shapes per type in the one-INSERT layout (`r3b/INT-one-…`, `r3b/BIGINT-one-…`, 14 keys); e.g. `r3/I-frac-lt`: Spark `[[2,b],[9,z]]`, RePark `[[2,b],[9,z],[null,n]]`. The `-split-` and `-three-` layouts (NULL key in its own INSERT) match Spark. Follow-up: emulate Java Iceberg's manifest-level delete in the fork's overwrite-by-filter, so a filter that deletes files in a manifest decides a NULL-key file of that same manifest as Java does. |

## PR2 — nested struct-field assignment in UPDATE and MERGE

**Date:** 2026-09-25 · **Branch:** `feat/u8-write-sql-2` · **Base:** `78d7d85d` (main after U8 PR1)
**Model:** Claude Opus 5.5 (`claude-opus-5-5`) · **Path:** STANDARD. **risk_tier: standard.**

**Why now.** Two scoreboard cells answered wrong on the Spark door. `W-UPDATE-NESTED-FIELD`
(`UPDATE t SET st.a = 99 WHERE id = 1`) refused `UNRESOLVED_COLUMN.WITH_SUGGESTION` for
`a`, and `W-MERGE-NESTED` (`… WHEN MATCHED THEN UPDATE SET t.st.a = s.na`) refused
`nested-field assignment is not supported`. Whole-struct MERGE assignments (`SET t.st =
named_struct(…)`, `UPDATE SET *`, a struct `INSERT` value) also refused, because the MERGE
store-assignment gate compared struct types with their Iceberg field ids.

**What Spark does (measured 2026-09-25, 93 probes, committed as
`python/repark/tests/u8_write_sql_nested_spark_oracle.json` keys `pr2/…`, generator
`target/probe-u8-pr2/build_oracle.py`, sources `target/probe-u8-pr2/spark.json`,
`spark2.json`, `spark3.json`).** Spark resolves a SET key by field extraction. It then aligns
the assignments per top-level column (`AssignmentUtils`) and rebuilds each touched struct as
`named_struct` over its fields, with the assigned leaves cast by ANSI store assignment. Before
this PR RePark matched 23 of the 54 first-round probes; after it, 47 (the other seven are R-13 and R-14).

| Clause | Statement | Proof obligation | Verdict | Evidence |
|---|---|---|---|---|
| C-025 | `UPDATE t SET st.a = 99 WHERE id = 1` leaves `[[1,{a:99,b:p}]]` with the schema unchanged and one `overwrite` (added 1, deleted 1, total 1, one data file in and out). Other UPDATE shapes also write Spark's rows: two fields in one SET, a field beside a top-level column, a deep field (`st.inner.x`), a sub-struct, a case-folded key (`ST.A`), an alias or full-name qualifier, a quoted field name, a value that reads the row (`st.a + 10`, `x.id * 3`), a `WHERE` on a field, a NULL struct (the assigned field is set, the others stay NULL), and a MoR table. | Rows, schema and summary on both doors. | PROVEN | Rust `tests::nested_assign::update_sets_one_struct_field_and_keeps_its_siblings`; facade `test_update_sets_one_struct_field_as_spark_does`; the oracle replays `pr2/U-cell`, `U-two`, `U-top-and-field`, `U-case`, `U-backtick`, `U-qualified`, `U2-full-name`, `U2-quoted-field-ok`, `U2-alias-value`, `U2-where-deep`, `U2-null-struct-where`, `U-mor`, `U2-mor-deep`. Cell `W-UPDATE-NESTED-FIELD` replays EQUAL. |
| C-026 | `MERGE INTO t t USING (SELECT 2 AS id, 20 AS na) s ON t.id = s.id WHEN MATCHED THEN UPDATE SET t.st.a = s.na` leaves `[[1,{a:1,b:p}],[2,{a:20,b:q}]]` with the schema unchanged and one `overwrite` (added 2, deleted 2, total 2). The same holds for an unqualified key (`st.a`), an unaliased target, two fields, a deep field, a value reading the target (`t.st.a + s.na`), a BIGINT source, conditional MATCHED clauses, `WHEN NOT MATCHED BY SOURCE … UPDATE SET t.st.a = -1`, and a MoR table. | Rows, schema and summary on both doors. | PROVEN | Rust `tests::nested_assign::merge_sets_one_struct_field_from_the_source`; facade `test_merge_sets_one_struct_field_as_spark_does`; the oracle replays `pr2/M-cell`, `M-unqualified`, `M2-unaliased`, `M-two`, `M-deep`, `M-self-ref`, `M-bigint-src`, `M2-cond-two-matched`, `M-nmbs`, `M-mor`, `M2-mor-deep`. Cell `W-MERGE-NESTED` replays EQUAL. |
| C-027 | An assigned leaf is cast as Spark's store assignment casts it: `1.5` and `CAST(1.5 AS DOUBLE)` into INT write 1, BIGINT into INT writes the value, 7 into STRING writes `'7'`, a DATE into STRING writes `'2024-01-02'`, NULL writes NULL. STRING or BOOLEAN into INT refuses `[INCOMPATIBLE_DATA_FOR_TABLE.CANNOT_SAFELY_CAST] Cannot write incompatible data for the table ``: Cannot safely cast `st`.`a` "STRING" to "INT". SQLSTATE: KD000` on UPDATE and MERGE. A struct-valued leaf resolves by name: reordered and re-cased fields write, a NULL value writes NULL, a missing field refuses `CANNOT_FIND_DATA` (`… Cannot find data for the output column `st`.`inner`.`y`.``), an extra field refuses `EXTRA_STRUCT_FIELDS` (`… Cannot write extra fields `z` to the struct `st`.`inner`.``), a bad leaf refuses `CANNOT_SAFELY_CAST` for `st`.`inner`.`x`. | Exact class, condition, SQLSTATE, text and rows. | PROVEN | Rust `merge::nested_assign::tests::a_struct_value_resolves_by_name_as_spark_does`; the oracle replays `pr2/U-str-int`, `U-str-bad`, `U-dec`, `U-double`, `U-bigint`, `U-int-to-str`, `U-null`, `U2-bool-into-int`, `U2-date-into-str`, `M-str-int`, `M-dec`, `U3-reorder`, `U3-extra`, `U3-missing`, `U3-bad-leaf`, `U3-case-names`, `U3-null-leaf-struct`, `M3-deep-reorder`, `M3-deep-missing`, `U2-wrong-names`. Mutations M42, M43. |
| C-028 | A SET key resolves as Spark's field extraction does, and a failure refuses with Spark's text: `st.zz` → `[FIELD_NOT_FOUND] No such struct field `zz` in `a`, `b`. SQLSTATE: 42704`; `id.a` → `[INVALID_EXTRACT_BASE_FIELD_TYPE] Can't extract a value from "id". Need a complex type [STRUCT, ARRAY, MAP] but got "BIGINT". SQLSTATE: 42000`; `arr.element.x` on an array of structs → `FIELD_NOT_FOUND` for `element` in `x`; `ai.b` on an array of INT → `[DATATYPE_MISMATCH.UNEXPECTED_INPUT_TYPE] Cannot resolve "ai[b]" … "INTEGRAL" … SQLSTATE: 42K09`. A map value (`m.value.x`, `mi.k.x`), a map key (`m.key`) or an array field (`arr.x`) refuses `[DATATYPE_MISMATCH.INVALID_ROW_LEVEL_OPERATION_ASSIGNMENTS] Cannot resolve "m[value].x = 1" due to data type mismatch: \n- Updating nested fields is only supported for StructType but 'm' is of type MapType(StringType,StructType(StructField(x,IntegerType,true)),true) SQLSTATE: 42K09`. The same texts hold on UPDATE and MERGE. | Exact texts, table unchanged. | PROVEN | Rust `merge::nested_assign::tests::key_resolution_refuses_as_spark_does`, `scala_type_names_follow_spark_to_string`; the oracle replays `pr2/U-missing`, `U-not-struct`, `U-arr-element`, `U3-arr-idx`, `U2-arr-int`, `U-map-value`, `U-map-key`, `U2-map-int-key`, `U2-arr-field`, `M-missing`, `M-map-value`, `M-arr-element`. |
| C-029 | Repeated and conflicting keys refuse with one `INVALID_ROW_LEVEL_OPERATION_ASSIGNMENTS` text that lists every assignment in Spark's pretty form (`"st = named_struct(a, 5, b, z)", "st.a = 1"`). The details are `Multiple assignments for 'st.a': 1, 2` (values in Spark's SQL form, `'x', s.na`), `Conflicting assignments for 'st': <T>.st = named_struct('a', 5, 'b', 'z'), <T>.st.`a` = 1` (the parent first; `t.st…` on MERGE), `'st.inner'` for a deep conflict and `'st.`b c`'` for a quoted field. A nested MERGE `INSERT` key refuses `… Cannot resolve "id = id", "st.a = na" … \n- INSERT assignment keys cannot be nested fields: t.st.`a` = s.na` (two keys joined by `, `); a missing one refuses `FIELD_NOT_FOUND`. | Exact texts, table unchanged. | PROVEN | Rust `merge::nested_assign::tests::conflicting_and_repeated_assignments_refuse_with_every_error`, `pretty_values_drop_qualifiers_and_string_quotes`; the oracle replays `pr2/U-dup`, `U-whole-and-field`, `U-field-and-whole`, `U-deep-conflict`, `U2-dup-strings`, `U2-quoted-field`, `M-whole-and-field`, `M-dup`, `M2-dup-strings`, `M-insert-nested-col`, `M2-insert-two-nested`, `M2-insert-missing`. Mutations M41, M45. |
| C-030 | The MERGE store-assignment gate judges a struct pair field by field, ignoring Iceberg field ids (`store_assign::ansi_store_assignable`), and the SET cast names the struct type without them (`update_cast::store_assignment_cast_sql`). Whole-struct `SET t.st = named_struct('a', s.na, 'b', 'z')`, `UPDATE SET *` from a struct source, and `INSERT (id, st) VALUES (s.id, named_struct(…))` write Spark's rows (before this PR each refused `not ANSI-store-assignable`). A STRING leaf, a renamed field or a different width still refuses. | Unit pins of the matrix and the cast; rows on both doors. | PROVEN | iceberg `write::store_assign::tests::structs_are_judged_field_by_field_without_field_ids`, `field_ids_are_stripped_at_every_depth`, `write::update_cast::tests::the_cast_names_the_target_type_without_field_ids`; Rust `tests::nested_assign::whole_struct_merge_assignments_pass_the_store_assignment_gate`; the oracle replays `pr2/M-whole`, `M-set-star`, `M-insert-struct-value`. Mutations M46, M47. |
| C-031 | Near misses keep their answers: a top-level `UPDATE … SET id = 7` and `MERGE … SET t.id = 7`, a whole-struct `UPDATE SET st = named_struct(…)` (also MoR), and a whole-struct reorder all write Spark's rows. A statement with only top-level targets is not re-rendered and loads no extra table. A nested key whose first part is no column is left to the existing path (`zz.a`: `UNRESOLVED_COLUMN`). The replay contract: the facade replays all 93 keys; the Rust door replays 67 keys counted exactly (every key without a residue record and without an ARRAY or MAP column); a residue key is held to RePark's recorded answer. | Rows on both doors; the fold's no-op path. | PROVEN | Rust `merge::nested_assign::tests::top_level_assignments_are_not_folded`, `an_unknown_column_is_left_to_the_existing_path`, `tests::nested_assign_oracle::every_nested_assignment_measurement_answers_as_spark_did`; facade `test_the_nested_assignment_measurements_replay_as_spark_answered` (`pr2/U-top-unchanged`, `M-top-unchanged`, `U-whole`, `U2-mor-whole`, `U3-whole-reorder`, `U-missing-top`). Mutations M38, M39, M40, M44. |

VERDICT (2026-09-25, PR2): 7 clauses, 7 PROVEN, 0 OPEN, 0 REJECTED.

### PR2 mutation record (2026-09-25)

Each line was broken, the named tests ran red, and the file was restored. Red lines are in
`target/probe-u8-pr2/mut/M38.txt` .. `M47.txt`.

| # | Break | Red |
|---|---|---|
| M38 | the fold never marks a column as nested | Rust `update_sets_one_struct_field_and_keeps_its_siblings`, `merge_sets_one_struct_field_from_the_source`, `nested_assignments_fold_into_one_struct_rebuild`, `conflicting_and_repeated_assignments_refuse_with_every_error`, `every_nested_assignment_measurement_answers_as_spark_did` |
| M39 | the UPDATE route executes the unfolded statement | Rust `update_sets_one_struct_field_and_keeps_its_siblings` |
| M40 | the MERGE route lowers the unfolded clauses | Rust `merge_sets_one_struct_field_from_the_source` |
| M41 | repeated assignments to one field are not refused | Rust `conflicting_and_repeated_assignments_refuse_with_every_error`, `every_nested_assignment_measurement_answers_as_spark_did` |
| M42 | a nested leaf skips the store-assignment check | Rust `every_nested_assignment_measurement_answers_as_spark_did` |
| M43 | a struct-valued leaf is cast by Arrow without the by-name checks | Rust `a_struct_value_resolves_by_name_as_spark_does`, `every_nested_assignment_measurement_answers_as_spark_did` |
| M44 | the key keeps its qualifier | Rust `nested_assignments_fold_into_one_struct_rebuild`, `merge_sets_one_struct_field_from_the_source`, `every_nested_assignment_measurement_answers_as_spark_did` |
| M45 | nested INSERT keys are not refused | Rust `every_nested_assignment_measurement_answers_as_spark_did` |
| M46 | the MERGE gate judges structs by identity again | iceberg `structs_are_judged_field_by_field_without_field_ids`; Rust `merge_sets_one_struct_field_from_the_source` |
| M47 | the cast keeps Iceberg field ids in the type name | iceberg `the_cast_names_the_target_type_without_field_ids`; Rust `merge_sets_one_struct_field_from_the_source` |

### PR2 residues

| # | Residue |
|---|---|
| R-13 | An `UPDATE` without `WHERE` on a table with a struct column fails in the fork's `IcebergUpdateExec` (`Arrow error: Invalid argument error: arguments need to have the same data type`). A top-level `UPDATE t SET id = 7` fails the same way. The fold is not the cause: the same statements with a `WHERE` answer Spark's rows. Keys `pr2/U-self-ref`, `U-ref-other`, `U-null-struct`, `U-deep`, `U-deep-mid`, `U-deep-and-sibling`. The fix belongs in the fork. |
| R-14 | A table with a `NOT NULL` struct field cannot be seeded: `INSERT … named_struct('a', 1, 'b', 'p')` fails `Unsupported CAST from Struct("a": Int32, "b": Utf8) to Struct("a": non-null Int32, "b": Utf8)`. So Spark's `NOT_NULL_ASSERT_VIOLATION` for `SET st.a = NULL` is unreachable (`pr2/U-notnull-field`). This predates the PR. |
| R-15 | An overflowing leaf (`st.a = 3000000000`) refuses with Arrow's cast text (`Can't cast value 3000000000 to type Int32`). Spark raises `CAST_OVERFLOW_IN_TABLE_INSERT` / `22003` naming `st`.`a`. Both refuse and leave the table unchanged (`pr2/U2-overflow`, `M2-overflow`). |
| R-16 | Top-level texts outside the fold keep their pre-existing rendering. A missing top-level column or a wrong qualifier answers `UNRESOLVED_COLUMN` for the last part, without Spark's table-qualified suggestions (`pr2/U-missing-top`, `U-table-qualified`). The top-level `CANNOT_SAFELY_CAST` names the table where Spark prints `` (`pr2/U2-order`, `U2-order2`). A NOT MATCHED clause after NOT MATCHED BY SOURCE answers RePark's order text where Spark answers `PARSE_SYNTAX_ERROR` (`pr2/M2-two-clauses`). Spark also appends `; line N pos M` to key refusals; the oracle normalizes that suffix away. |
| R-17 | Whole-struct assignments outside the fold. `UPDATE t SET st = named_struct('q', 1, 'b', 'z')` writes `{a: NULL, b: z}`, a silent wrong result, where Spark refuses `CANNOT_FIND_DATA` for `st`.`a` (`pr2/U3-whole-wrong`). This predates the PR: Arrow's by-name struct cast fills a missing field with NULL. A whole-struct MERGE assignment with reordered fields refuses with RePark's gate text where Spark writes (`pr2/M3-whole-reorder`). With a renamed field it refuses with that gate text where Spark answers `CANNOT_FIND_DATA` (`pr2/M3-whole-wrong`). Routing top-level struct values through the fold's by-name check is the follow-up. |

### PR2 coverage

The ledger's one `COVERAGE_ATTESTATION` block above is PR1's. PR2 files its categories here.

| Category | Status | Evidence / justification | Artifacts |
|---|---|---|---|
| AT-1 | ATTACKED | Every clause comes from 93 Spark probes committed as the nested oracle. Both scoreboard cells replay EQUAL. After the PR, 76 of the 93 match Spark on outcome, rows and normalized text; the other 17 carry residue records R-13..R-17. Before the PR, 23 of the first 54 matched. | `python/repark/tests/u8_write_sql_nested_spark_oracle.json`, `python/repark/tests/test_ice_write_sql_1.py`, `crates/repark-spark/src/tests/nested_assign_oracle.rs` |
| AT-2 | ATTACKED | Keys cover struct, deep struct, map value, map key, array of struct, array of INT, atomic, missing field, missing column, qualifiers (alias, table, full name), case and quoting. Values cover casts, NULL, row reads, source reads, sub-structs by name. Clauses cover matched, conditional matched, NOT MATCHED BY SOURCE, INSERT keys, COW and MoR. | `crates/repark-spark/src/merge/nested_assign/tests.rs`, `python/repark/tests/u8_write_sql_nested_spark_oracle.json` |
| AT-3 | ATTACKED | Every refusal is raised before the write plans, and each replay compares the table's rows after the refusal. | `crates/repark-spark/src/tests/nested_assign_oracle.rs` |
| AT-4 | N/A | The fold rewrites assignments before the existing UPDATE and MERGE paths; commit, isolation and branch handling are unchanged. | — |
| AT-5 | N/A | No privileged action, credential or path handling. Field names reach the rebuilt SQL as quoted string literals and backticked identifiers, and the rebuilt expression re-parses through the door's own dialect. | — |
| AT-6 | ATTACKED | The full repark-spark lib suite (2253 tests) and the repark-iceberg write suite (539) stay green, and the MERGE gate still refuses a STRING leaf, a renamed field and a different width. | `crates/repark-iceberg/src/write/store_assign.rs`, `crates/repark-spark/src/tests/nested_assign.rs` |
| AT-7 | ATTACKED | A statement with no multi-part SET target returns before any table load. A folded statement adds one table load and one LIMIT 0 plan per nested value. | `crates/repark-spark/src/merge/nested_assign.rs`, `crates/repark-spark/src/update_cast.rs` |
| AT-8 | ATTACKED | The cast SQL has one home in repark-iceberg. The fold is one module that both routes call. There is no new crate edge and no code comment. router.rs stays at 1000 lines, lib.rs at 156, merge.rs at 962 and the test module at 974. | `crates/repark-iceberg/src/write/update_cast.rs`, `crates/repark-spark/src/merge.rs`, `crates/repark-spark/src/router.rs` |
| AT-9 | ATTACKED | The registry gains DML-9 (FIXED 2026-09-25), with the residues stated. | `docs/spark-sql-iceberg-parity.md` |
| AT-10 | ATTACKED | Each load-bearing line was broken and its named tests ran red (M38..M47). | `crates/repark-spark/src/merge/nested_assign/tests.rs`, `crates/repark-iceberg/src/write/store_assign.rs` |
