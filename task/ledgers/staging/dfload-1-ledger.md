# Unit ledger — DFLOAD-1 (U10) · `format("iceberg").load(<path>)` through a static table (lane xo58-dfl)

**Date:** 2026-09-23 · **Branch:** `fix/u10-df-load-path` · **Base:** `f411192d` · **Model:** swe-2-high · **Policy:** [../../../AGENTS.md](../../../AGENTS.md).
**Path:** STANDARD. **risk_tier: standard** (a new read door beside the catalog one; the catalog route is untouched).

**Retires:** this ledger moves to `../completed/` when the unit's last commit lands.

**Why now.** On `f411192d` `spark.read.format("iceberg").load(p)` hands every argument to the catalog
identifier parser, so any `p` containing `/` — a table location or a metadata file — refuses
`AnalysisException: invalid table identifier '<p>': invalid unquoted segment` (measured,
`/tmp/xo-xo-opus58/probe/dfload-finding.md`; scoreboard cells `R-DF-LOAD-PATH` and
`R-DF-LOAD-METADATA-JSON`). Spark 4.1.2's `IcebergSource` treats an argument containing `/` as a
filesystem path: `.metadata.json` pins that file, anything else is a table location whose current
metadata is resolved. Both cells record Spark answering `[[2,"b","y"],[3,"c","x"]]` on the
create-insert-delete fixture.

**The route as ruled (WO dfload-r1 §3).** The `format("iceberg").load` arm tests `"/" in argument`
first — Spark's own rule — and nothing else changes: identifiers without `/` keep the catalog route
byte-for-byte, including `ns.t`, `cat.ns.t`, `ns.t.snapshots`, `` `ns`.`t` `` and the legacy
`snapshot-id`/`as-of-timestamp`/`tag` pins on an identifier. On the path route, a `.metadata.json`
suffix loads that file directly through the fork's `StaticTable::from_metadata_file`; any other path
is a location — strip one trailing `/`, read `<loc>/metadata/version-hint.text` when it exists (a
bare integer in the Java `int` range selects `v<N>.metadata.json`, which must exist on disk else `AnalysisException` names
the path and the hinted file; a hint that is not a single integer in that range falls through, like Iceberg's
`HadoopTableOperations.findVersion`), else list the DIRECT children of `<loc>/metadata/` — the
fork's local `FileIO::list` is recursive, matching `findVersion`'s non-recursive `listStatus` —
and take the `*.metadata.json` with the highest leading integer (`NNNNN-<uuid>`, or `v<N>` with N in
the Java `int` range, as `HadoopTableOperations.version` parses it with `Integer.parseInt`); no
resolvable metadata raises
`AnalysisException` naming the supplied path. The resolved table reads through
`IcebergStaticTableProvider::try_new_from_table` — a static, read-only provider handed to
`read_table`; the pins observe that no registration is left behind (C-007). Any time-travel or
incremental reader option beside a path refuses the pinned `AnalysisException` before resolution —
declared rather than ignored (C-004). **WO dfload-r5
§2:** a lower-case `file://` argument whose next character is not `/` (a non-empty authority,
`localhost` included) refuses `IllegalArgumentException` with Spark's `Wrong FS: <p>, expected: file:///` text
before `file_io_for_location` runs — `<p>` is the argument verbatim for `.metadata.json`, else the
argument with at most one trailing `/` removed plus `/metadata` (measured on live Spark 4.1.2 + Iceberg in
`/tmp/sparkm`, `/tmp/xo-xo-opus62/probe/dflfs-spark.out`). **WO dfload-r6 §3:** before the same
point, a `file:` argument of any scheme case whose remainder is `/X` (X not starting `/`) or `///X`
reads exactly like `file:///X`, and a lower-case `file:` argument whose next character is not `/`
refuses `IllegalArgumentException` `java.net.URISyntaxException: Relative path in absolute URI: <p>`
(`<p>` the argument with at most one trailing `/` removed); refusals that name the path keep
naming the argument as supplied (measured against a Hadoop-catalog table on Spark 4.1.2,
`/tmp/xo-xo-opus62/probe/dflfs{2,3,4}-spark.out`).

**Not in this unit:** the fork (pin `604edca` unchanged — `StaticTable::from_metadata_file`,
`FileIO::list`, `IcebergStaticTableProvider` are already public), `Cargo.toml`/`Cargo.lock`,
`time_travel.rs` (both crates), `wap.rs`, `metadata_tables.rs`, `STATUS.md`, existing expected
values, and the catalog identifier route's behaviour.

## PROPOSITION LEDGER — DFLOAD-1 — 2026-09-23

| C-001 | A load argument that is an absolute local path to a table location (`/abs`, so it contains `/` and takes the path door), with or without one trailing `/`, and with no reader option that C-004 refuses, reads that table's current snapshot. | `python/repark/tests/test_iceberg_load_path.py::test_load_table_location_reads_current_rows`, `test_load_table_location_trailing_slash`. | **PROVEN** | `load(<abs>)` and `load(<abs>/)` each answer `_assert_current`: the sorted rows `== [[2,"b","y"],[3,"c","x"]]` (the current snapshot after the delete), with `int64` / `string` / `string` Arrow types. Before the change the same `load(<path>)` refused `AnalysisException` `invalid table identifier '<path>': invalid unquoted segment`, as measured in `/tmp/xo-xo-opus58/probe/dfload-finding.md`; that is a record of the old behaviour, not a pin. |
| C-002 | A load argument containing `/` and ending `.metadata.json` that names an absolute local file (`/abs/…/metadata/<file>.metadata.json`), with no reader option that C-004 refuses, loads that exact file's snapshot: the latest file the current rows, an older file that version's rows. | `test_load_latest_metadata_file_reads_current_rows`, `test_load_older_metadata_file_reads_that_version`; Rust `explicit_metadata_path_resolves_verbatim`. | **PROVEN** | The latest file answers `_assert_current` (`== [[2,"b","y"],[3,"c","x"]]`), and the pre-delete file answers `_sorted_rows(arrow) == [[1,"a","z"],[2,"b","y"],[3,"c","x"]]`. `resolve_metadata_location` returns a `.metadata.json` path verbatim (`assert_eq!(resolved, pinned)`). Removing the `.metadata.json` early return turns `explicit_metadata_path_resolves_verbatim` red (r4 mutation). As a design fact with no pin: `reader.py` routes on `"/" in path` only, so a slash-free argument such as `v1.metadata.json` is an identifier on the catalog route (C-006), not a path. |
| C-003 | For a location on the path door, metadata resolution prefers `version-hint.text`. A bare integer within the Java `int` range (C-010) selects `v<N>.metadata.json`, which must exist on disk, else `AnalysisException` names the path and the hinted file; a pinned non-integer hint falls through to the listing. Otherwise it takes the highest leading-integer metadata file among the DIRECT children of `<loc>/metadata/`, in `NNNNN-<uuid>` or `v<N>` form. The `v` stem is a whole ASCII-digit integer within the Java `int` range (C-010) behind a lower-case `v`. The numbered form's suffix is a canonical 36-character UUID, hex of any case, with dashes at 8/13/18/23. | Rust `iceberg_path::tests`: `version_hint_wins_over_directory_listing`, `hinted_metadata_file_must_exist`, `unparsable_hint_falls_back_to_listing`, `non_utf8_hint_falls_back_to_listing`, `hint_zero_selects_v0`, `hint_below_java_int_range_falls_back_to_listing`, `hint_beyond_u64_falls_back_to_listing`, `numbered_metadata_files_pick_the_highest_version`, `numbered_metadata_requires_a_canonical_uuid`, `v_form_metadata_files_pick_the_highest_version`, `v_form_requires_a_whole_integer_stem`, `names_outside_the_two_forms_are_ignored`, `uuid_dashes_at_wrong_positions_are_rejected`, `uuid_suffix_length_and_case_edges`, `upper_case_v_prefix_is_not_a_candidate`, `valid_forms_still_resolve`, `each_valid_form_parses_to_its_version`, `nested_metadata_files_are_not_candidates`, `nested_metadata_files_are_not_candidates_trailing_slash`, `nested_metadata_files_are_not_candidates_file_scheme`, `nested_numbered_metadata_is_not_a_candidate`, `only_nested_metadata_names_the_supplied_path`; Python `test_load_hinted_missing_metadata_names_the_path`. | **PROVEN** | A hint of `3` selects `v3` over a listed `00010-<uuid>`; a hint of `0` selects `v0`. A hint of `7` with no `v7.metadata.json` refuses. The pinned non-integer hints `abc`, the bytes `\xff\xfe3`, `-1` and `99999999999999999999` each fall back to the listing. `00010-` beats `00002-`, and `v10` beats `v9` and `v999junk`. Each of these gives `None` and is skipped: `999-`, `12-notauuid`, `+12-<uuid>`, `-<uuid>`, `v+3`, `v.metadata.json`, `v-1`, `v1.2`, `12-<uuid>-extra`, a non-hex UUID, the four wrong-dash-position UUIDs, 35- and 37-character suffixes, `V3.metadata.json`, and `junk.metadata.json.txt` / `abc.metadata.json` / `5.metadata.json`. `00001-<uuid>`, `v1`, `v007`, an upper-case-hex and a mixed-case-hex UUID each parse to their version. Nested `metadata/sub/…` files are not candidates under `/abs`, `/abs/` and `file:///abs` roots; nested `metadata/metadata/…` files are not candidates under `/abs`; a location whose only metadata is nested refuses. The hinted-missing refusal equals, in full, `no Iceberg table found at '<path>': version-hint.text names 'v7.metadata.json', which does not exist`, on the Rust resolver (`assert_eq!` on `to_string()`) and on the Python door (`str(raised.value) ==`). Mutations, each turned red before restore: reverting the full-stem parse turned `v_form_requires_a_whole_integer_stem` red (r2, re-run r4); dropping the hinted-file exists check turned `hinted_metadata_file_must_exist` red (r2, re-run r4); accepting an empty UUID suffix turned `numbered_metadata_requires_a_canonical_uuid` red (r3); dropping the UUID check turned `names_outside_the_two_forms_are_ignored` red (r3); dropping the direct-child filter turned the five pins `nested_metadata_files_are_not_candidates`, `nested_metadata_files_are_not_candidates_trailing_slash`, `nested_metadata_files_are_not_candidates_file_scheme`, `nested_numbered_metadata_is_not_a_candidate` and `only_nested_metadata_names_the_supplied_path` red (r4); accepting any four dashes turned `uuid_dashes_at_wrong_positions_are_rejected` red (r4); erroring on a non-UTF-8 hint turned `non_utf8_hint_falls_back_to_listing` red (r4); appending ` extra` to the engine text turned `hinted_metadata_file_must_exist` red while the r7 `contains` pins stayed green (r8). |
| C-004 | Each time-travel reader option (`snapshot-id`, `as-of-timestamp`, `branch`, `tag`, `versionAsOf`, `timestampAsOf`) and each incremental reader option (`start-snapshot-id`, `end-snapshot-id`, `start-timestamp`, `end-timestamp`) beside a path refuses the pinned `AnalysisException` before metadata resolution, naming the refused key lower-cased. The key match ignores case, as pinned for `SNAPSHOT-ID`, `Snapshot-Id`, `versionAsOf` and `timestampAsOf`. Two refused keys are named sorted and `, `-joined, as pinned for `tag` + `start-timestamp`. The semantic-option gate refuses `compression` and `mergeSchema` first, and the unrelated options `foo` and `split-size` are ignored. | `python/repark/tests/test_iceberg_load_path.py::test_load_path_with_travel_or_incremental_option_refuses` (12 parameters), `test_load_path_with_two_refused_options_names_them_sorted`, `test_load_path_option_refuses_before_resolution`, `test_load_path_semantic_gate_refuses_first` (`compression`, `mergeSchema`), `test_load_path_ignores_an_unrelated_option` (`foo`, `split-size`); facade `reader_iceberg_path.load_iceberg_path`. | **PROVEN** | `PATH_REFUSED_OPTIONS` holds 12 parameters over 10 distinct lower-cased keys: `snapshot-id`, `SNAPSHOT-ID`, `Snapshot-Id`, `as-of-timestamp`, `branch`, `tag`, `versionAsOf`, `timestampAsOf`, `start-snapshot-id`, `end-snapshot-id`, `start-timestamp`, `end-timestamp`. Each parameter raises `AnalysisException` whose `str` equals, in full, `format('iceberg').load(<path>) reads one pinned metadata snapshot and does not support time-travel or incremental options; got <key lower-cased>`. `.option("tag", "main").option("start-timestamp", "1")` equals the same text ending `got start-timestamp, tag`. `snapshot-id` beside a missing location equals the option text, not the no-metadata text. `compression` and `mergeSchema` beside a path equal `reader option '<key>' is not supported by repark yet (would silently change load semantics if ignored)`. `foo` and `split-size` beside a path leave `_assert_current` rows `== [[2,"b","y"],[3,"c","x"]]`. Mutations, each turned red before restore: dropping the `unsupported` check turned the option pins of the day red (r4, before the r9 parametrize); appending ` extra` to the text turned the two option pins of the day red (r8); filtering only `_ICEBERG_TIME_TRAVEL_OPTIONS` turned the four incremental parameters and `test_load_path_with_two_refused_options_names_them_sorted` red (r9); filtering `{"snapshot-id", "start-snapshot-id"}` turned the 8 parameters whose keys are outside that set, and the two-key test, red (r9); `list(` in place of `sorted(` turned the two-key test red (r9); moving the check after the native read turned `test_load_path_option_refuses_before_resolution` red (r9). |
| C-005 | A local location reaching the path door with no resolvable metadata, and no reader option that C-004 refuses, raises `AnalysisException` naming the supplied path. The pinned cases are a missing directory, an empty `metadata/` directory, a `metadata/` directory whose only metadata files are nested, and one holding only non-candidate names. | `test_load_missing_location_names_the_path`, `test_load_empty_metadata_dir_names_the_path`; Rust `missing_location_reports_the_supplied_path`, `empty_metadata_dir_reports_the_supplied_path`, `only_nested_metadata_names_the_supplied_path`, `non_candidate_metadata_names_report_the_supplied_path`. | **PROVEN** | The four Rust pins cover `/no/such/dir`, an existing empty `metadata/` dir, a dir whose only metadata is nested, and a `metadata/` dir holding only `junk.metadata.json.txt`, `v2147483648.metadata.json` and `999-.metadata.json`. Each asserts `matches!(error, Error::Analysis(_))` and `assert_eq!(error.to_string(), no_metadata_message(&root))`, which is `no Iceberg table found at '<path>': expected metadata files under '<location>/metadata' or a '<location>/metadata/version-hint.text'` with the supplied path. The two Python pins assert `str(raised.value) ==` the same text for the missing and the empty-`metadata/` cases. A generic refusal without the path turned `missing_location_reports_the_supplied_path`, `empty_metadata_dir_reports_the_supplied_path` and `only_nested_metadata_names_the_supplied_path` red (r4 mutation). Appending ` extra` to the engine text turned those three red while the r7 `contains` pins stayed green (r8 mutation). |
| C-006 | The pinned slash-free arguments take the catalog route and answer as that route does: `ns.events`, `mem.ns.events`, the `snapshots`, `history` and `files` metadata-table selectors, the quoted `` `ns`.`events` ``, and the six time-travel options on `ns.events` (`snapshot-id`, `as-of-timestamp`, `tag`, `branch`, `versionAsOf`, `timestampAsOf`). | `test_load_two_part_identifier_keeps_catalog_route`, `test_load_three_part_identifier_keeps_catalog_route`, `test_load_metadata_table_identifier_keeps_catalog_route`, `test_load_metadata_table_selectors_keep_catalog_route` (`history`, `files`), `test_load_quoted_identifier_keeps_catalog_route`, `test_load_identifier_snapshot_id_keeps_spark_refusal`, `test_load_identifier_travel_option_keeps_catalog_route` (`as-of-timestamp`, `tag`, `versionAsOf`, `timestampAsOf`), `test_load_identifier_branch_keeps_catalog_route`. | **PROVEN** | 12 test cases in total. `ns.events`, `mem.ns.events`, `` `ns`.`events` `` and `branch=main` on `ns.events` answer `_assert_current` rows. `ns.events.<selector>` equals `Error during planning: table 'ns.events.<selector>' not found` in full for `snapshots`, `history` and `files`. On `ns.events`, these raise `IllegalArgumentException` whose `str` equals, in full: `snapshot-id` gives ``Time travel option `snapshot-id` is no longer supported, use Spark built-in `versionAsOf` instead``; `as-of-timestamp` gives ``Time travel option `as-of-timestamp` (in millis) is no longer supported, use Spark built-in `timestampAsOf` instead (properly formatted timestamp)``; `tag` gives ``Time travel option `tag` is no longer supported, use Spark built-in `versionAsOf` instead``; `versionAsOf=1` gives `Cannot find snapshot with ID 1`; `timestampAsOf=1` gives `Cannot find a snapshot older than 1970-01-01T00:00:01+00:00`. Routing dotted identifiers to the path door turns all 12 cases red (r7 mutation, re-run in r9 over the 12). These pins assert the route's current answers; they do not compare against the pre-change branch. |
| C-007 | The path read leaves no registration behind: the session's catalogs, every listed catalog's namespaces and tables, its temp views and its current catalog are equal before the load and after the frame is collected. Both scoreboard cells answer Spark's recorded rows on the lane build. | `python/repark/tests/test_iceberg_load_path.py::test_load_path_registers_no_catalog_table` (its `_registration_inventory` walks every listed catalog and records `currentCatalog()`); `cells_read.py` replay of `R-DF-LOAD-PATH` and `R-DF-LOAD-METADATA-JSON`. | **PROVEN** | The test first makes the non-default `mem` current and asserts `before["current_catalog"] == "mem"`. It takes `_registration_inventory` before the load and again after `_assert_current` has collected the frame, and asserts them equal. The inventory holds the `listCatalogs()` names; for each listed catalog (`mem`, `spark_catalog`), made current in turn and then restored, its `listDatabases()` names and each namespace's `listTables(<ns>)` names; the `listTables()` temp views; `list_temp_view_names()`; and `currentCatalog()` after the restore. The test then asserts `currentCatalog() == "mem"` and `not spark.catalog.table_exists("path.events")`. The pin observes only before and after, not the read itself. As a design fact with no pin, `read_iceberg_path` hands the provider to `SessionContext::read_table` and never calls `register_table`. Mutations, each turned the pin red: registering the provider as a temp view inside `read_iceberg_path` (r5); creating a namespace `spark_catalog.path_mutant` inside the path door, while the r6 current-catalog-only inventory stayed green (r7); making the helper reset to `spark_catalog` instead of restoring (`'spark_catalog' == 'mem'`), while the r7 pin stayed green (r8). The `cells_read.py` replay on the r10 lane build answers `[[2,"b","y"],[3,"c","x"]]` for both cells, equal to the recorded live-Spark answer. |
| C-008 | A load argument that starts with the lower-case `file://` and whose next character is not `/` refuses `IllegalArgumentException` with Spark's `Wrong FS: <p>, expected: file:///`. That is a non-empty authority, pinned for `tmp` and `localhost`. `<p>` is the argument verbatim when it ends `.metadata.json`, and otherwise the argument with at most one trailing `/` removed plus `/metadata`. `file:///<abs>`, `file:///<abs>/` and `file:///<abs>/metadata/<latest>.metadata.json` still read the current rows. The upper-case `FILE://` and mixed-case `File://` authority spellings are not refused this way. | Rust `iceberg_path::tests`: `file_authority_location_refuses_wrong_fs`, `file_authority_location_trailing_slash_refuses_wrong_fs`, `file_authority_metadata_file_refuses_wrong_fs`, `file_localhost_location_refuses_wrong_fs`, `file_localhost_metadata_file_refuses_wrong_fs`, `file_authority_two_trailing_slashes_keeps_one`, `upper_case_file_authority_is_not_refused_wrong_fs`; Python `test_load_file_authority_location_refuses_wrong_fs`, `test_load_file_authority_metadata_file_refuses_wrong_fs`, `test_load_file_scheme_location_reads_current_rows`, `test_load_file_scheme_latest_metadata_file_reads_current_rows`. | **PROVEN** | Six Rust pins assert `Error::IllegalArgument(message)` with `message ==` the full `Wrong FS` string. The forms are `file://tmp/…`, `file://tmp/…/`, `file://tmp/…//` (giving `Wrong FS: file://tmp/…//metadata, expected: file:///`, one of the two slashes removed), `file://tmp/…/metadata/v1.metadata.json`, `file://localhost/tmp/…` and `file://localhost/tmp/…/metadata/v1.metadata.json`. The two Python Wrong FS pins assert `str(raised.value) ==` the full string for the location and latest-metadata-file forms. The `file:///` near misses answer `_assert_current` rows. `upper_case_file_authority_is_not_refused_wrong_fs` asserts that `FILE://tmp/…` and `File://localhost/…` give `Error::Analysis` equal to the full no-metadata text naming the argument. That is current behaviour, ledgered as the R-DF-LOAD-SPELLINGS divergence. On `c2f719aa` the five Rust pins then present and the two Python Wrong FS pins ran red (seven). Dropping the refusal call turned `file_authority_location_refuses_wrong_fs`, `file_authority_location_trailing_slash_refuses_wrong_fs`, `file_authority_metadata_file_refuses_wrong_fs`, `file_localhost_location_refuses_wrong_fs` and `file_localhost_metadata_file_refuses_wrong_fs` red (r5 mutation). |
| C-009 | A `file:` argument whose remainder after `file:` is `/X` (X not starting `/`) or `///X` reads the same rows as `file:///X`, whatever the scheme case. The pinned spellings are `file:/abs`, `FILE:/abs`, `fIlE:/abs`, `FILE:///abs`, `fIlE:///abs` and `file:/abs/metadata/<latest>.metadata.json`, and `file:////abs` keeps reading. A lower-case `file:` argument whose next character is not `/` refuses `IllegalArgumentException` with exactly `java.net.URISyntaxException: Relative path in absolute URI: <p>`, where `<p>` is the argument with at most one trailing `/` removed. The upper-case `FILE:` and mixed-case `File:` relative spellings are not refused this way. The normalised `file:/<abs>/no/such/dir` still names the argument as supplied. | Rust `iceberg_path::tests`: `file_single_slash_location_reads_rows`, `file_upper_case_single_slash_location_reads_rows`, `file_mixed_case_single_slash_location_reads_rows`, `file_upper_case_empty_authority_location_reads_rows`, `file_mixed_case_empty_authority_location_reads_rows`, `file_single_slash_latest_metadata_file_reads_rows`, `file_triple_slash_location_reads_rows`, `file_relative_location_refuses_uri_syntax`, `file_relative_location_trailing_slash_refuses_uri_syntax`, `file_relative_metadata_file_refuses_uri_syntax`, `relative_file_path_keeps_one_of_two_trailing_slashes`, `upper_case_relative_file_path_is_not_refused_uri_syntax`, `file_single_slash_missing_location_names_the_supplied_argument`; Python `test_load_file_single_slash_location_reads_current_rows`, `test_load_file_single_slash_latest_metadata_file_reads_current_rows`, `test_load_file_four_slash_location_reads_current_rows`, `test_load_file_relative_location_refuses_uri_syntax`, `test_load_file_relative_location_two_trailing_slashes_keeps_one`. | **PROVEN** | The seven Rust read pins build a real two-row table and assert the sorted `id` column `== vec![1, 2]`, for `file:/abs`, `FILE:/abs`, `fIlE:/abs`, `FILE:///abs`, `fIlE:///abs`, `file:/abs/metadata/<latest>.metadata.json` and the `file:///abs` near miss. The Python pins run `_assert_current` on `file:/abs`, `FILE:/abs`, `fIlE:/abs`, `file:/abs/metadata/<latest>.metadata.json` and `file:////abs`. Four Rust refusal pins assert `Error::IllegalArgument(message)` with `message ==` the full `URISyntaxException` string, for `file:tmp/…`, `file:tmp/…/`, `file:tmp/…//` (naming `file:tmp/…/`) and `file:tmp/…/metadata/v1.metadata.json`; the two Python refusal pins assert `str(raised.value) ==` the full string for `file:tmp/…` and `file:tmp/…//`. `upper_case_relative_file_path_is_not_refused_uri_syntax` asserts that `File:tmp/…` and `FILE:tmp/…` give the full `malformed storage location` `AnalysisException`, the R4 divergence. `file:/<abs>/no/such/dir` gives `Error::Analysis` equal to the no-metadata text naming the supplied argument. On `60770d43` these ten pins ran red: four Rust read pins, three Rust refusal pins, and three Python `file:/` / `file:tmp` pins. Mutations, each turned red before restore: dropping the normalisation turned `file_single_slash_location_reads_rows`, `file_upper_case_single_slash_location_reads_rows`, `file_mixed_case_empty_authority_location_reads_rows` and `file_single_slash_latest_metadata_file_reads_rows` red (r6); dropping the relative refusal turned `file_relative_location_refuses_uri_syntax`, `file_relative_location_trailing_slash_refuses_uri_syntax` and `file_relative_metadata_file_refuses_uri_syntax` red (r6); naming the normalised spelling in the no-metadata error turned `file_single_slash_missing_location_names_the_supplied_argument` red (r6); removing every trailing slash turned `relative_file_path_keeps_one_of_two_trailing_slashes` red (r7); accepting only the exact spellings `file:` and `FILE:` turned `file_mixed_case_single_slash_location_reads_rows` and `file_mixed_case_empty_authority_location_reads_rows` red (r10). |
| C-010 | A `v<N>.metadata.json` name and a `version-hint.text` value are versions only within the Java `int` range `0..=2147483647`, matching Iceberg 1.11's `HadoopTableOperations`, which parses them with `Integer.parseInt` (read from its bytecode in r7). A `v<N>` name beyond the range is not a listing candidate, `v0` is the lowest and `v2147483647` the highest candidate, and a `v<N>` beyond `u64` stays ignored. A hint beyond the range falls through to the listing, and a hint of `2147483647` selects that file. The `NNNNN-<uuid>` form keeps its `u64` range `0..=18446744073709551615`, since Iceberg's Hadoop path does not parse that form; its `VERSION_PATTERN` is `v([^\.]*)\..*`. | Rust `iceberg_path::tests`: `v_form_versions_beyond_java_int_are_not_candidates`, `v_form_java_int_max_is_the_highest_candidate`, `v_form_zero_is_the_lowest_candidate`, `v_form_u64_overflow_stays_ignored`, `hint_beyond_java_int_falls_back_to_listing`, `hint_at_java_int_max_selects_that_file`, `numbered_form_zero_is_a_candidate`, `numbered_form_above_u32_is_a_candidate`, `numbered_form_u64_max_is_a_candidate`, `numbered_form_beyond_u64_is_ignored`, `numbered_form_above_u32_beats_a_small_number`; Python `test_load_v_form_beyond_java_int_is_not_a_candidate`. | **PROVEN** | Measured on Spark 4.1.2 + Iceberg 1.11 against a Hadoop-catalog table (`/tmp/xo-xo-opus62/probe/dflver.py`, `dflver-spark.out`). With `v1`, `v2` and a `v2147483648` copy of `v1`, and no hint, `load` answers `v2`'s rows; with a `v2147483647` copy as well, it answers that copy's (empty) rows. A hint of `2147483648` answers `v2`'s rows, a `v99999999999999999999` copy is ignored, and a hint of `2147483647` answers that copy. The Rust pins assert: `metadata_file_version("v2147483648.metadata.json") == None`; `metadata_file_version("v2147483647.metadata.json") == Some(2_147_483_647)`; `metadata_file_version("v0.metadata.json") == Some(0)`; `latest_metadata_location` picks `v2` over `v2147483648`, `v2147483647` over `v2147483648`, and `v2` over `v99999999999999999999`; a `2147483648` hint resolves `{root}/metadata/v2.metadata.json` and a `2147483647` hint resolves `{root}/metadata/v2147483647.metadata.json`. For the numbered form they assert `00000-<uuid>` gives `Some(0)`, `4294967296-<uuid>` gives `Some(4_294_967_296)`, `18446744073709551615-<uuid>` gives `Some(u64::MAX)` and `18446744073709551616-<uuid>` gives `None`, and that `4294967296-<uuid>` beats `00002-<uuid>`. The Python pin asserts that a `v2147483648.metadata.json` copy of the create snapshot leaves `_assert_current` rows. On `af49f4c0` the Python pin and three of the four Rust pins then present ran red; `v_form_u64_overflow_stays_ignored` was already green. Mutations, each turned red before restore: parsing versions as `u64` turned `v_form_versions_beyond_java_int_are_not_candidates`, `v_form_java_int_max_is_the_highest_candidate` and `hint_beyond_java_int_falls_back_to_listing` red (r7); excluding the endpoints (`0 < v < 2147483647`) turned `v_form_zero_is_the_lowest_candidate`, `hint_at_java_int_max_selects_that_file`, `v_form_java_int_max_is_the_highest_candidate` and `v_form_versions_beyond_java_int_are_not_candidates` red (r9); parsing the numbered leg as `u32` then widening turned `numbered_form_above_u32_is_a_candidate`, `numbered_form_u64_max_is_a_candidate` and `numbered_form_above_u32_beats_a_small_number` red, while `numbered_form_beyond_u64_is_ignored` stayed green by design (r10). The hint's `0` and `-1` edges are pinned under C-003. |

## Design notes (why, not what)

- The engine lives in `crates/repark-core/src/iceberg_path.rs` with its picker battery in `iceberg_path/tests.rs`: `session.rs` sits exactly at its 1000-line ceiling, so the new method is a sibling free-standing impl block in a new module rather than an addition there. `lib.rs` gained `mod iceberg_path;` at net-zero by removing a stale half-finished comment whose text already lives at `session.rs`.
- The PyO3 door is a module-level `#[pyfunction] read_iceberg_path` in `session_sources.rs` — the same free-function shape the incremental binding uses — because `crates/repark-python/src/session.rs` sits at its exact 1122-line baseline and cannot take a method.
- Python holds only the routing decision and the option refusal: `reader.py` tests `"/" in path` inside the iceberg arm and delegates to `reader_iceberg_path.load_iceberg_path`, which refuses option keys, forwards the raw path, and wraps the returned frame. Resolution order, FileIO construction and provider wiring all live in the engine, matching the bindings-as-thin-adapter discipline.
- `file_io_for_location` is reused rather than re-implemented, so the path route builds its FileIO the same way the catalog path does; the pinned spellings that reach it and read are `/abs`, `/abs/`, `file:///abs`, `file:///abs/`, `file:////abs` and the `file:///` forms that `local_file_location` rewrites `file:/abs`, `FILE:/abs`, `fIlE:/abs`, `FILE:///abs` and `fIlE:///abs` to, plus the `.metadata.json` forms of `/abs`, `file:///abs` and `file:/abs`. A lower-case `file://<authority>/…` or `file:<relative>` argument never reaches it: `refuse_file_authority` and `refuse_relative_file_path` run first in `read_iceberg_path` (C-008, C-009). The rewrite is local to the path door; `file_io_for_location` and the catalog route still refuse `file:/abs`.
- By design, the option refusal fires in the facade before the native call, so a `snapshot-id` beside a path is not half-applied and the path pins exactly one metadata snapshot. C-004's `test_load_path_option_refuses_before_resolution` observes the refusal winning over a missing location; no pin observes the native call itself.
- The fork's local `FileIO::list` is recursive (it walks subdirectories), so `resolve_metadata_location` keeps only entries whose parent is the listed `metadata/` directory — `is_direct_child`, comparing `file://`-stripped spellings because local `FileInfo.location` comes back without the scheme and slash-normalized (measured for `/abs`, `/abs/` and `file:///abs` roots, each pinned by a `nested_metadata_files_are_not_candidates*` test). A `file://<authority>` root, whose stripped spelling would not match the listed parent, is refused before listing (C-008), so every spelling that reaches `is_direct_child` starts with `/` on both sides.

## Observed, out of unit

- `load()` on a path that is not Iceberg-shaped (e.g. a parquet directory) now reaches metadata resolution and refuses with `AnalysisException` naming the path, instead of the old invalid-identifier refusal — a better message for the same refusal class; callers passing a non-Iceberg path with `format("iceberg")` were already refusing.
- Spark's `IcebergSource` also accepts `OPTIONS (path '...')`-style table-function spellings; that door is unmeasured and out of scope here.
- A `version-hint.text` that is not a single trimmed integer within the Java `int` range (or not UTF-8) falls through to directory listing — Iceberg's `HadoopTableOperations.findVersion` shape — pinned by `unparsable_hint_falls_back_to_listing` and `non_utf8_hint_falls_back_to_listing`. A parseable hint whose `v<N>.metadata.json` does not exist is a hard `AnalysisException` naming the path and the file (critic finding V-001), pinned by `hinted_metadata_file_must_exist` and `test_load_hinted_missing_metadata_names_the_path`.
- **KNOWN DIVERGENCE — residue `R-DF-LOAD-SPELLINGS` (WO dfload-r6 §3 R4, no code change).** Measured on Spark 4.1.2 + Iceberg against a Hadoop-catalog table (`/tmp/xo-xo-opus62/probe/dflfs{3,4}-spark.out`) and on the r6 build:
  - a non-lower-case scheme with an authority (`FILE://tmp/…`, `FILE://tmp/…/`, `File://localhost/abs`, `FILE://tmp/…/metadata/v2.metadata.json`): Spark raises `Py4JJavaError` `org.apache.iceberg.exceptions.RuntimeIOException: Failed to get file system for path: <p>/metadata/version-hint.text` (the `.metadata.json` form names the argument) caused by `UnsupportedFileSystemException: No FileSystem for scheme "FILE"` / `"File"`; RePark refuses `AnalysisException` `no Iceberg table found at '<argument>'` for the location forms and `PySparkException` `DataInvalid => Failed to read file …` for the `.metadata.json` form. Both refuse, in different classes.
  - `//abs`: Spark raises the same `RuntimeIOException` caused by `No FileSystem for scheme "null"`; RePark reads the table.
  - a non-lower-case relative scheme (`File:tmp/…`): unmeasured on Spark; RePark refuses `AnalysisException` `malformed storage location` from `file_io_for_location`.
  - the degenerate forms: Spark answers `file:` with `IllegalArgumentException` `Cannot parse identifier: file:`, and `file:/`, `file://`, `file:///` and `file:/abs/metadata/version-hint.text` with `AnalysisException [TABLE_OR_VIEW_NOT_FOUND]`. RePark answers `file:` (no `/`, so the catalog route) with `AnalysisException` `invalid table identifier 'file:'`, and `file:/`, `file://`, `file:///` and `file:/abs/metadata/version-hint.text` with `AnalysisException` `no Iceberg table found at '<argument>'`. Both refuse `AnalysisException` for all four, with different texts.
- Still UNRULED and unmeasured on Spark: relative `a/b` refuses `AnalysisException` `is not an absolute path`; `s3://` / `s3a://` without a configured region fail `PySparkException` `region is missing`.
- Correction to the r5 record: the r5 probe (`dflfs-spark.out`) ran on Spark 4.1.2 in `/tmp/sparkm`, not Spark 3.5. Its `TABLE_OR_VIEW_NOT_FOUND` for `file:///abs` and `file:/abs` came from an in-memory table with no `v<N>.metadata.json`, not from Spark refusing the spelling. Against a Hadoop-catalog table, Spark 4.1.2 reads both (`dflfs2-spark.out`).
- Class R residue, UNRULED (no code change in r7): `HadoopTableOperations.version` is `Integer.parseInt` over the `v(...)` capture, so Spark also accepts a signed stem (`v+3`, `v-1`). After finding the maximum it reads `metadataFilePath(<int>)`, the canonical `v<N>.metadata.json`, not the listed name, so a `v007.metadata.json` counts as version 7 but Spark reads `v7.metadata.json`. RePark keeps C-003's ruled whole-digit stem and reads the listed file itself. Unmeasured on Spark.

## Close

All ten clauses C-001…C-010 are PROVEN by pin. The attestation below covers the ten categories for the whole unit.

```text
COVERAGE_ATTESTATION:
  pr_unit: dfload-1
  complete: true
  categories:
    - id: AT-1
      status: ATTACKED
      evidence: Every ruled behaviour — the contains-slash route, both resolution shapes, the option refusal, the missing-metadata refusal and the untouched catalog route — has a clause with a named pin; the picker ordering has a Rust pin per rule.
      artifacts: [python/repark/tests/test_iceberg_load_path.py, crates/repark-core/src/iceberg_path/tests.rs]
    - id: AT-2
      status: ATTACKED
      evidence: Boundaries covered are location vs trailing-slash location vs file-scheme spelling vs `file://<authority>` spelling (refused Wrong FS) vs `file:/abs` / `FILE:/abs` / `fIlE:///abs` (read as `file:///`) vs `file:<relative>` (refused URISyntaxException), latest vs older metadata file, hint vs listing, hinted file present vs missing, parsable vs unparsable vs non-UTF-8 vs beyond-Java-int hint, `v2147483647` vs `v2147483648` vs a `u64`-overflowing `v` stem, NNNNN-uuid vs vN naming, canonical vs malformed UUID suffixes and digit stems and dash positions, whole-integer vs junk v stems, highest-integer selection, direct vs nested metadata children, empty metadata dir vs missing dir vs only-nested dir, and the slash-free near-miss identifiers.
      artifacts: [test_load_table_location_trailing_slash, numbered_metadata_files_pick_the_highest_version, numbered_metadata_requires_a_canonical_uuid, version_hint_wins_over_directory_listing, hinted_metadata_file_must_exist, v_form_requires_a_whole_integer_stem, names_outside_the_two_forms_are_ignored, valid_forms_still_resolve, each_valid_form_parses_to_its_version, uuid_dashes_at_wrong_positions_are_rejected, non_utf8_hint_falls_back_to_listing, nested_metadata_files_are_not_candidates, nested_metadata_files_are_not_candidates_trailing_slash, nested_metadata_files_are_not_candidates_file_scheme, nested_numbered_metadata_is_not_a_candidate, only_nested_metadata_names_the_supplied_path, explicit_metadata_path_resolves_verbatim, file_authority_location_refuses_wrong_fs, file_authority_location_trailing_slash_refuses_wrong_fs, file_authority_metadata_file_refuses_wrong_fs, file_localhost_location_refuses_wrong_fs, file_localhost_metadata_file_refuses_wrong_fs, test_load_file_scheme_location_reads_current_rows, test_load_file_scheme_latest_metadata_file_reads_current_rows, file_single_slash_location_reads_rows, file_upper_case_single_slash_location_reads_rows, file_mixed_case_empty_authority_location_reads_rows, file_single_slash_latest_metadata_file_reads_rows, file_relative_location_refuses_uri_syntax, file_relative_location_trailing_slash_refuses_uri_syntax, file_relative_metadata_file_refuses_uri_syntax, file_single_slash_missing_location_names_the_supplied_argument, test_load_file_four_slash_location_reads_current_rows, test_load_file_relative_location_refuses_uri_syntax, relative_file_path_keeps_one_of_two_trailing_slashes, test_load_file_relative_location_two_trailing_slashes_keeps_one, v_form_versions_beyond_java_int_are_not_candidates, v_form_java_int_max_is_the_highest_candidate, v_form_u64_overflow_stays_ignored, hint_beyond_java_int_falls_back_to_listing, test_load_v_form_beyond_java_int_is_not_a_candidate]
    - id: AT-3
      status: ATTACKED
      evidence: Both refusals are read-path only — nothing is registered, written or mutated on refusal; the option refusal fires before any FileIO or metadata access.
      artifacts: [python/repark/src/repark/spark/session/reader_iceberg_path.py]
    - id: AT-4
      status: N/A
      justification: No new shared state. The read is one async resolution plus a static provider; the session's existing lock discipline is unchanged.
    - id: AT-5
      status: ATTACKED
      evidence: The route is bounded by the contains-slash test — a catalog identifier can never reach filesystem resolution, and a path can never reach the catalog. FileIO is built by the shared file_io_for_location, so no new scheme surface is parsed here.
      artifacts: [crates/repark-core/src/iceberg_path.rs, python/repark/src/repark/spark/session/reader.py]
    - id: AT-6
      status: ATTACKED
      evidence: Both scoreboard cells replayed against the lane build answer the recorded live-Spark rows [[2,"b","y"],[3,"c","x"]] exactly.
      artifacts: [docs/spark-sql-iceberg-parity.md, python/repark/tests/test_iceberg_load_path.py]
    - id: AT-7
      status: N/A
      justification: One metadata list or one hint read plus one metadata file per load — the same shape Spark's path resolution performs; no per-row work added.
    - id: AT-8
      status: ATTACKED
      evidence: Fork pin 604edca unchanged; only already-public fork APIs are consumed (StaticTable::from_metadata_file, FileIO::list, IcebergStaticTableProvider::try_new_from_table). Cargo.toml and Cargo.lock untouched.
      artifacts: [crates/repark-core/src/iceberg_path.rs]
    - id: AT-9
      status: ATTACKED
      evidence: The missing-metadata refusal names the supplied path verbatim; the option refusal names the offending option keys; catalog-route refusals keep their existing Spark texts byte-for-byte.
      artifacts: [test_load_missing_location_names_the_path, test_load_path_with_travel_or_incremental_option_refuses]
    - id: AT-10
      status: ATTACKED
      evidence: The Python pins ran red on the pre-change branch (5 path failures, 6 near-misses green, commit d82b7ca6) before the engine and facade made them green; each Rust picker pin fails on a one-line inversion of the rule it covers. Round 2 (critic r1): the three new resolver pins ran red on the rebased tree — hinted_metadata_file_must_exist and unparsable_hint_falls_back_to_listing and v_form_requires_a_whole_integer_stem — and both ruled mutations (drop the exists check, revert the full-stem parse) were shown red before restore. Round 3 (critic r2): numbered_metadata_requires_a_canonical_uuid and names_outside_the_two_forms_are_ignored ran red on the r2 tree, and both UUID-suffix mutations (empty suffix, unchecked suffix) were shown red before restore. Round 4 (critic r3): the five nested-listing pins ran red on the r3 tree; the mutations shown red before restore were drop the direct-child filter (five nested pins), accept any four dashes (uuid_dashes_at_wrong_positions_are_rejected), error on a non-UTF-8 hint (non_utf8_hint_falls_back_to_listing), remove the .metadata.json early return (explicit_metadata_path_resolves_verbatim), drop the path from no_metadata_error (the three path-naming pins), drop the facade option check (both option-refusal pins), and the r2 pair re-run (full-stem revert, exists-check drop). Round 5 (critic r4): the five Rust and two Python `Wrong FS` refusal pins ran red on c2f719aa; dropping the refusal call turned the five Rust pins red, and registering the provider as a temp view turned the C-007 inventory pin red, each before restore. Round 6 (WO dfload-r6): the seven Rust and three Python `file:` spelling pins ran red on 60770d43; dropping the normalisation (four Rust read pins), dropping the relative refusal (three Rust refusal pins) and naming the normalised spelling in the no-metadata error (the supplied-argument pin) each turned red before restore. Round 7 (critic r5): the three Rust Java-int range pins and the Python `v2147483648` pin ran red on af49f4c0. Before restore, these mutations each turned red: parsing versions as `u64` (three range pins), removing every trailing slash from the relative refusal (the two-slash pin), creating a `spark_catalog` namespace inside the path door (the every-catalog inventory pin, while the r6 current-catalog inventory stayed green), skipping the one-trailing-`/` strip in resolution (`listing_resolves_the_highest_numbered_metadata_file`, `nested_metadata_files_are_not_candidates_trailing_slash`), and routing dotted identifiers to the path door (the five C-006 pins). Round 8 (critic r6): every refusal pin on the branch now compares the full message. Before restore, these mutations each turned red: appending ` extra` to the facade option refusal (both C-004 pins, while the r7 regex pins stayed green), appending ` extra` to the engine's no-metadata and hinted-missing texts (five Rust pins, while the r7 `contains` pins stayed green), and resetting the inventory helper to `spark_catalog` instead of restoring (the C-007 pin, while the r7 pin stayed green). Round 9 (critic r7): every enumerated set member in C-004, C-005, C-006 and C-010 gained a pin. Before restore, these mutations each turned red: the three ruled facade filter mutations (incremental leg dropped, the r7 two-key filter, `sorted(` dropped), moving the option check after the native read, excluding the version-range endpoints, and re-running the dotted-identifier routing over all twelve C-006 cases. Round 10 (critic r8): every range, case-quantifier and trailing-slash bound in C-003, C-004, C-008, C-009 and C-010 has a pin per deciding member. Before restore, these mutations each turned red: a `u32` numbered parse, and accepting only the exact scheme spellings `file:` and `FILE:`.
      artifacts: [python/repark/tests/test_iceberg_load_path.py, crates/repark-core/src/iceberg_path/tests.rs]
```
