# Unit ledger — REVIEW-FIX-9 step 1 · the polars renderer matches polars at its boundaries

**Unit:** REVIEW-FIX-9 step 1 · **Date:** 2026-09-10 · **Branch:** `fix/review-fix-3-9-14` · **Base:** `origin/main`
**Model:** swe-2-high
**Policy:** [AGENTS.md](../../../AGENTS.md). **Path:** STANDARD. **risk_tier: standard.**

**Why now.** The review-1 logic round confirmed two renderer-boundary defects against
live polars 1.43.2: a four-element list cell renders in full where polars ellipsizes
(`[0, 1, … 3]`, Q-27), and `±999999.0` spells fixed where polars switches to
`±9.99999e5` (Q-28; Q-26's `max_rows = 1` half is worked in REVIEW-FIX-3, which this
card's D-3 subsumes — the shared pin lives once in
`test_polars_max_rows_boundaries_match_live_polars`).

**Retires:** this ledger moves to `../completed/` when the unit's last commit lands.

**Not in this step:** float spellings outside the measured bands (all spellings now
follow polars' `fmt_float` branches), nested-list depth behaviour, the duckdb
renderer, any dependency file, `STATUS.md`, `briefs/next-sequence.md`, `gh`, pushes.

## PROPOSITION LEDGER — REVIEW-FIX-9 step 1 — 2026-09-10

| Clause | Proposition (checkable) | Proof obligation | Verdict | Evidence |
|---|---|---|---|---|
| C-001 | D-1: the list-cell ellipsis threshold is polars' — a four-element list renders `[0, 1, … 3]`. | `test_polars_oracle_list_ellipsis_boundary` | **PROVEN** | Red 2026-09-10 (`pytest -k list_ellipsis_boundary`, base tree): `repr(frame.eager()) == _polars_text_of(frame)` failed — repark rendered the four-element cell in full, widening the `li` column one character over the oracle's `[0, 1, … 3]`. Green after `len(items) > 4` became `> 3` in `_polars_nested_at_depth`: lists of length 3 (`[0, 1, 2]`), 4 and 5 render byte-identically to `str(pl.from_arrow(...))` on the same Arrow table. |
| C-002 | D-2: float spelling follows polars at the fixed/scientific switch — `±999999.0` → `±9.99999e5`. | `test_polars_oracle_float_fixed_scientific_switch` | **PROVEN** | Red 2026-09-10 (same run): repark printed `999999.0` where the oracle prints `9.99999e5`. Green after `_polars_float_text` was re-derived from polars' `fmt_float` (`SCIENTIFIC_BOUND = 999999.0`): integral floats below the bound stay fixed, at-or-above go shortest-sci while the Rust `{:}`-length stays ≤ 9 (the length check counts the sign and omits `.0`), and longer expansions go four-decimal-sci when `|v|` leaves `[1e-6, 999999.0]`, six-decimal-trimmed otherwise. The derivation was validated against live polars on 963 probed values (boundary sweeps at ±1e-6..±1e16, randoms) with zero mismatches before it was written. |
| C-003 | D-3: the oracle pins exercise each boundary — list lengths 3, 4 and 5, and the float switch either side — against `str(pl.from_arrow(...))` on the same Arrow table, so a threshold cannot drift unpinned again; expected bytes are generated by live polars, never hand-typed. | the two new oracle pins | **PROVEN** | `test_polars_oracle_list_ellipsis_boundary` builds `array(0,1,2)` / `array(0,1,2,3)` / `array(0,1,2,3,4)` in one frame and asserts `repr(frame.eager()) == str(pl.from_arrow(frame.to_arrow()))`; `test_polars_oracle_float_fixed_scientific_switch` exercises `999998.0` (fixed below), `±999999.0` (the switch), `999999.5` (non-integral above, still fixed), `999999.9999` (past the bound through the length gate → `1.0000e6`) and `1234567.0` (shortest-sci `1.234567e6`). Both were red on the base tree and green after the fix. |

VERDICT: 3 clauses, 3 PROVEN, 0 OPEN, 0 REJECTED.

## Green

`pytest python/repark/tests/test_display_polars_default.py
python/repark/tests/test_display_lazy_1.py python/repark/tests/test_display_styles.py -q`
2026-09-10: `79 passed`; the wider display surface (show goldens, t3-ux-polish,
metadata tables, session, cache/persist — 110 tests) green in the same tree.

## Red first

## Residue

- The float rewrite is a full re-derivation, not a boundary patch: it also corrects
  the negative-integral nine-digit case (`-123456789.0`, whose sign counts in the
  length gate → `-1.2346e8`) and the non-integral just-past-the-bound case
  (`999999.9999` → `1.0000e6`), both pinned. Behaviour inside the previously correct
  bands is unchanged (the 963-value sweep covers them).
- `POLARS_FMT_*` env vars other than `POLARS_FMT_MAX_ROWS` stay out of scope; the
  oracle helper already clears `POLARS_FMT_STR_LEN` / `POLARS_FMT_MAX_COLS`.

## Gates

- `pytest … test_display_polars_default.py test_display_lazy_1.py test_display_styles.py -q`
  2026-09-10: `79 passed` (exit 0).
- `python scripts/check_lib_py.py` 2026-09-10: `640 files clean` (exit 0).
- Comment fence 2026-09-10: the staged-diff comment grep prints nothing.

```yaml
COVERAGE_ATTESTATION:
  pr_unit: review-fix-9
  complete: true
  categories:
    - id: AT-1
      status: ATTACKED
      evidence: All three clauses walked pin by pin with red outputs pasted in the cells: the list pin reds on the one-character column-width widening a full four-element cell causes; the float pin reds on the fixed 999999.0 spelling; both green byte-identically after the fix.
      artifacts: [python/repark/src/repark/spark/dataframe/polars_cells.py, python/repark/tests/test_display_polars_default.py]
    - id: AT-2
      status: ATTACKED
      evidence: Boundaries pinned on both sides: list lengths 3 (full) and 4/5 (elided); floats 999998.0 (fixed) and ±999999.0 (scientific), plus the two second-order boundaries the re-derivation exposed — 999999.5 still fixed under the length gate and 999999.9999 scientific through it.
      artifacts: [python/repark/tests/test_display_polars_default.py]
    - id: AT-3
      status: ATTACKED
      evidence: The float algorithm was not guessed: polars 1.43.2's fmt_float source (SCIENTIFIC_BOUND, the Rust Display-length gate, the {:.4e}/{:e}/{:.6} branches) was read and the port validated on 963 live-polars probes with zero mismatches before landing.
      artifacts: [python/repark/src/repark/spark/dataframe/polars_cells.py]
    - id: AT-4
      status: N/A
      justification: Cell spellings are pure functions of Arrow values; no session or environment state enters _polars_float_text or the list threshold.
    - id: AT-5
      status: N/A
      justification: Display-only change; no secret, catalog, or auth surface.
    - id: AT-6
      status: ATTACKED
      evidence: 79-test display trio plus the 110-test display-adjacent suite green with no existing pin weakened; the pre-existing oracle pins (ints/strings/nulls/nests, floats/bools/dates, wide gap frame) still green under the new float speller.
      artifacts: [python/repark/tests/test_display_polars_default.py, python/repark/tests/test_display_styles.py]
    - id: AT-7
      status: N/A
      justification: String formatting cost is per-cell and unchanged in shape; the rewrite deletes branches rather than adding work.
    - id: AT-8
      status: ATTACKED
      evidence: Both oracle pins compare against str(pl.from_arrow(...)) built from the same Arrow table with live polars at run time, so expected bytes are generated, never hand-typed; a polars-version drift would red the pin rather than silently diverge.
      artifacts: [python/repark/tests/test_display_polars_default.py]
    - id: AT-9
      status: N/A
      justification: No log or metric surface changed.
    - id: AT-10
      status: ATTACKED
      evidence: Every clause carries a red-first oracle pin with the failing output pasted; the mutation-sensitive boundaries (threshold > 3, SCIENTIFIC_BOUND) are enforced by byte-identity with live polars, so a one-off drift reds immediately.
      artifacts: [task/ledgers/staging/review-fix-9-ledger.md, python/repark/tests/test_display_polars_default.py]
```
