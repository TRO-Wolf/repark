# Run 24d report — 2026-09-19 day run: the parity campaign, fork side

**Run:** 24d (unit `day-24d`, lane prefix `pd-`). **Window:** 04:52 → 17:30 EDT. **Orchestrator:** claude-opus-5.
**Actors:** Devin SWE-2 (swe-2-high), 22 recorded rounds + 2 stopped (one stopped at start to free a clone, one resume that hung with no output for 50 min). No Opus actor was needed.
**Reviewers:** Grok 4.6 — per PR a Rust perf review, a logic review, and a verification critic with mutation before the merge queue. 28+ runs; see §4 for costs.
**Beside:** 24b (read-perf fork lane) merging into the same fork main through `la-fork-merge.sh`; 24c holds every parity bump; every fork merge is announced in `claims.txt` with the new fork main sha.
**Box:** one GitHub clone `/tmp/pd-fork` plus one `--shared` checkout `/tmp/pd-fork2`; at most two cargo things (an actor round or a building critic each); every local build through `build-slot.sh`; one JVM through `jvm-lock.sh`. No AWS. No Cargo.toml edit in any unit.

## 1. The list — before / after

| # | Unit | Before | After | Fork PR |
|---|---|---|---|---|
| 1 | IPI-52 F-AVRO-NAME-1 | `my col` partition → table unreadable forever; **also found:** Spark-written v2 manifests with sanitised names read the partition as NULL (silent), v3 failed loud, `é`/`列` failed at open | **MERGED** `7bd2fea3`. Java's `AvroSchemaUtil` sanitiser (UTF-16 units, Java letter/digit categories) + `iceberg-field-name`; collisions fail loud like Java's `Duplicate field`; reader binds by restored name; OCF header repair makes the already-broken RePark table READABLE (no global validator, no Cargo change); zero-copy fast path | #308 |
| 2 | F-RPD-TARGET-SMALL-1 (24c's RP-34 blocker) | RPD 8→8 where Spark 0 | **Measured:** the planner rule is Java's exactly (Spark-sized files select 0); the gap is parquet bytes (fork 1,297 B vs Spark 1,590 B; ~218 B are parquet-mr's deprecated min/max stats — parquet-rs has no knob). Declared (Q-24d-1); 24c pinned the cells as a declared layout divergence. Java-visible writer layout fixed: `iceberg.schema` byte-identical (nested key order too), root `table`, `pos` not dictionary-encoded | #307 READY, unmerged (see §2) |
| 3 | IPI-08 F-META-DELETE-1 (fork half) | MoR DELETE covering whole files wrote deletes/DVs | **MERGED** `31e15c73`. `Table::can_delete_using_metadata` = Java `SparkTable.canDeleteUsingMetadata` (bytecode-verified); 72-cell Spark oracle. **Found and fixed on the way (S0):** `StrictMetricsEvaluator` eq/not_eq compared raw decimal mantissas — a strict-metrics decision (incl. the existing `overwrite_by_row_filter`/`delete_from_row_filter`) could drop a file holding `10.00` for `d <> 10`. Also Java's inclusive `uniqueValue` pruning, code-point prefix order, nested fields unprovable | #309 |
| 4a | IPI-09 F-METRICS-CONFIG-1 (fork half) | `write.metadata.metrics.*` ignored; fork compaction rewrote a `none` table WITH bounds | **MERGED** `d09afd3d`. `MetricsConfig::for_table` / `for_position_delete_table` (max-inferred, sorted promotion, list/map descendants, typed error on bad values) wired into every fork writer incl. the DataFusion INSERT/CoW/position-delete writers; 12 Spark cells | #313 |
| 4b | IPI-10 F-LOCATION-1 (fork half) | object storage / partitioned paths / `write.metadata.path` ignored | Java `LocationProviders` (murmur3 hash dirs, Hadoop-Path context incl. `//mytable`), `write.metadata.path` for metadata/manifests/lists in every fork catalog; S3 Tables refuses external paths; Hadoop-convention tables refuse the property like Java; `write.location-provider.impl` refused loud | #314 DRAFT (see §2) |
| 4c | IPI-11 F-RM-DELETES-1 (fork half) | delete manifests immune in `rewrite_manifests` | **MERGED** `ef8e22d8`. Opt-in `rewrite_delete_manifests(true)` with Spark-action semantics, sequence numbers preserved, empty delete manifests dropped | #318 |
| 4d | IPI-13 / IPI-11 merge-on-commit | — | **Triaged RePark-side** (claims 07:3x): RePark writes its own MoR position deletes (file granularity) and uses `fast_append` where Spark's `newAppend()` is MergeAppend (the fork has `merge_append`). P-RPD-* cells predate fork #304 — re-measure first | — |
| 5 | Q-23b-B | CoW DELETE commits `overwrite` | **Triaged RePark-side**: the fork's OverwriteFiles already classifies like Java; RePark's CoW path must be adding a replacement file. Superseded in most shapes by IPI-08's metadata-only DELETE routing | — |
| 6 | F-COMMIT-JITTER-1 | no jitter in commit retries | Java `Tasks.runTaskWithRetry` schedule, bytecode-exact, jitter +[0,10%) | **MERGED** `360bea37` #320 |
| 7 | IPI-28 F-TRANSFORM-ARROW-TYPES-1 | `truncate(1, b)` refused on LargeBinary | **MERGED** `4a0664ad`. truncate/bucket over every Arrow string/binary layout; **found and fixed (P1):** binary NOT STARTS WITH longer than the width would have inclusive-pruned a matching partition; binary starts_with now evaluates in all five evaluators | #315 |
| 7b | RDF sort / zorder (IPI-43), transform sort orders | — | **Not started** (time). Fork has no sort/zorder strategy | — |
| 8 | IPI-45, IPI-22, IPI-30, Q-23a-C | — | **Not started.** Survey: the fork already has `inspect/position_deletes.rs`, `scan/incremental.rs` + `ChangelogScanTask`, `compute_table_stats`, `compute_partition_stats`, `rewrite_table_path` — these are mostly RePark CALL/reader routing (24c). Fork gaps left: `add_files`, container accessors | — |

## 2. Pull requests (fork, TRO-Wolf/iceberg-rust)

| PR | Unit | Actor rounds | Reviews (Grok 4.6) | State |
|---|---|---|---|---|
| #308 | F-AVRO-NAME-1 | Devin r1, r2, r3 (r2 gate-rejected: 2 `///` lines); orchestrator CI fix (typos, ceiling) and removed the co-author trailer lines Devin had added | perf NEEDS_REMEDIATION (2 P1) · logic NEEDS_REMEDIATION (2 P1; first run stalled) · verify **PASS** | **MERGED** `7bd2fea3` 08:46 |
| #309 | F-META-DELETE-1 | Devin r1, r2, r3 | perf NEEDS_REMEDIATION (P1) · logic L-001 **refuted by measurement** (Q-24d-2) · verify NEEDS_REMEDIATION (V-01 S0 decimal scale) · focused re-verify **PASS** | **MERGED** `31e15c73` 11:58 (first attempt CHECKS-RED on a storage-opendal S3 fixture race; re-run) |
| #313 | F-METRICS-CONFIG-1 | Devin r1 (gate-rejected, 1 moved comment), r2, r3 (first r1 stopped at start) | perf P2s · logic **PASS** · verify **PASS** (first run stalled) | **MERGED** `d09afd3d` 13:03 (once NOT-UP-TO-DATE after a 24b merge) |
| #315 | F-TRANSFORM-ARROW-TYPES-1 | Devin r1 (gate-rejected: 35 moved-comment hits), r2, r3 | perf P2s · logic **P1** · verify **PASS** | **MERGED** `4a0664ad` 14:03 (first attempt CHECKS-RED: ledger lacked the ASF header) |
| #318 | F-RM-DELETES-1 | Devin r1, r2 | perf NEEDS_REMEDIATION · logic **PASS** · verify **PASS** | **MERGED** `ef8e22d8` 16:14 |
| #320 | F-COMMIT-JITTER-1 | Devin r1 | logic+perf (combined, small unit) P3 edge cases (Q-24d-3) · verify all behavioural mutants red (Q-24d-4) | **MERGED** `360bea37` 16:43 (first driver gave up during a GitHub API outage; re-queued) |
| #314 | F-LOCATION-1 | Devin r1 (gate-rejected 3 `///`), r2, r3, r4 (resume hung → fresh), r5 | perf LOOKS-GOOD · logic 2 P2 · verify V-001 · re-verify V-002 · final verify **PASS** | **DRAFT, not merged.** Final CI red: `catalog::tests::test_table_commit` (a commit with `SetLocation` now writes the next metadata file under the NEW location's `metadata/`; the pin expects the old one). Java's metastore path is also derived from the new metadata, so the pin likely encodes old fork behaviour. Next run: verify, re-pin or fix, re-run the whole `catalog::` filter, merge. PR comment filed |
| #307 | F-RPD-TARGET-SMALL-1 | Devin r1, r2 | perf LOOKS-GOOD (P3) · logic L-001 P2 fixed · verify **PASS** (Spark `iceberg.schema` byte-equal) | **READY, verified, not merged** — dequeued at 16:45: a rebase + 27-min CI could not finish by 17:30. Next run: rebase, queue, merge |

Comment gate: every pushed head `comment-ban hits=0`; four rounds were rejected by the gate and sent back (#308 r2, #313 r1, #314 r1, #315 r1). I stripped no worker comment myself; my own commits were CI-mechanical (typos, ceilings, a ledger header, a test-file split under the 1,000-line default, conflict union).

## 3. Rulings

- **Q-24d-1** RPD target-small residue is parquet-rs vs parquet-mr bytes; declared, no rule change.
- **Q-24d-2** #309 logic L-001 (UTF-16 string order) refuted by a Spark measurement: Java orders strings by code point = UTF-8 order (`utf16_delete_truth.json`).
- **Q-24d-3** #320 L-001..L-004 are pathological-config P3 residues (overflow at attempt 1025, ≥2e9 ms sleeps, out-of-i32 parsing).
- **Q-24d-4** #320's uncaught M4a is an equivalent mutant.
- Q-23b-B and IPI-13/IPI-11-merge-on-commit routed to 24c as RePark-side (measured by reading the fork).

## 4. Costs

- Grok 4.6: **$20.12** across 31 runs (`/tmp/grok-worker/runs.tsv`, lanes `pd-rv-*`). Three stalls (1-turn runs) re-run fresh; one run refused to start (transient bwrap check), re-run.
- Devin SWE-2: free tier, 22 recorded rounds (`/tmp/devin-worker/runs.tsv`, lanes `pd-fork`, `pd-fork2`).
- No Opus actor rounds.

## 4b. Fork main at the end

`360bea37`. This run's merges: #308, #309, #313, #315, #318, #320 (all tree-equal, all announced in claims.txt). Open: #314 (draft, one pin), #307 (ready, verified). 24b merged #310, #311, #316 into the same main.

## 5. Owner questions

1. **Devin trailers.** Devin added a co-author trailer line to two #308 commits despite the brief; the pre-push hook caught it and I rewrote the messages. *Recommendation:* the devin-worker launcher should strip/refuse those lines at hand-back (f-fix-trailers.sh exists — wire it in).
2. **Flaky fork fixture.** `storage-opendal tests::test_file_io_s3_list_bucket_root` lists `bucket1` while another test writes into it (#309's first CI). *Recommendation:* a tiny fork card giving each test its own prefix.
3. **Fork merge races.** Two fork lanes merging into one main cost two 27-minute CI cycles (NOT-UP-TO-DATE). *Recommendation:* the merge driver rebases a queued fork PR itself when it reaches the head and main moved, then waits for checks.
4. **Java's own NaN hole.** Java's inclusive `uniqueValue` prunes `d <> 5` on a `[5,5]` file whose NaN counts are absent even if it holds NaN rows; the fork now matches Java (parity) — the fork's writers always write NaN counts, so fork-written tables are safe. Declared.

## 6. For 24c (all in claims.txt)

IPI-52 pins at the bump (E-QUOTED-SPACE-COL); route DELETE through `can_delete_using_metadata` on both modes and doors (72 cells; D2: Spark commits an empty `delete` snapshot on no-match); RePark writers call `MetricsConfig::for_table`/`for_position_delete_table` and `TableLocationGenerator::new` (after #314); `rewrite_manifests` CALL turns on `rewrite_delete_manifests(true)` and honours `spec_id`; INSERT via `merge_append`; RePark position-delete writer properties should use the fork's `position_delete_writer_properties_for` (delete-type kv, pos plain); re-measure the 16-way append storm after #320; D-CREATE-PART-TRUNC-BIN.

## 7. Next run (fork side)

`rewrite_data_files` sort then zorder (IPI-43) — likely an Opus-tier design; transform sort orders; `add_files`; container accessors (Q-23a-C); the #314/#307 remainders if not merged (§2).

## 8. STATUS.md / Rust-first

No STATUS.md edit (no fork clause there). Every behaviour change is fork Rust; no Python.

## 9. Disk and lanes

Lanes `/tmp/pd-fork` and `/tmp/pd-fork2` (the `--shared` one): `/tmp/pd-fork2` (49 G) removed at the end; **`/tmp/pd-fork` (44 G) is still on disk** — the harness refused to delete this session's working directory; every branch head in it is on GitHub (its stale local `fix/f-location-1` is superseded by the pushed `47d96a2c`). The orchestrating session can remove it. Every `pd-rv-*` review clone removed after reading. Durable: `/tmp/oc-worker/pd-oracle/` (Spark 4.1.2 recorders + truths: avro_names, meta_delete (72 cells), utf16_delete, metrics_location, binary_transforms, rewrite_manifests, tasks_builder.javap), `/tmp/oc-worker/pd-fork/`, `/tmp/oc-worker/pd-fork2/` (briefs, follow-ups, PR bodies, every reviewer out.json).
