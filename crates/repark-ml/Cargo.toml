# ===========================================================================================
# repark-ml — native tier-1 estimators (M3).
#
# Hand-rolled linear algebra (Cholesky) + streaming fit accumulators. Zero new crates.io
# deps beyond workspace `thiserror` — no ndarray / nalgebra / linfa. Python never trains;
# models hold params only. See docs/design/python-facade.md §4 Q3 (the in-repo ML authority:
# scope, the Rust-rule amendment for multi-pass fit, and this crate's phase-3 scheduling).
# ===========================================================================================

[package]
name = "repark-ml"
description = "Native ML estimators for repark (streaming OLS / IRLS / Lloyd; params-only models)."
edition.workspace = true
version.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
