# The repark-ml crate provides native OLS, IRLS, and Lloyd estimators; models hold parameters only.

[package]
name = "repark-ml"
description = "Native ML estimators for repark (streaming OLS / IRLS / Lloyd; params-only models)."
edition.workspace = true
version.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
