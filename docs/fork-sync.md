# Fork sync — keeping repark and TRO-Wolf/iceberg-rust aligned

RePark consumes the whole `iceberg*` family from the owned fork via `[patch.crates-io]`,
**rev-pinned**: five `rev = "…"` lines in the root [Cargo.toml](../Cargo.toml) that must stay
byte-identical (the **single-writer-per-pin** invariant). This page is the *sync contract* —
the structural decision that the fork stays a sibling repo is
[ADR-0001](adr/0001-own-iceberg-fork.md) and is not restated here.

## The three rules

1. **The pin moves only via its own PR.** `make bump-fork-pin REV=<sha|branch>` rewrites all
   five rev lines + `Cargo.lock` and prints the fork changelog URL for the PR body;
   `make preflight` gates the PR like any other. Never bump the pin as a side effect of an
   unrelated change.
2. **Fork main must be green before it is pinnable.** The fork's own CI decides; a red fork
   main is not a valid `REV`.
3. **Upstream flows through the fork, never directly.** `apache/iceberg-rust` improvements
   merge fork-side first, then reach repark as an ordinary pin bump (rule 1). RePark never
   patches Iceberg semantics locally — engine-agnostic table-format work lives in the fork
   (CLAUDE.md / AGENTS.md invariant).

## Drift visibility

The weekly **`fork-sync-drift`** workflow
([.github/workflows/fork-sync-drift.yml](../.github/workflows/fork-sync-drift.yml); also
manually dispatchable) reports three numbers in its run summary, so nobody has to remember
to check:

| Number | Meaning |
|---|---|
| Fork main ahead of pin | commits on the fork's `main` that repark does not consume yet |
| Upstream ahead of fork | commits on `apache/iceberg-rust` `main` past the fork's merge-base |
| Pin reachability | whether the pinned rev still exists on the fork (force-push guard) |

**Soft thresholds:** fork **>10** commits ahead of the pin → schedule a pin bump; upstream
**>50** ahead of the fork → schedule a fork-side upstream merge in the next planning pass.
**Hard stop:** the pin going unreachable — investigate before any other fork action.

## Pin history

The live pin is the five identical `rev` lines in the root [Cargo.toml](../Cargo.toml).
This table is append-only: one row per dedicated pin-bump PR (rule 1). Started 2026-08-15.

| Date | Old pin | New pin | Notes |
|---|---|---|---|
| 2026-09-12 | `9e3522e314e57cc6b074c9fb8fc1afe43f7c2b46` | `3ebf7d36c2c1fe3664572e23ebc89a698ce2b55c` | RP-19: F-S3ROOT-1 `#281` — a bare-bucket object-store location (`s3://bucket`, every S3 Tables table's location shape) resolves to the bucket root like Java's `S3URI`: the shared `scheme_relative_path` returns the empty key on an exact `{scheme}://{bucket}` host match across the S3, GCS and OSS arms (`s3://bx` / `s3://b-other/…` stay refused; azdls never had the defect), and `list` joins `s3://b` + `key` as `s3://b/k` so `list("s3://b")` and `list("s3://b/")` answer byte-identical locations. Consumer: the `ORPHAN-S3TABLES-1` registry row opens with the parser half of the owner's S3 Tables orphan bug FIXED at this pin — dev-bucket reproduction 2026-09-12: `Invalid s3 url: s3://<id>--table-s3` before, `405 MethodNotAllowed` from `ListObjectsV2` after — while the 405 listing refusal itself stays open for the ORPHAN-S3TABLES-1 card's loud refusal. No riders. |
| 2026-09-14 | `3ebf7d36c2c1fe3664572e23ebc89a698ce2b55c` | `edc38c6aa5cdbe132235f4fefc01d3066d6cff23` | RP-20: F-GLUE-REPLACE-1 `#282` — `GlueCatalog::publish_replace_table` is a version-id-checked `UpdateTable` of the metadata location through the Glue commit transport, after reading the staged metadata back and matching its table uuid; a stale expected base is a retryable `CatalogCommitConflicts` before any send, a lost response a typed `CommitStateUnknown`. `begin_replace` continues the metadata-file version with a fresh uuid. Consumer: gold dbt `table` models rebuild on Glue (C6); `CREATE OR REPLACE TABLE … AS` twice joins the nightly aws-acceptance. No riders. |
| 2026-09-17 | `edc38c6aa5cdbe132235f4fefc01d3066d6cff23` | `75da2b582be2771d9b2a8c38084c2988c4334c49` | RP-21: F-ICE-NAN-PUSHDOWN-1 `#284` — the DataFusion filter conversion never builds a NaN literal: `=` / `<=>` with NaN push `is_nan`, `!=` pushes `not_nan`, `IN` with NaN pushes `is_nan OR in(rest)`, ranges and `NOT IN` with NaN stay unpushed (rating rows V2-26 / V3-14: `WHERE d = CAST('NaN' AS DOUBLE)` returned no rows). F-ICE-HADOOP-VN-1 `#286` — a commit whose next metadata location is a Hadoop `vN(.gz).metadata.json` exclusive-creates the file (local fs temp + `hard_link`, OpenDAL `if_not_exists` where the service has it) and a collision is a retryable `CatalogCommitConflicts`, so a stale memory-catalog writer on an adopted Hadoop table no longer overwrites another writer's version file (rating row V2-20c). Consumers: run 19b units ICE-NAN-PUSHDOWN-1 and ICE-HADOOP-VN-1 (RePark pins and registry rows follow this bump). No riders. |
| 2026-09-17 | `75da2b582be2771d9b2a8c38084c2988c4334c49` | `96fc9f1f12d737156fc24afec0f0923838394cde` | RP-22: F-RDF-OPTIONS-1 `#283` — `RewriteDataFiles` takes Java 1.11's options: one commit for all file groups by default, `partial_progress` with `max-commits` (groups per commit = ceil(groups / max-commits)), `rewrite_all`, `output_spec_id` (grouping keys on the current default spec; the write uses the output spec), `rewrite_job_order`, and `max_concurrent_file_group_rewrites` (validated, run sequentially) (rating row V2-08, claim C-4). F-SORTED-INSERT-1 `#287` — `IcebergTableProvider::insert_into` sorts each writer stream by the table's default sort order (identity and transform keys, direction and null order, NaN placed as Java `Float.compare` does, nested struct children by field id) and stamps `sort_order_id` on every data file (0 when unsorted or unresolvable) (rating row V2-12, claim C-7). F-PROMOTE-READ-1 `#285` — values written before a type promotion read under the promoted type (rating rows V2-10c, V2-06b, V3-11, V3-14). Consumers: run 20c units ICE-RDF-OPTIONS-1 and ICE-SORTED-INSERT-1, run 20a unit ICE-PROMOTE-READ-1 (RePark pins and registry rows follow this bump). No riders. |
| 2026-09-17 | `96fc9f1f12d737156fc24afec0f0923838394cde` | `4151b4884452e504c38871dcbc13b09c3f6378ea` | RP-23: F-EVO-SCAN-1 `#289` — scans and UPDATE/DELETE after ADD/RENAME COLUMN bind the current schema (rating row V2-10e). F-RTAS-OPS-1 `#290` — `StagedTableTransaction::with_replace_write(true)` commits a staged CREATE OR REPLACE through `overwrite_by_row_filter(AlwaysTrue)`: operation `overwrite` with files, `delete` without, no `deleted-*` keys, as Java's RTAS (rating row V2-24); the empty-commit permission is an opt-in only the staged replace sets. F-TARGET-FILE-SIZE-1 `#288` — `RollingFileWriter` rolls on 1000-row slices when flushed + in-progress bytes reach the target (Java 1.11.0 `ROWS_DIVISOR` / `ParquetWriter.length()`), the DataFusion write engine writes dictionary-OFF like Spark's files, and every catalog's create stamps `write.parquet.compression-codec = zstd` (rating rows V2-15, V3-COV-7). Consumers: run 20a ICE-EVO-DML-1; run 20c ICE-RTAS-BYNAME-1 (RTAS opt-in), ICE-SORTED-INSERT-1 / ICE-TARGET-FILE-SIZE-1 pins; ICE-RTAS-OPS-2 (2026-09-18) took the RTAS opt-in. |
| 2026-09-17 | `4151b4884452e504c38871dcbc13b09c3f6378ea` | `8fb44a39666523a4a4ac3a4421228adcd102ffd4` | RP-24: F-OCC-SCOPED-1 `#291` — serializable conflict validation is filter-scoped: each concurrently added data file and each concurrently added delete file is tested against the operation's conflict-detection filter by its own spec's partition projection first, then by inclusive metrics (Java `ManifestGroup.filterData` / `MergingSnapshotProducer.validateAddedDataFiles`), so a concurrent commit in a disjoint partition rebases and retries instead of aborting the commit (rating row V2-20a, residue DML-5). The `AlwaysTrue` default filter and snapshot isolation are unchanged. Consumers: run 21a unit ICE-OCC-SCOPED-1 (the RePark half passes each MERGE/UPDATE/DELETE target filter into `conflict_detection_filter`). No riders. |
| 2026-09-18 | `8fb44a39666523a4a4ac3a4421228adcd102ffd4` | `64705c99b0bd5558fd28e273c8f05e615c535262` | RP-25: F-NESTED-EVO-1 `#292` — the Arrow reader projects nested struct, list and map children by `PARQUET:field_id` (never name or position): a child added after a file was written fills from its `initial-default` (every primitive, Time/Uuid/Binary/Fixed included) or NULL, a required child with no default is refused `Missing required field`, map keys project by id, List/LargeList cross-variant, identical nested columns pass through untouched, the projection plan is rebuilt when a later batch's schema differs, and a struct that mixes stamped and unstamped nested ids where a missing child shares an unstamped name fails loud (rating row V2-10d). Consumer: run 21a unit ICE-NESTED-EVO-1 (RePark half — adoption reads and nested DDL). No riders. |
| 2026-09-18 | `64705c99b0bd5558fd28e273c8f05e615c535262` | `8477b249587f26a796f204feee904f2fbb5e1d8b` | RP-26: F-HADOOP-VN-REPLACE-1 `#293` — a staged CREATE OR REPLACE on a Hadoop `vN` base stages `v(N+1)` and creates it once at commit under exclusive create, so a stale replace fails with retryable `CatalogCommitConflicts`, the winner's bytes stay intact and no uuid file is left. F-CHERRYPICK-WAP-ORDER-1 `#293` — a duplicate WAP cherry-pick is validated first and refuses with Java's text `Duplicate request to cherry pick wap id that was published already: <wap-id>`. F-UPDATE-SCHEMA-SAME-1 `#293` — a schema update that changes nothing commits nothing (no metadata file, no schema id). `rollback_to_time` on a snapshotless table carries Java's message; one cherry-pick plan per validate→commit (perf only). Consumers: ICE-HADOOP-VN-1 R-001, ICE-BRANCH-OPS-1 R-001, ICE-COLUMN-REORDER-1 R-001 (RePark pins and registry rows follow this bump). No riders. |
| 2026-09-18 | `8477b249587f26a796f204feee904f2fbb5e1d8b` | `fdaa82d454d2014386328da9ba6a23fce467c0cf` | RP-27: F-OCC-EXEC-1 `#294` — the DataFusion DELETE/UPDATE execs scope serializable conflict validation by their own scan prune predicate (Java's `SparkPositionDeltaWrite` / `SparkCopyOnWriteOperation` rule), so disjoint-partition plain-WHERE UPDATE statements commit concurrently; the prune conversion drops every conjunct that could be narrower than the exact WHERE (inexact literal conversion, non-injective casts, float AboveMax/BelowMin sentinels, signed zero, `!= NaN`), closing a pre-existing silent DELETE/UPDATE miss. |
| 2026-09-12 | `41e25ba2b99a0d90caf4317b4236f5e1d4b683f5` | `9e3522e314e57cc6b074c9fb8fc1afe43f7c2b46` | RP-18: F-REWRITE-SIZE-1 step 2 `#280` — the maintenance rewrite disables the dictionary per column from the input files' footers (dead dictionary pages were the measured 1.47× cause of the S2-24 rewrite inflation) and an unset `write.parquet.compression-level` now means zstd 3 like Java. Consumer: `AP-1-R-001` — with AP-3 (`#526`, the projection multiplies the uncompressed footer sum by `byte_ratio`) and this bump both landed, the projection and the live rewrite sit under one codec with compression counted once; the fourth 20 % check decides the residue ([perf/adapt-part-ap1-remeasure-3-2026-09-12.md](perf/adapt-part-ap1-remeasure-3-2026-09-12.md)). No riders. |
| 2026-09-12 | `090bc82142c09e0afd8c0ddaf594ab5843e1e77d` | `41e25ba2b99a0d90caf4317b4236f5e1d4b683f5` | RP-17: F-WRITE-COMPRESS-2 `#278` — the four writer sites `#276` left at parquet-rs's UNCOMPRESSED default (residue F-WRITE-COMPRESS-1-R-001…R-004: the maintenance rewrite writer, the COW/MoR rewrite writers, the position-delete writers) now honour `write.parquet.compression-codec` / `write.parquet.compression-level` like the INSERT path, default zstd. Consumer: `AP-1-R-001` — run 8's re-measure recorded `rewrite_data_files` inflating a 3 074 844-byte zstd bed to 7 928 680 bytes of UNCOMPRESSED live files, which polluted the 20 % check; with compaction compressing, the projection and the actual are measured under one codec in the third re-measure ([perf/adapt-part-ap1-remeasure-2-2026-09-12.md](perf/adapt-part-ap1-remeasure-2-2026-09-12.md)). No riders. |
| 2026-09-11 | `85db42f285703682629b3b53bd1ebcd3091c6bc5` | `090bc82142c09e0afd8c0ddaf594ab5843e1e77d` | RP-16: F-WRITE-COMPRESS-1 `#276` — `IcebergWriteExec` built its data-file writer with `WriterProperties::default()`, so every `INSERT INTO` file was UNCOMPRESSED and ignored `write.parquet.compression-codec` (Iceberg default zstd) and `write.parquet.compression-level`; the fix parses both properties (`parquet_compression_from_properties`, case-insensitive, unknown codec or bad level `DataInvalid`) and the INSERT path sets the codec. Consumer: `AP-1-R-001` — the footer-measured `byte_ratio` read 1.000000 on INSERT-grown beds. Riders on this bump: `#273` F-INSERT-DIST-1 (INSERT declares hash distribution on evaluated partition values), `#274` F-CATIO-WEIGHT (the shared manifest `ObjectCache` charges retained object graphs), `#275` F-HMS and `#277` F-MINIO-QUAY (fixture images: the Hive metastore JARs are checksum-pinned, and the MinIO images move to quay.io after Docker Hub dropped `minio/minio` and `minio/mc`). Four other writer sites stay uncompressed as fork residue F-WRITE-COMPRESS-1-R-001…R-004. |
| 2026-09-06 | `8bc325a33a8bc92dc4fb8596424772f6be3fa492` | `85db42f285703682629b3b53bd1ebcd3091c6bc5` | RP-15: F-RDF-EVO-1 `#272` (`rewrite_data_files` re-points every compaction read task at the current schema with the full current projection, so old-file batches evolve — NULL-fill added, omit dropped, rename by field id, legal promotions — before the partition calculator and writer; manifest partition summaries widen carried tuples int→long / float→double via `PrimitiveLiteral::promote_to`). Consumer: `RDF-SCHEMA-EVO-1` FIXED — the owner's S3 Tables refusal after `ADD COLUMN` + `ADD PARTITION FIELD`. Fork critic PASS: 10/10 pins, 7 red under the batch revert, 12 attack shapes with no lost value, `cargo test -p iceberg` 3652 passed. |
| 2026-09-06 | `2ed39cb0aebb8388801b588c390ab99ad9656327` | `8bc325a33a8bc92dc4fb8596424772f6be3fa492` | RP-14: F-27 `#271` (a: an empty projection reads no column bytes, only row counts — delete vectors still filter; b: `IcebergTableScan::statistics()` reports exact row counts when the snapshot has no delete files, no residual and no limit, so `count(*)` folds; d: a session-derived split target so small tables scan in parallel; the LIKE-pushdown goldens read `N=2`/`N=3` at the harness-pinned `target_partitions(4)`). Un-skips the PERF-ICE-SCAN-1 pins (`PERF-ICE-COUNTSTAR-1`, `PERF-ICE-SCANPART-1` → FIXED) and re-closes that ledger's C-006. |
| 2026-09-05 | `791196435b3552cbb4199fb68e929d3ca49a4ac0` | `2ed39cb0aebb8388801b588c390ab99ad9656327` | RP-13: F-28 `#269` (the partition splitter groups a batch with Arrow kernels, one partition literal per group; 14/14 result types property-tested), F-CATIO-KEY `#270` (the shared manifest cache stores the raw parse and applies list-entry context per caller — closes the lineage poisoning PERF-ICE-CATALOG-IO-2 halted on; the `PERF-CATALOG-LINEAGE-CACHE-1` detector pins flip in the default-ON follow-up). F-27 (count-star projection) is still in flight on ICE-SCAN and rides RP-14. |
| 2026-09-05 | `189a73ed86c9bd29888fbd545f7957df8df25f18` | `791196435b3552cbb4199fb68e929d3ca49a4ac0` | RP-12: F-26 `#267` (the fork's in-tree MoR v3 DELETE/UPDATE hands `known_partitions` to the DV container close), F-CATIO `#268` (one table load per planning round; `TableBuilder::object_cache` + `MemoryCatalogBuilder::with_shared_object_cache_bytes`, default off). The PERF-ICE-CATALOG-IO-1 part-3 pin stays skipped until RePark wires the shared cache (follow-up unit); the Glue / S3 Tables part-2 legs stay skipped (F-CATIO-AWS not yet filed on the fork). |
| 2026-09-04 | `85a4aaf0cda9ea643bfe34c1666228178e363e94` | `189a73ed86c9bd29888fbd545f7957df8df25f18` | RP-11: F-24 `#266`. The v3 parquet-to-DV rewrite honours Spark's `min-input-files=5`; groups of 1 or 2 stay parquet and return four zeros. `rewrite-all=true` bypasses the floor (CALL still refuses `options`). Family frozen. Take/skip in the RP-11 ledger. |
| 2026-09-04 | `594bdbe5f257455d77ac49f1a2d50794a1aea6fd` | `85a4aaf0cda9ea643bfe34c1666228178e363e94` | RP-10: F-25. `validate_fresh_dvs_only` walks data manifests newest-first and stops once every `added_dvs` key is found (buffer 1 until the first manifest is consumed); full walk only for an oldest-manifest or never-found key. Family frozen. Take/skip in the RP-10 ledger. |
| 2026-09-03 | `c1d6c9de1498cf04765893ef3f698d915766a6a7` | `594bdbe5f257455d77ac49f1a2d50794a1aea6fd` | RP-9: F-23. The DV close skips the data-manifest walk when there are no legacy deletes and `known_partitions` covers every touched path; otherwise it walks until every wanted path is found (first manifest loaded alone). `data_sequence_numbers` is total with legacy deletes, otherwise only the paths the map missed (may be empty). Family frozen. Take/skip in the RP-9 ledger. |
| 2026-09-03 | `ff4764d3eba037ecfa185be5de5f639cbffef80b` | `c1d6c9de1498cf04765893ef3f698d915766a6a7` | RP-8: `#261` F-19/F-20 (files-exist narrowed to the replacement blobs, the sibling rewrite deleted, `FanoutWriter::close` drains ascending), `#262` F-21 (the DataFusion delete exec merges legacy parquet position deletes; `validate_fresh_dvs_only` now blocks only file-scoped), `#263` F-22 (one delete-manifest pass returns `legacy_deletes` + `data_sequence_numbers`; `load_legacy_positions_by_path` is one projected read per delete file; the close takes an optional pre-loaded `ManifestList`). BREAKING: `DvContainerClose::retained_references` and `StampedDeleteFile` are gone. Family frozen. Take/skip in the RP-8 ledger. |
| 2026-09-02 | `fb0cacfa8ceda87f865fb0ae53be4b46e0ef8b7a` | `ff4764d3eba037ecfa185be5de5f639cbffef80b` | RP-7: `#260` F-18 deletion-vector container close — only the touched blob is rewritten, the sibling entry stays byte-identical in its old container, removal is keyed by Java's `DeleteFileSet` triple, and the data-file walk is lazy (`close_touched_dv_containers_with_partitions`). Family frozen. Take/skip in the RP-7 ledger. |
| 2026-09-01 | `00cdde00685bbc94552b29fcf8ed6767fe051ce6` | `fb0cacfa8ceda87f865fb0ae53be4b46e0ef8b7a` | RP-6: `#253` PR-1 REPLACE added>deleted refuse, `#254` PR-2 evolved-spec rewrite, `#252` PR-5A Glue/S3 Tables commit seams, `#255` PR-3 V3 MoR UPDATE lineage, `#256` PR-6B branch MoR UPDATE, `#257` PR-4 V2→V3 upgrade, `#250`/`#251` docs+PR-6A. Family frozen. Take/skip in the RP-6 ledger. |
| 2026-09-01 | `33be9a0f411c37cd8d7b38c4db81eec30c1344cc` | `00cdde00685bbc94552b29fcf8ed6767fe051ce6` | RP-5: `#245` F-6b `with_commit_branch`, `#246` R91 unknown-on-write, `#247` F-8 metadata projection/listing, `#248` F-16r delete-ratio bounds-only, `#249` F-6c branch-head scans. Family frozen. Take/skip in the RP-5 ledger. |
| 2026-08-31 | `d408da42fb91db2010662fe1da3783b82fa6e1ed` | `33be9a0f411c37cd8d7b38c4db81eec30c1344cc` | RP-4: `#241` test pin, `#243` F-7 slice 1 (v3 rewrite lineage), `#244` F-6 `to_branch` (carried). Family frozen. Take/skip in the RP-4 ledger. |
| 2026-08-30 | `ce92a7bfe2c1be569ed0de1178ed410e8ec3a117` | `d408da42fb91db2010662fe1da3783b82fa6e1ed` | RP-3: F-17 `#237` (DV container close), F-14 `#235` (Hadoop `vN` pointer math), F-7 U3 `#227`, F-16 `#232`, F-9/F-15 `#233`, H7-P1/R114 `#239`. Family frozen. Take/skip in the RP-3 ledger. |
| 2026-08-27 | `5e7b2e4f8fcb0ff65943cdbc10cdd8f4132fe0b6` | `ce92a7bfe2c1be569ed0de1178ed410e8ec3a117` | RP-2: consume fork F-3 `#216` (remove-dangling-deletes compose), F-5 `#217` (ReplacePartitions scope, BEHAVIOR), F-13 `#219`/`#221`/`#222` (V3 MOR DV writes, BREAKING), F-7 U1+U2 `#225`/`#226` (lineage, BEHAVIOR). Family frozen; the fork's variant type adds `parquet-variant*` 58.4.0. |
| 2026-08-23 | `0c5fd58d4ab73a0113a8b28b717cf5d002b0f8f2` | `5e7b2e4f8fcb0ff65943cdbc10cdd8f4132fe0b6` | RP-1: consume fork F-0 `#214`, F-1 (RPDF floor 5), F-2 `#215`. Family frozen. F-8a last-`$` filter. |
| 2026-08-15 | `b009ac158f7584a956fa9292c0e9675a411ecf0d` | `0c5fd58d4ab73a0113a8b28b717cf5d002b0f8f2` | AD-1 via `make bump-fork-pin` + `#182`/`#183` adapter. HALTED predecessor: repark `#102` (pin `1dae9b66`, never merged). `#186` is not a rider. |

Riders on the 2026-08-15 row (FW-0's nine main-side commits plus `#192`–`#195`, content-present on the target):

1. `TRO-Wolf/iceberg-rust#182 Feat/slate 2026 07 31 combined`
2. `TRO-Wolf/iceberg-rust#183 Feat/fk mor perf campaign`
3. `TRO-Wolf/iceberg-rust#184 Fix/qb posdelete bounds and partition stamp`
4. `TRO-Wolf/iceberg-rust#185 docs(task): DF 52→54 churn map (V0 recon)`
5. `TRO-Wolf/iceberg-rust#187 Chore/df54 family bump recut`
6. `TRO-Wolf/iceberg-rust#188 docs(ledger): #187 catch-up — queue reconciliation + 3 lessons`
7. `TRO-Wolf/iceberg-rust#189 docs(datafusion): H7-S2 review round 2 — peak-memory doc clause + ledger corrections`
8. `TRO-Wolf/iceberg-rust#190 U3 / hazard-1: Java MIDPOINT row-group selection over ranged splits`
9. `TRO-Wolf/iceberg-rust#191 2026-08 audit hardening — decimal parity, bounded recursion, typed metadata failures, namespaces, cache bytes, secret redaction`
10. `TRO-Wolf/iceberg-rust#192 inspect: project timestamptz/timestamptz_ns in data_file metadata tables`
11. `TRO-Wolf/iceberg-rust#193 Emit Arrow timestamptz as tz=UTC (Spark toArrow)`
12. `TRO-Wolf/iceberg-rust#194 inspect: drop empty partition column; tolerate $ in table names`
13. `TRO-Wolf/iceberg-rust#195 scope IcebergCatalogProvider namespace walk`

Equivalence (FW-0, copy-true): pin `b009ac15` (18-commit DF54 side branch) is content-equivalent on `1dae9b66` via `#182` (scan/`plan_tasks`/`with_file_prune_only`) + `#187` (DF54 family + `PageIndexPolicy` + nested-insert re-pin); six `docs(task)` tip-stamps are SHA-unique and not product content. This bump then carries `#192`–`#195` on top of that equivalent main.

## Open fork asks

Table-format behaviour RePark measured against Spark and cannot fix locally (rule 3): each ask is
named here with its measurement, and lands as a fork PR that reaches RePark through an ordinary
pin bump (rule 1). An ask leaves this list when its PR is named in a pin-history row above.

| Ask | Fork seam | Measured Spark answer vs the fork's | Filed by |
|---|---|---|---|
| `F-RDF-SORT-STAMP-1` | `crates/iceberg/src/maintenance/rewrite_data_files_write.rs` builds `DataFileWriterBuilder::new(rolling_builder).with_partition_spec(spec)` — no default-order sort, no `with_sort_order_id` | On a table with `WRITE ORDERED BY (id)`, Spark 4.1.2's `rewrite_data_files` binpack re-sorts each output file by the table's default order and stamps it: 6 sorted inputs compact to 2 files, `sort_order_id = 1`, id ascending within each (heads `[0, 1, 2, 6, 7, 8]` and `[31, 32, 33, 37, 38, 39]`; cell `binpack_after`, 2026-09-17). The fork's compacted files are unsorted with `sort_order_id` NULL. | ICE-SORTED-INSERT-1 round 3 (registry `WRITE-ORDER-RDF-1`) |
| `F-COW-UPDATE-STAMP-1` | `crates/integrations/datafusion/src/physical_plan/delete.rs::copy_on_write_update` (reached from `IcebergUpdateExec`, the provider's `update`) writes the rewritten files with neither `write_sort_plan` nor `with_sort_order_id` | On the same table Spark's COW `UPDATE t SET id = id WHERE p = 0` rewrites sorted files stamped `sort_order_id = 1` (cell `v3_partitioned_update` / `v2_partitioned_update`, 2026-09-17). The fork's rewrite stamps NULL; its output is ordered only because the rewrite preserves the input files' row order, so an unsorted input stays unsorted. `UPDATE t SET id = id WHERE p = 0` reaches this exec because it misses both of RePark's owned UPDATE gates: `predicate_dml::try_allowed_update_in` accepts scalar-expression assignments (`scalar_set_assignments`) but only an uncorrelated `col IN (SELECT …)` WHERE, and `p = 0` is not that shape; `predicate_dml::plain::try_allowed_plain_identity` handles DELETE only. The assignment is not the gate: `SET id = 42 WHERE p = 0` lands here too. | ICE-SORTED-INSERT-1 round 3 (registry `WRITE-ORDER-COW-UPDATE-1`) |

## Debug

First checks: dispatch `fork-sync-drift` manually and read the run summary; locally,
`grep -oE 'rev = "[0-9a-f]{40}"' Cargo.toml | sort -u` must print exactly one line.
Escalate to: [map.md](map.md#debug).
