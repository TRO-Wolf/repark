# PERF-FACADE-CDF-1 — `createDataFrame(list of tuples)` stops normalizing every cell in Python five times (Muse Spark 1.3, standard pattern; perf analysis slate item 3, candidate 2)

Lane `$HOME/repark-lanes/lanes/oc-cdf1`, branch `perf/facade-cdf-1`, base `origin/main` (current, ≥ e8344e8e which carries PERF-FACADE-1 and its tracked runner). Wait until `~/repark-lanes/briefs/mklane-cdf1.log` contains `venv-ok`. You are the Actor of a SEPMO unit. Read `AGENTS.md`, `.agents/skills/engineering-method/SKILL.md`, `docs/testing.md`, `.agents/skills/sepmo/unit-runbook.md`, `live-cell-rules.md` (copy from ~/repark-lanes/briefs/live-cell-rules.md if absent), `docs/perf/map.md`, `docs/perf/facade-boundary-baseline.md` and the tracked runner `python/repark-parity/bench/facade/` (`make facade-bench`; its `createDataFrame` cells are your fixture and your before/after instrument — extend it rather than writing scratch probes), the analysis `docs/perf/engine-iceberg-analysis-2026-09-04.md` §2 row 2 (`session/create_dataframe_rows.py:498 _create_dataframe_from_rows_inner` → `create_dataframe_values.py:165 _normalize_create_dataframe_cell` 2.14 s cumulative → `create_dataframe_inference.py:428 _prepare_nested_cell` 1.22 s; `create_dataframe_tuples.py:107 _refuse_long_double_merge`), §5 item 3, §7.3, then the five `create_dataframe_*.py` modules under `python/repark/src/repark/spark/session/`, their `map.md`, and the createDataFrame parity suites (`python/repark/tests/test_create_dataframe*.py`, the VALUES-path rules, the TY-*/DEC-* rows the inference honours).

## Rules (non-negotiable)
No code comments anywhere (Python `#` beyond `# noqa`, docstrings beyond the forced one-liner, TOML/YAML/shell `#`). Never delete a pre-existing comment. Reasons in the touched `map.md`s (`pins: <unit>/C-NNN`) and the ledger. Self-check before every commit: `git diff --cached -- '*.py' '*.toml' | grep -nE '^\+.*#' | grep -v noqa` prints nothing. Identity `git -c user.name="TRO-Wolf" -c user.email=64240326+TRO-Wolf@users.noreply.github.com`; the LAST line of every commit message is exactly `Authored-By: Muse Spark (muse-spark-1.3) <noreply@meta.ai>`; no other trailer. Never push, never `gh`, never `aws`, never `--no-verify`, never touch `.github/`, never change dependencies. Maps in lockstep; no amends. Size ceilings only ratchet down: every ratchet in `scripts/check_lib_py.py` is mirrored in `python/repark-parity/tests/test_cap_1_source_file_line_cap.py` in the same commit, and a file that would grow gets a new module instead. NO cargo build is needed (this is facade-side Python); the lane venv resolves `repark` to a sibling checkout at the same main — prove `repark.__file__` and use `PYTHONPATH=$HOME/repark-lanes/lanes/oc-cdf1/python/repark/src` (or repoint the venv's `.pth` to the lane) so the FACADE under test is the lane's; numbers still need a release native — the sibling's is release (`__debug_assertions__ False`; check) — and a quiet box (record load, re-measure the floor). Spark JVM: wait up to 15 min for an empty box, then run beside at most one other JVM; ivy redirect `cp -a ~/.ivy2.5.2 <lane>/.ivy2` + `PYSPARK_SUBMIT_ARGS`. Kill any background pytest/JVM you started before hand-back. The API freeze binds: no public name changes; `createDataFrame` keeps every documented signature.

## The measured problem
`createDataFrame(list of tuples)` at 1e5 rows costs ~1,717 ms (analysis) / ~803 ms (facade runner, `create/100000/tuples_count`) against `createDataFrame(pandas)` far lower, because inference normalizes every cell in Python across five passes (`_normalize_create_dataframe_cell`, `_prepare_nested_cell`, the long/double merge refusal walk, the per-row schema check, the Arrow conversion). Deliverable: column-wise inference and conversion — one pass that types each column from its values (Spark's merge rules: long/double refusal, int widths, decimal precision/scale, string/bytes, date/timestamp with tz, bool, None) and builds Arrow arrays column by column (`pyarrow.array` with the resolved type, or the existing per-cell normalizer only for nested columns: struct/array/map/Row cells), preserving every current answer: the VALUES-path rules, the refusal messages and their exact text, `verifySchema`, explicit-schema paths (`schema=StructType`, DDL string, name list), `Row` inputs, dict inputs, mixed None columns, empty input, the TY-*/DEC-* pinned inference answers. Target: `create/100000/tuples_count` ≤ 100 ms (analysis' target) — report what you reach and, if the target is missed, the isolated cost that remains and where it sits, honestly.

## Method
Baseline first with `make facade-bench --cells create/*` (or the runner's flag form) on the unchanged lane; then after each step; medians, spread, floor, × floor, load; extend the runner with an `old-vs-new` swap for the createDataFrame path the way it already does for collect (so the baseline document's before/after comes from one process), and add the nested-column and explicit-schema cells so the fixed path and the delegated path are both measured.

## Red-first
`python/repark/tests/test_perf_facade_cdf_1.py` (≤ 1000 lines): equality pins of the new path against the old path (keep the old inference callable under a private name for the pin, as PERF-FACADE-1 did for the converter) on a wide value matrix — every scalar Python type, None in every column and whole-None columns, mixed int/float (the long/double refusal), ints beyond int64, Decimal at several scales, str/bytes/bytearray, date/datetime naive and aware, bool, nested tuple/list/dict/Row cells, ragged rows (refusal), 0 rows, 1 row, 1e4 rows — asserting type-and-value equality of the resulting DataFrame's schema and `collect()`, and the exact refusal messages; plus a live leg comparing schema and rows against PySpark 4.1.2 `createDataFrame` for the scalar matrix. Red under a deliberately wrong inference (e.g. ints typed as double) before the implementation; mutation score after (drop the long/double refusal → which pins red; wrong decimal scale → which; skip the None mask → which; treat a nested column as scalar → which).

## Ledger and docs
`task/ledgers/staging/perf-facade-cdf-1-ledger.md` (house form, `Model: muse-spark-1.3`, `risk_tier: standard`), `task/ledgers/staging/map.md` row, registry row `PERF-FACADE-CDF-1` filed as a measured perf row with before/after and status FIXED (form: `PERF-FACADE-COLLECT-1`), the createDataFrame section of `docs/perf/facade-boundary-baseline.md` re-measured with the runner, the session `map.md` design note with the pins line, `python/repark/tests/map.md` row. `STATUS.md` and `briefs/next-sequence.md` untouched.

## Gates before hand-back
`make ci && make check-python-conventions && PYTHONPATH=$HOME/repark-lanes/lanes/oc-cdf1/python/repark/src .venv/bin/python -m pytest python/repark/tests -q --timeout 900 -x && .venv/bin/python -m pytest python/repark-parity/tests -q && REPARK_PARITY_LIVE=1 PYTHONPATH=$HOME/repark-lanes/lanes/oc-cdf1/python/repark/src .venv/bin/python -m pytest python/repark/tests/test_parity_live.py python/repark/tests/test_perf_facade_cdf_1.py -q && make check-map-sync && make check-ledger-grammar && make check-ledgers && make check-docs-compaction && python3 scripts/ledger_lifecycle.py check --base origin/main && typos .`
Hand back `handback.json` at the lane root: `{"status":"done|halt","commits":[...],"gates":"...","numbers":{"tuples_1e5_before_ms":…,"tuples_1e5_after_ms":…,"pandas_1e5_ms":…,"nested_1e4_before_ms":…,"nested_1e4_after_ms":…,"floors":…},"notes":"...","questions":[...]}` and end with the before/after table and the mutation score. HALT with evidence if a Spark inference rule cannot be reproduced column-wise without changing a pinned answer.

## Durability (2026-09-05, after a power outage wiped every /tmp lane)
Lanes now live under `$HOME/repark-lanes/lanes/`, not `/tmp`. Commit early and often so a crash loses minutes, not hours. Never push yourself.
