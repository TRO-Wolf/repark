# map — repark/spark/session/

CC-2 closing-critic remediation: review-round label narration swept from prose; safety and
accuracy contracts restored in condensed form (see the unit ledger's findings dispositions).
CC-2 close: restored base function docstrings hashed by `test_moved_symbol_bodies_match_the_integrated_baseline`.

## Purpose

Package split of the former monolithic `session.py`. One responsibility per module; `_funcs.py`
is the compatibility router that re-exports every prior name.

## Contents

| Path | Role |
|---|---|
| `_coerce.py` | `range_bound_as_int`, `sql_clause_end_after` — lifted so `session_core.py` can ratchet its exact `check_lib_py` exception baseline. |
| `_funcs.py` | Compatibility router: imports each responsibility module, binds cross-module edges, re-exports every prior name. **ICE-VIEWS-1 (2026-09-20):** re-exports `_CREATE_TEMP_VIEW_SQL_RE`. |
| `session_configuration.py` | SQLConf defaults, DataFusion validation/forwarding, display-style normalization. `_SQLCONF_DEFAULTS` holds declared inference, session-timezone, ANSI (`spark.sql.ansi.enabled` → `true`, the Spark 4 default; `RESET`/`unset` fall back here, applied), and timestamp-type defaults. **ICE-RTAS-BYNAME-1 round 2 (2026-09-17):** default `spark.sql.caseSensitive → false`. pins: ice-rtas-byname-1/C-010. Runtime temp-directory `SET` stays refused (the Rust builder already applied it). DataFusion `SET` values route through the shared Spark string-literal helper. **DISPLAY-POLARS-1 step 1 (2026-09-09):** default flips to `polars` via new `default_display_style()` (env `REPARK_DISPLAY_STYLE` first, refuse-loud through `normalize_display_style`; builder config and `session.display_style` keep outranking both), re-exported through `_funcs.py` beside its two siblings. pins: display-polars-1/C-001. **Step 4:** `_DISPLAY_INT_DEFAULTS` (max_rows 10, max_cols 8, str_len 30 — str_len measured against polars 1.43.2, see D-12 in the unit ledger) plus `_normalize_display_int` (bool refuses, positive ints and digit strings only), `_builder_display_int` (last case-insensitive builder hit wins), `_display_token_key`, `_sync_display_int_into_builder_config`, `_canonicalize_display_key` (builder-write canonicalization with int validation), and `_reuse_display_ints` (validate plus token/snapshot/store/tomb apply). Only the two names `session_core.py` calls ride `_funcs.py`; the rest stay module-private, so the production-file-size symbol tables do not move. pins: display-polars-1/C-005 **EAGER-BUDGET-1 step 1 (2026-09-13):** `_RETAINED_CACHE_BYTES_KEY` (`repark.cache.retained_bytes`), `_retained_cache_bytes_value` (the live native readback) and `_refuse_read_only_conf_key` (the `INVALID_CONF_VALUE.REQUIREMENT` read-only refusal) live here for `builder_conf.RuntimeConfig` to dispatch on. pins: eager-budget-1/C-003 **CFG-1 step 3 (2026-09-09):** `fold_config_file_into_builder` (forced-or-discovered pairs through `Builder.config`, absent-keys-only) plus the SAF-006 knob resolvers moved here from `session_core.Builder` as config-dict functions. pins: cfg-1/C-026, C-027 Merge hygiene (2026-09-09): CFG-1 step 3 and DISPLAY-POLARS-1 step 4 both appended to this module on separate branches; the merge keeps both blocks (no name collisions) and `ruff format` owns the spacing between them. **REVIEW-FIX-14 (2026-09-10):** `_normalize_display_int` refuses `repark.display.max_rows` above `_DISPLAY_MAX_ROWS_CEILING` (10_000, refuse-loud) so the styled probe's `limit(max_rows + 1)` is bounded (Q-51). pins: review-fix-14/C-002 **ICE-DYN-OVERWRITE-1 (2026-09-17):** `PARTITION_OVERWRITE_MODE_KEY` (the one spelling, used as the `_SQLCONF_DEFAULTS` key). pins: ice-dyn-overwrite-1/C-007. **ICE-WAP-BRANCH-1 (2026-09-19):** `WAP_BRANCH_KEY` / `WAP_ID_KEY` / `WAP_SESSION_KEYS` — the two spellings `builder_conf` forwards. They carry no `_SQLCONF_DEFAULTS` entry: unset means unset, and Spark answers no default for either. pins: ice-wap-branch-1/C-007. **ICE-SESSION-WRITE-CONF-1 (2026-09-19):** the three `ICEBERG_SESSION_*` key spellings plus `is_iceberg_session_write_key` (membership tuple plus prefix, the forward predicate `builder_conf` dispatches on). |
| `catalog_resolution.py` | Catalog selection, aliasing, namespace defaults, table-name resolution; relation-parser edge bound by the router to avoid a cycle. **MAINT-POLICY-1 audit fix (2026-09-10):** also owns `_temp_view_home_ref` (moved byte-identical out of `session_core.py` to pay for the `run_maintenance` class-body declaration; `session_core` imports it back). |
| `session_state.py` | Active-session context state and the drop-in warning lifecycle. |
| `reader_support.py` | Reader option sets and integer bounds, JDBC integer options, reader paths, JSON empty-input checks. **CSV-INFER-PERF-1:** inferSchema without `nullValue` keeps DataFusion's 1000-row sample, Utf8-re-reads every typed column, then one `try_cast` aggregation widens or keeps so a conflict past row 1000 cannot raise or silently truncate (offsets use try_cast timestamp on the raw text; dates with a clock become timestamp; invalid calendar dates stay string). Native-Utf8 leftover numeric grammar (`Inf`/`NaN`/`+5`/long digits) runs for every Utf8 column in the same `try_cast` aggregation, at every file width. A `multiLine` first read is already all-Utf8, so promotion uses the full bigint/double/boolean/timestamp/date candidate list; boolean accepts only `true`/`false` (case-insensitive, Spark `CSVInferSchema`), so `1`/`t`/`yes`/` true` stay string. `nullValue` still Utf8-forces and promotes bigint/double/boolean/timestamp/date via one `try_cast` failure-count aggregation (timestamp only when a cell has `:`, date only when it does not). `utf8_columns` is injected only on that re-read, never taken from the user option bag. The last line of `_promote_csv_string_types`'s docstring is the pre-existing trial-materialize sentence; this row is the one-agg fact. JSON also runs the leftover ntz recast; DataFusion infers Utf8 so it is a no-op. **TORTURE-1 step 3 (D-4):** `flag_secret_columns`/`flagsecretcolumns` ride `_CSV_NATIVE_OPTION_KEYS`/`_JSON_NATIVE_OPTION_KEYS` into the engine's option map (csv + json only — `_CSV_JSON_ONLY_OPTION_KEYS` keeps them loud elsewhere), and the inferSchema re-read pops them so `warn` emits exactly one WARNING per logical read. pins: nullability-2/C-006; csv-infer-perf-1/C-002, C-005; torture-1/C-018 **ICE-TT-RESOLVE-1 (2026-09-19):** `_ICEBERG_TIME_TRAVEL_OPTIONS` gains `versionasof`/`timestampasof`; `_parse_snapshot_id_option`/`_parse_as_of_timestamp_option` keep the legacy pins' loud errors while `reader.py` forwards the built-ins as raw strings for the shared Rust resolver. pins: ice-tt-resolve-1/C-004 **ICE-CHANGELOG-1 (2026-09-20):** `start-snapshot-id`/`end-snapshot-id` leave `_UNSUPPORTED_SEMANTIC_READER_OPTIONS` — they are served now, not a future seed — and join `start-timestamp`/`end-timestamp` in the new `_ICEBERG_INCREMENTAL_OPTIONS`; `collect_incremental_window` returns those option strings verbatim (lower-cased keys) for the engine to parse. On a non-Iceberg format all four stay loud with the `format('iceberg')`-only shape the time-travel options use. pins: ice-changelog-1/C-001, C-004, C-005 |
| `create_dataframe_values.py` | Scalar normalization, SQL literals, schema parsing, Spark-type mapping. String cells use the shared literal helper; non-binary collated strings refuse; `NullType` maps to VOID. **FN-FIX-1:** `float('nan')` is kept and emitted as `CAST('NaN' AS DOUBLE)`. **FACADE-4 step 1 (2026-09-14):** `_data_type_to_sql_type` reads the shared Rust table (`sql_marker_from_descriptor`) with the collation refusal and unknown-subtype fallback kept here. **Round 2:** the `_sql_type_token_python` leaf consults the row table's SQL-marker column so pass-through subclasses emit base's `isinstance` markers (`INT`, `BIGINT`, `VOID`, `DECIMAL(p,s)`), not engine tokens. **Round 4 (2026-09-14, L-009):** `_data_type_to_sql_type` picks its primary class through `_type_table._sql_order` (base's integer-first `isinstance` chain) and applies the collation refusal only after that pick via `_sql_refuse_collation`, so multiple-inheritance and collated classes resolve exactly as base did; `_sql_type_token_python` reads the same order for its leaf marker. pins: fn-fix-1-registry-rows/C-002; facade-4/C-012, C-022, C-031 |
| `create_dataframe_schema.py` | DDL parsing, pandas/Polars null witnesses, schema-name permutation. |
| `create_dataframe_rows.py` | Named-row binding, pandas/Polars extraction, VALUES and Arrow-memtable materialization, scratch-view cleanup. Materialized frames keep the `_source_view_name` tag for `declareSorted`. **FN-FIX-1:** Sparse integer/bool nan-fill stays SQL null; Sparse[object] NaN is DOUBLE. pins: fn-fix-1-registry-rows/C-002 **FACADE-1 (2026-09-12):** `_materialize_exporter_as_memtable_frame` registers any `__arrow_c_stream__` exporter through the capsule helper (IPC fallback on version skew). Polars `createDataFrame` uses that exporter path when pyarrow is hidden. pins: facade-1/C-001, C-002 **FACADE-3 step 3 (2026-09-13):** dict and `Row` list branches try `_rust_cdf_named_arrow_table` (injected from `create_dataframe_columns`) before the `_rows_from_mapping_list` walk — the native named export owns the bind on covered shapes; a `None` keeps this module's funnel, which reproduces the pinned homogeneity / key-set / schema-bind refusals. pins: facade-3/C-019 **F-PY-2 (S2-21 Python review):** a `named_declined` flag set only when `cdf_arrow_export_named` exists and returned `None` routes the rebuilt tuples straight to `_arrow_table_from_raw_tuples_fast`/`_legacy` — the tuple-door native export is never retried on them (a symbol-absent skew keeps the old retry). `_rows_from_mapping_list` also hoists the `Row` import out of its per-row loop and splits the dict arm into an isinstance scan plus a comprehension — same checks, same order, less doomed-path work; the real-base fallback table lives in `docs/perf/facade-3-cdf-step3-2026-09-13.md`. pins: facade-3/C-026 |
| `create_dataframe_inference.py` | Nested Arrow inference, struct merging, decimal-envelope checks, SQL-to-Arrow types. VOID/NULL map to `pa.null()`. **FACADE-4 step 1 (2026-09-14):** `_sql_type_to_arrow` resolves flat atomic tokens through the shared Rust table (`sql_token_to_arrow_capsule`); decimal and nested spellings stay on the Python path. **Round 2:** native calls bind through the cached `_type_table._native_function`; nested spellings keep the `fromDDL` → classes route — the only native token-to-Arrow call answers `string` for `ARRAY<`/`MAP<`/`STRUCT<` through its catch-all arm rather than refusing. pins: facade-4/C-013, C-026 |
| `create_dataframe_arrow.py` | pandas/Polars Arrow-column normalization, timestamp localization, decimal validation. **FN-FIX-1:** object-dtype all-NaN keeps DOUBLE NaN. pins: fn-fix-1-registry-rows/C-002 |
| `create_dataframe_tuples.py` | Tuple-to-Arrow conversion, scalar/list merge refusals, dense FixedSizeList and sparse ML-vector reshape. **PERF-FACADE-CDF-1:** the duplicate-name refuse, the all-null CAST parse (keeps `DECIMAL(p, s)` parens intact while stripping the CAST wrapper paren) and the Arrow build wrap are module functions shared with the column-wise path, called by the tuple converter. |
| `create_dataframe_columns.py` | **PERF-FACADE-CDF-1:** column-wise census, inference and conversion. One `set(map(type, …))` census per column; single-kind scalar columns convert straight to Arrow; mixed/exotic columns normalize through the shared cell normalizer and build through the unchanged tuple converter. Explicit schemas dispatch to the legacy row-wise path. **FACADE-1:** pyarrow loads inside the functions that need it, so package import no longer requires pyarrow. pins: facade-1/C-002 **FACADE-3 step 2 (2026-09-13):** `_rust_cdf_arrow_table` tries `_native.cdf_arrow_export` first — the Rust builder takes the resolved names, raw tuples, an optional `pa.Schema` for explicit schemas, the session-UTC flag, the NTZ flag, both inference contextvars and `decimal.getcontext().prec`, and returns a `__arrow_c_stream__` exporter that `pa.table` drains. `None` (or any native error) falls back to the column-wise/legacy path, so every refusal the Rust port does not reproduce keeps its pinned class and message; the step-2 release re-measure of this dispatch lives in `docs/perf/facade-3-cdf-step2-2026-09-13.md`. pins: facade-3/C-010, C-011 **FACADE-3 step 3 (2026-09-13):** `_rust_cdf_named_arrow_table` mirrors it for `Row`/dict lists — passes the raw list, `is_row`, optional name list and `pa.Schema` into `_native.cdf_arrow_export_named`; `None`/error falls back to `_rows_from_mapping_list`; the step-3 baseline split and release re-measure of this dispatch live in `docs/perf/facade-3-cdf-step3-2026-09-13.md`. pins: facade-3/C-018, C-019, C-022 |
| `sql_udf_parsing.py` | SQL lexical scanning, comment-safe select-list splitting, simple UDF-call parsing. |
| `sql_udf_discovery.py` | Registry-UDF discovery, trailing-clause peeling. |
| `sql_udf_residual.py` | WHERE-residual base-projection planning. |
| `sql_udf_materialization.py` | Expression-UDF materialization, ORDER BY alias planning, UDF error cleanup. |
| `sql_udf_rewrite.py` | Select-list Python-UDF rewrite assembly. |
| `sql_cache_statements.py` | **ICE-CATALOG-SESSION-1 S5 (2026-09-20):** the `CACHE [LAZY] TABLE` / `UNCACHE TABLE [IF EXISTS]` / `REFRESH [TABLE]` / `REFRESH 'path'` door. `try_sql_cache_statement` runs in the `sql()` rewrite loop beside the SET door and routes every recognised shape to the existing `catalog_surface` implementation, so `isCached` agrees with SQL and no second cache exists (H-05); AS SELECT / OPTIONS / PARTITION tails fall through to the engine's loud refusal. pins: ice-catalog-session-1/C-019, C-020, C-021 |
| `sql_relations.py` | Statement-shape patterns, string/comment masking, CTE discovery, relation scanning, multipart identifier parsing. |
| `sql_set_statements.py` | **SQL-SET-DOOR-1 (2026-09-14, registry B-TZ-5):** the `SET`/`RESET`/`SET TIME ZONE` door. `try_sql_set_statement` recognises the D-1 shapes (case-insensitive keywords, surrounding whitespace and one trailing `;` tolerated) after `_strip_sql_comments` drops `--`/`/* … */` comments outside string literals — so `SET x = 1 -- c` stores `1` and `SET k = 'a;b'` keeps the quoted value — and runs them through `RuntimeConfig` (`conf.set`/`get`/`unset`) so a SQL `SET` has exactly the effect `spark.conf.set` has — the two knobs validate in Rust and apply to the live session (TZ-3 / SET-ANSI-RUNTIME-1 FIXED 2026-09-15). Every `datafusion.*` key and every `spark.wap.*` assignment/unset returns `None` for the engine — the `datafusion.*` exclusion is load-bearing (`RuntimeConfig.set` forwards those keys through this same SQL entry point and intercepting them would loop) and the `spark.wap.*` keys are no longer excluded — **ICE-WAP-BRANCH-1 (2026-09-19)** routes them through `RuntimeConfig` like the other live knobs, so `SET spark.wap.branch = audit` answers Spark's `(key, value)` row and the audit branch reaches the Rust carrier (pins: ice-wap-branch-1/C-007). `RESET` of a collation key calls `refuse_collation_session_key` before `conf.unset`, mirroring the engine's `refuse_collation_reset_variable` valve the intercept would otherwise bypass. `RESET ALL` is routed to the bare-`RESET` path (Spark treats them identically) before the key pattern could read `ALL` as a conf name, and a surviving `;` outside literals or an unterminated quote defers to the engine — the same answer the pre-door tree gave. A leading-trivia scan returns `None` before any whole-query copy when the first keyword is not `SET`/`RESET` (P1-SET-FULLSCAN); `_sql_mask_strings_and_comments` runs only when `;` survives the trailing-`;` strip (P2-MASK-ALWAYS). `SET ROLE` is not intercepted. `SET key TO value` raises `ParseException` `[INVALID_SET_SYNTAX]`. Offset zones follow Java `ZoneId.of` (`+05`/`+5`/`+18:00`/`GMT+8` accept; `+18:01` refuses). Backtick-quoted keys, double-quoted `SET TIME ZONE`, and `INTERVAL '+HH:MM' HOUR TO MINUTE` are recognised. `RESET <key>` restores a builder-seeded value when one exists (no `builder_conf.py` edit). Spark-class errors the conf layer cannot raise are produced here: `AnalysisException` `CANNOT_MODIFY_STATIC_CONFIG` (`SQLSTATE: 46110`), `IllegalArgumentException` `INVALID_CONF_VALUE.TIME_ZONE` / `TYPE_MISMATCH` / `REQUIREMENT` (`SQLSTATE: 22022`), plus the dated `SET TIME ZONE LOCAL` refusal (SET-TZ-LOCAL-1). Result frames are `pa.Table`s through `_materialize_arrow_as_memtable_frame`; `SET k` / `SET` / `SET -v` redact with Spark's default `spark.redaction.regex` against key or value (`*********(redacted)`); `SET k = v` echoes raw. pins: sql-set-door-1/C-001, C-002, C-003, C-006, C-007, C-008, C-009, C-010, C-011, C-012, C-013, C-014 **SET-ANSI-RUNTIME-1 (2026-09-15):** the Python zone/boolean pre-validators are deleted — `conf.set` validates in Rust (single gate, same verdicts); `RESET` of a builder-seeded knob goes through the lenient native restore. pins: set-ansi-runtime-1/C-001, C-002, C-003. **ICE-DYN-OVERWRITE-1 (2026-09-17):** `set`/`unset` of `PARTITION_OVERWRITE_MODE_KEY` route through the native setter the same way (invalid refuses before storing; `unset` restores `STATIC`). pins: ice-dyn-overwrite-1/C-007, C-010. **R-17c-4:** the restore refreshes the companion cache for the zone knob. pins: set-ansi-runtime-1/C-002 |
| `sql_relations.py` | Statement-shape patterns, string/comment masking, CTE discovery, relation scanning, multipart identifier parsing. **CATALOG-SURFACE-1 (2026-09-14):** `_sql_table_ref`'s docstring names `resolve_table_name` alone as the resolve-first sibling — the `_sql_table_ref_resolved` convenience wrapper it also cited was removed with `session_core.table`'s move to `catalog_surface`. pins: catalog-surface-1/C-004 **IPI-21 (2026-09-20):** `_DROP_TABLE_SQL_RE` grows an optional trailing `(\s+PURGE)?` group so the name blob stops swallowing `t PURGE` as one identifier segment, and `_CREATE_TABLE_PREFIX_RE` accepts the bare `REPLACE TABLE` spelling so it qualifies like `CREATE OR REPLACE TABLE` (IPI-25). Both symbol hashes move in `python/repark/tests/test_production_file_size.py`. pins: ipi-21-25-42-small-parser/C-005, C-008. **ICE-VIEWS-1 (2026-09-20):** `_CREATE_TEMP_VIEW_SQL_RE` so durable view bodies pass through verbatim. pins: ice-views-1/C-001 |
| `builder_conf.py` | SparkContext, RuntimeConfig. `set` refuses session keys containing `collation` (silent-ignore path). **SET-ANSI-RUNTIME-1 (2026-09-15):** `set`/`unset` of the two exact keys (`spark.sql.ansi.enabled`, `spark.sql.session.timeZone`) route through the native setter before storing — invalid refuses with Spark's class, `unset` tombstones and applies the registered default. pins: set-ansi-runtime-1/C-001, C-002, C-003. **ICE-RTAS-BYNAME-1 round 2 (2026-09-17):** `set`/`unset` of `spark.sql.caseSensitive` route the same way (default `false`). pins: ice-rtas-byname-1/C-010. **R-17c-4:** the companion cache refreshes on every zone mutation; one `_ensure_alive()` handle per call. pins: set-ansi-runtime-1/C-002. **R-17c-6:** padded zone input refuses (trim reverted); refusals echo raw. pins: set-ansi-runtime-1/C-002. `datafusion.runtime.memory_limit` swaps a new `FairSpillPool` (one truth, not two knobs); `datafusion.runtime.temp_directory` is build-time (runtime SET refuses, names `TMPDIR`); RAM-relative FairSpillPool default (cap 8 GiB) with the `sort_spill_reservation_bytes × target_partitions` floor. **DISPLAY-POLARS-1 step 4 (2026-09-09):** `RuntimeConfig` set/get/unset intercept the three int display keys the same way they intercept the style key (validate, canonical store, alive token, builder sync, canonical tomb); `get` reads the live token so runtime `conf.set` takes effect at render time, falling back to the validated builder map when the token field is absent (build path). pins: display-polars-1/C-005 **EAGER-BUDGET-1 step 1 (2026-09-13):** `repark.cache.retained_bytes` is a computed read-only key — `get` returns the decimal native total, `getAll` carries it, `isModifiable` answers `False`, `set`/`unset` refuse with `INVALID_CONF_VALUE.REQUIREMENT`. pins: eager-budget-1/C-003 **EAGER-BUDGET-1 review (2026-09-13, R12b-D-5):** `RuntimeConfig.set` on a `repark.cache.max_bytes`/`max_total_bytes` spelling clears case-variant tomb entries and store spellings so the last spelling set wins and a differently-cased `unset` cannot fail open (`_CACHE_BYTE_BUDGET_KEYS_LOWER`). pins: eager-budget-1/C-004, C-008 **REVIEW-FIX-14 (2026-09-10):** `conf.get` on an unset `repark.display.style` reads the alive-token snapshot `unset` wrote rather than re-reading `REPARK_DISPLAY_STYLE`, closing the Q-52 lockstep hole (ADR-0004); `unset` itself still resolves the env-aware default once at the unset call. pins: review-fix-14/C-001 **ICE-DYN-OVERWRITE-1 round 3 (2026-09-17):** the rebase onto ICE-RTAS-BYNAME-1 had dropped `PARTITION_OVERWRITE_MODE_KEY` from the `unset` native-restore tuple (it kept `SPARK_SQL_CASE_SENSITIVE_KEY`), so `unset` tombstoned the store and left the live session dynamic; the key is back beside the other three. pins: ice-dyn-overwrite-1/C-022 **ICE-WAP-BRANCH-1 (2026-09-19):** `set` forwards `spark.wap.branch` / `spark.wap.id` (`WAP_SESSION_KEYS`) to the native setter beside the overwrite-mode key, and `unset` forwards the empty string — the Rust carrier owns what an empty value means (clear the key), so Python still decides nothing. pins: ice-wap-branch-1/C-007 **ICE-SESSION-WRITE-CONF-1 (2026-09-19):** `set`/`unset` of the `spark.sql.iceberg.*` write confs forward through the native setter (Rust validates and applies); the module parses nothing. |
| `session_core.py` | ReparkSession (SQL/catalog methods). `Builder._set_config_entry` refuses collation keys; the getOrCreate reuse fold calls `refuse_collation_session_key` so a planted `_config` key cannot silently store. **DISPLAY-POLARS-1 step 4 (2026-09-09, follow-up):** the module holds no display-key state of its own — `_set_config_entry` funnels display keys through `_canonicalize_display_key` and the reuse path applies int keys through one `_reuse_display_ints` call (both live in `session_configuration.py`); token-miss reads fall back to the builder map, so the file ratchets DOWN. pins: display-polars-1/C-005 **CFG-1 step 3 (2026-09-09):** `Builder.config_file` / `configFile` (forced path, `None` keeps discovery) forwarded as native `config_path`; the knob resolvers moved to `session_configuration.py`. pins: cfg-1/C-026, C-027 **MAINT-POLICY-1 step 4 (2026-09-10):** `run_maintenance` is declared in the class body as a thin delegator to `session_maintenance.py` (audit fix: the earlier `__init__` assignment was invisible to the example-coverage enumerator). `_temp_view_home_ref` moved byte-identical to `catalog_resolution.py` to pay for the declaration; the file sits on its ratcheted `check_lib_py` baseline. pins: maint-policy-1/C-021, C-022, C-023, C-024, C-025, C-030 **SQL-SET-DOOR-1 (2026-09-14):** `sql()` hands a recognised `SET`/`RESET` statement to `sql_set_statements.try_sql_set_statement` ahead of the UDTF/UDF scans — the one hook the ownership ruling grants. Orchestrator ruling R-1 (2026-09-14): keep the net-zero 2304 baseline; P3-IMPORT-IN-SQL moved the SET-door import to module level (still exactly 2304). pins: sql-set-door-1/C-001 **ICE-TT-RESOLVE-1 round 2 (2026-09-19):** `read_iceberg_table` docstring restored plus one built-ins sentence; the i64 gates move to `reader_support.check_i64_pin`; the file holds its exact 2287 baseline. pins: ice-tt-resolve-1/C-012 |
| `sql_relations.py` | Statement-shape patterns, string/comment masking, CTE discovery, relation scanning, multipart identifier parsing. **CATALOG-SURFACE-1 (2026-09-14):** `_sql_table_ref`'s docstring names `resolve_table_name` alone as the resolve-first sibling — the `_sql_table_ref_resolved` convenience wrapper it also cited was removed with `session_core.table`'s move to `catalog_surface`. pins: catalog-surface-1/C-004 **ICE-CATALOG-SESSION-1 S4 (2026-09-20):** `_DESCRIBE_TABLE_PREFIX_RE` (the `DESCRIBE [TABLE] [EXTENDED|FORMATTED]` head the new expansion arm scans under). |
| `builder_conf.py` | SparkContext, RuntimeConfig. `set` refuses session keys containing `collation` (silent-ignore path). **SET-ANSI-RUNTIME-1 (2026-09-15):** `set`/`unset` of the two exact keys (`spark.sql.ansi.enabled`, `spark.sql.session.timeZone`) route through the native setter before storing — invalid refuses with Spark's class, `unset` tombstones and applies the registered default. pins: set-ansi-runtime-1/C-001, C-002, C-003. **ICE-RTAS-BYNAME-1 round 2 (2026-09-17):** `set`/`unset` of `spark.sql.caseSensitive` route the same way (default `false`). pins: ice-rtas-byname-1/C-010. **R-17c-4:** the companion cache refreshes on every zone mutation; one `_ensure_alive()` handle per call. pins: set-ansi-runtime-1/C-002. **R-17c-6:** padded zone input refuses (trim reverted); refusals echo raw. pins: set-ansi-runtime-1/C-002. `datafusion.runtime.memory_limit` swaps a new `FairSpillPool` (one truth, not two knobs); `datafusion.runtime.temp_directory` is build-time (runtime SET refuses, names `TMPDIR`); RAM-relative FairSpillPool default (cap 8 GiB) with the `sort_spill_reservation_bytes × target_partitions` floor. **DISPLAY-POLARS-1 step 4 (2026-09-09):** `RuntimeConfig` set/get/unset intercept the three int display keys the same way they intercept the style key (validate, canonical store, alive token, builder sync, canonical tomb); `get` reads the live token so runtime `conf.set` takes effect at render time, falling back to the validated builder map when the token field is absent (build path). pins: display-polars-1/C-005 **EAGER-BUDGET-1 step 1 (2026-09-13):** `repark.cache.retained_bytes` is a computed read-only key — `get` returns the decimal native total, `getAll` carries it, `isModifiable` answers `False`, `set`/`unset` refuse with `INVALID_CONF_VALUE.REQUIREMENT`. pins: eager-budget-1/C-003 **EAGER-BUDGET-1 review (2026-09-13, R12b-D-5):** `RuntimeConfig.set` on a `repark.cache.max_bytes`/`max_total_bytes` spelling clears case-variant tomb entries and store spellings so the last spelling set wins and a differently-cased `unset` cannot fail open (`_CACHE_BYTE_BUDGET_KEYS_LOWER`). pins: eager-budget-1/C-004, C-008 **REVIEW-FIX-14 (2026-09-10):** `conf.get` on an unset `repark.display.style` reads the alive-token snapshot `unset` wrote rather than re-reading `REPARK_DISPLAY_STYLE`, closing the Q-52 lockstep hole (ADR-0004); `unset` itself still resolves the env-aware default once at the unset call. pins: review-fix-14/C-001 **ICE-DYN-OVERWRITE-1 round 3 (2026-09-17):** the rebase onto ICE-RTAS-BYNAME-1 had dropped `PARTITION_OVERWRITE_MODE_KEY` from the `unset` native-restore tuple (it kept `SPARK_SQL_CASE_SENSITIVE_KEY`), so `unset` tombstoned the store and left the live session dynamic; the key is back beside the other three. pins: ice-dyn-overwrite-1/C-022 **ICE-WAP-BRANCH-1 (2026-09-19):** `set` forwards `spark.wap.branch` / `spark.wap.id` (`WAP_SESSION_KEYS`) to the native setter beside the overwrite-mode key, and `unset` forwards the empty string — the Rust carrier owns what an empty value means (clear the key), so Python still decides nothing. pins: ice-wap-branch-1/C-007 |
| `session_core.py` | ReparkSession (SQL/catalog methods). `Builder._set_config_entry` refuses collation keys; the getOrCreate reuse fold calls `refuse_collation_session_key` so a planted `_config` key cannot silently store. **DISPLAY-POLARS-1 step 4 (2026-09-09, follow-up):** the module holds no display-key state of its own — `_set_config_entry` funnels display keys through `_canonicalize_display_key` and the reuse path applies int keys through one `_reuse_display_ints` call (both live in `session_configuration.py`); token-miss reads fall back to the builder map, so the file ratchets DOWN. pins: display-polars-1/C-005 **CFG-1 step 3 (2026-09-09):** `Builder.config_file` / `configFile` (forced path, `None` keeps discovery) forwarded as native `config_path`; the knob resolvers moved to `session_configuration.py`. pins: cfg-1/C-026, C-027 **MAINT-POLICY-1 step 4 (2026-09-10):** `run_maintenance` is declared in the class body as a thin delegator to `session_maintenance.py` (audit fix: the earlier `__init__` assignment was invisible to the example-coverage enumerator). `_temp_view_home_ref` moved byte-identical to `catalog_resolution.py` to pay for the declaration; the file sits on its ratcheted `check_lib_py` baseline. pins: maint-policy-1/C-021, C-022, C-023, C-024, C-025, C-030 **SQL-SET-DOOR-1 (2026-09-14):** `sql()` hands a recognised `SET`/`RESET` statement to `sql_set_statements.try_sql_set_statement` ahead of the UDTF/UDF scans — the one hook the ownership ruling grants. Orchestrator ruling R-1 (2026-09-14): keep the net-zero 2304 baseline; P3-IMPORT-IN-SQL moved the SET-door import to module level (still exactly 2304). pins: sql-set-door-1/C-001 **ICE-TT-RESOLVE-1 round 2 (2026-09-19):** `read_iceberg_table` docstring restored plus one built-ins sentence; the i64 gates move to `reader_support.check_i64_pin`; the file holds its exact 2287 baseline. pins: ice-tt-resolve-1/C-012 **ICE-CATALOG-SESSION-1 S4 (2026-09-20):** `_try_expand_describe_sql` qualifies `DESCRIBE [TABLE] [EXTENDED|FORMATTED] name` targets with `prefer_temp_view` (a temp view — including one named `namespace` — resolves to its HOME; other namespace-head first segments stay on the namespace door); baseline 2287 → 2327. **ICE-CATALOG-SESSION-1 S5 (2026-09-20):** `sql()` runs `sql_cache_statements.try_sql_cache_statement` beside the SET door (CACHE / UNCACHE / REFRESH through `catalog_surface`); baseline 2327 → 2335. |
| `sql_relations.py` | Statement-shape patterns, string/comment masking, CTE discovery, relation scanning, multipart identifier parsing. **CATALOG-SURFACE-1 (2026-09-14):** `_sql_table_ref`'s docstring names `resolve_table_name` alone as the resolve-first sibling — the `_sql_table_ref_resolved` convenience wrapper it also cited was removed with `session_core.table`'s move to `catalog_surface`. pins: catalog-surface-1/C-004 **ICE-CATALOG-SESSION-1 S4 (2026-09-20):** `_DESCRIBE_TABLE_PREFIX_RE` (the `DESCRIBE [TABLE] [EXTENDED|FORMATTED]` head the new expansion arm scans under). **ICE-CATALOG-SESSION-1 S6 (2026-09-20):** `_is_catalog_state_statement` (the `USE` / `SET datafusion.catalog.default_*` head the post-execute sync keys on). |
| `builder_conf.py` | SparkContext, RuntimeConfig. `set` refuses session keys containing `collation` (silent-ignore path). **SET-ANSI-RUNTIME-1 (2026-09-15):** `set`/`unset` of the two exact keys (`spark.sql.ansi.enabled`, `spark.sql.session.timeZone`) route through the native setter before storing — invalid refuses with Spark's class, `unset` tombstones and applies the registered default. pins: set-ansi-runtime-1/C-001, C-002, C-003. **ICE-RTAS-BYNAME-1 round 2 (2026-09-17):** `set`/`unset` of `spark.sql.caseSensitive` route the same way (default `false`). pins: ice-rtas-byname-1/C-010. **R-17c-4:** the companion cache refreshes on every zone mutation; one `_ensure_alive()` handle per call. pins: set-ansi-runtime-1/C-002. **R-17c-6:** padded zone input refuses (trim reverted); refusals echo raw. pins: set-ansi-runtime-1/C-002. `datafusion.runtime.memory_limit` swaps a new `FairSpillPool` (one truth, not two knobs); `datafusion.runtime.temp_directory` is build-time (runtime SET refuses, names `TMPDIR`); RAM-relative FairSpillPool default (cap 8 GiB) with the `sort_spill_reservation_bytes × target_partitions` floor. **DISPLAY-POLARS-1 step 4 (2026-09-09):** `RuntimeConfig` set/get/unset intercept the three int display keys the same way they intercept the style key (validate, canonical store, alive token, builder sync, canonical tomb); `get` reads the live token so runtime `conf.set` takes effect at render time, falling back to the validated builder map when the token field is absent (build path). pins: display-polars-1/C-005 **EAGER-BUDGET-1 step 1 (2026-09-13):** `repark.cache.retained_bytes` is a computed read-only key — `get` returns the decimal native total, `getAll` carries it, `isModifiable` answers `False`, `set`/`unset` refuse with `INVALID_CONF_VALUE.REQUIREMENT`. pins: eager-budget-1/C-003 **EAGER-BUDGET-1 review (2026-09-13, R12b-D-5):** `RuntimeConfig.set` on a `repark.cache.max_bytes`/`max_total_bytes` spelling clears case-variant tomb entries and store spellings so the last spelling set wins and a differently-cased `unset` cannot fail open (`_CACHE_BYTE_BUDGET_KEYS_LOWER`). pins: eager-budget-1/C-004, C-008 **REVIEW-FIX-14 (2026-09-10):** `conf.get` on an unset `repark.display.style` reads the alive-token snapshot `unset` wrote rather than re-reading `REPARK_DISPLAY_STYLE`, closing the Q-52 lockstep hole (ADR-0004); `unset` itself still resolves the env-aware default once at the unset call. pins: review-fix-14/C-001 **ICE-DYN-OVERWRITE-1 round 3 (2026-09-17):** the rebase onto ICE-RTAS-BYNAME-1 had dropped `PARTITION_OVERWRITE_MODE_KEY` from the `unset` native-restore tuple (it kept `SPARK_SQL_CASE_SENSITIVE_KEY`), so `unset` tombstoned the store and left the live session dynamic; the key is back beside the other three. pins: ice-dyn-overwrite-1/C-022 **ICE-WAP-BRANCH-1 (2026-09-19):** `set` forwards `spark.wap.branch` / `spark.wap.id` (`WAP_SESSION_KEYS`) to the native setter beside the overwrite-mode key, and `unset` forwards the empty string — the Rust carrier owns what an empty value means (clear the key), so Python still decides nothing. pins: ice-wap-branch-1/C-007 **ICE-CATALOG-SESSION-1 S6 (2026-09-20):** `set` of a `spark.sql.catalog.*` / `repark.sql.catalog.*` key stores, then offers the name's accumulated block to `register_late_catalog_block` (first complete parse registers; later sets update the side map); a new registration joins the facade known set. pins: ice-catalog-session-1/C-024 |
| `session_core.py` | ReparkSession (SQL/catalog methods). `Builder._set_config_entry` refuses collation keys; the getOrCreate reuse fold calls `refuse_collation_session_key` so a planted `_config` key cannot silently store. **DISPLAY-POLARS-1 step 4 (2026-09-09, follow-up):** the module holds no display-key state of its own — `_set_config_entry` funnels display keys through `_canonicalize_display_key` and the reuse path applies int keys through one `_reuse_display_ints` call (both live in `session_configuration.py`); token-miss reads fall back to the builder map, so the file ratchets DOWN. pins: display-polars-1/C-005 **CFG-1 step 3 (2026-09-09):** `Builder.config_file` / `configFile` (forced path, `None` keeps discovery) forwarded as native `config_path`; the knob resolvers moved to `session_configuration.py`. pins: cfg-1/C-026, C-027 **MAINT-POLICY-1 step 4 (2026-09-10):** `run_maintenance` is declared in the class body as a thin delegator to `session_maintenance.py` (audit fix: the earlier `__init__` assignment was invisible to the example-coverage enumerator). `_temp_view_home_ref` moved byte-identical to `catalog_resolution.py` to pay for the declaration; the file sits on its ratcheted `check_lib_py` baseline. pins: maint-policy-1/C-021, C-022, C-023, C-024, C-025, C-030 **SQL-SET-DOOR-1 (2026-09-14):** `sql()` hands a recognised `SET`/`RESET` statement to `sql_set_statements.try_sql_set_statement` ahead of the UDTF/UDF scans — the one hook the ownership ruling grants. Orchestrator ruling R-1 (2026-09-14): keep the net-zero 2304 baseline; P3-IMPORT-IN-SQL moved the SET-door import to module level (still exactly 2304). pins: sql-set-door-1/C-001 **ICE-TT-RESOLVE-1 round 2 (2026-09-19):** `read_iceberg_table` docstring restored plus one built-ins sentence; the i64 gates move to `reader_support.check_i64_pin`; the file holds its exact 2287 baseline. pins: ice-tt-resolve-1/C-012 **ICE-CATALOG-SESSION-1 S4 (2026-09-20):** `_try_expand_describe_sql` qualifies `DESCRIBE [TABLE] [EXTENDED|FORMATTED] name` targets with `prefer_temp_view` (a temp view — including one named `namespace` — resolves to its HOME; other namespace-head first segments stay on the namespace door); baseline 2287 → 2327. **ICE-CATALOG-SESSION-1 S5 (2026-09-20):** `sql()` runs `sql_cache_statements.try_sql_cache_statement` beside the SET door (CACHE / UNCACHE / REFRESH through `catalog_surface`); baseline 2327 → 2335. **ICE-CATALOG-SESSION-1 S6 (2026-09-20):** `sql()` syncs the facade box from the engine after every `USE` / `SET datafusion.catalog.default_*` (`_sync_catalog_state_from_engine` over the native getter); baseline 2335 → 2345. **ICE-CATALOG-SESSION-1 R6 (2026-09-20):** the sticky `_note_registered_catalog` flip also moves the engine session-defaults box catalog (native catalog-only set, namespace preserved, `current_catalog()` carrier untouched); baseline 2345 → 2346. |
| `session_core.py` | ReparkSession (SQL/catalog methods). `Builder._set_config_entry` refuses collation keys; the getOrCreate reuse fold calls `refuse_collation_session_key` so a planted `_config` key cannot silently store. **DF-SURFACE-A-1 critic round 1 (2026-09-14, R-9):** `_alive_token["facade_session"]` holds the owning facade session so `DataFrame.sparkSession` answers the creator even after a `newSession` is promoted — the exploded dict line is paid for by moving the `udf_registry` comment's fact here: `_alive_token["udf_registry"]` is the classic scalar Python UDF registry (name → entry) and dies with `stop()`. The file holds its exact 2304 baseline. pins: df-surface-a-1/C-008 **DISPLAY-POLARS-1 step 4 (2026-09-09, follow-up):** the module holds no display-key state of its own — `_set_config_entry` funnels display keys through `_canonicalize_display_key` and the reuse path applies int keys through one `_reuse_display_ints` call (both live in `session_configuration.py`); token-miss reads fall back to the builder map, so the file ratchets DOWN. pins: display-polars-1/C-005 **CFG-1 step 3 (2026-09-09):** `Builder.config_file` / `configFile` (forced path, `None` keeps discovery) forwarded as native `config_path`; the knob resolvers moved to `session_configuration.py`. pins: cfg-1/C-026, C-027 **MAINT-POLICY-1 step 4 (2026-09-10):** `run_maintenance` is declared in the class body as a thin delegator to `session_maintenance.py` (audit fix: the earlier `__init__` assignment was invisible to the example-coverage enumerator). `_temp_view_home_ref` moved byte-identical to `catalog_resolution.py` to pay for the declaration; the file sits on its ratcheted `check_lib_py` baseline. pins: maint-policy-1/C-021, C-022, C-023, C-024, C-025, C-030 |
| `session_core.py` | ReparkSession (SQL/catalog methods). `Builder._set_config_entry` refuses collation keys; the getOrCreate reuse fold calls `refuse_collation_session_key` so a planted `_config` key cannot silently store. **DISPLAY-POLARS-1 step 4 (2026-09-09, follow-up):** the module holds no display-key state of its own — `_set_config_entry` funnels display keys through `_canonicalize_display_key` and the reuse path applies int keys through one `_reuse_display_ints` call (both live in `session_configuration.py`); token-miss reads fall back to the builder map, so the file ratchets DOWN. pins: display-polars-1/C-005 **CFG-1 step 3 (2026-09-09):** `Builder.config_file` / `configFile` (forced path, `None` keeps discovery) forwarded as native `config_path`; the knob resolvers moved to `session_configuration.py`. pins: cfg-1/C-026, C-027 **MAINT-POLICY-1 step 4 (2026-09-10):** `run_maintenance` is declared in the class body as a thin delegator to `session_maintenance.py` (audit fix: the earlier `__init__` assignment was invisible to the example-coverage enumerator). `_temp_view_home_ref` moved byte-identical to `catalog_resolution.py` to pay for the declaration; the file sits on its ratcheted `check_lib_py` baseline. pins: maint-policy-1/C-021, C-022, C-023, C-024, C-025, C-030 **CATALOG-SURFACE-1 (2026-09-14):** `table()` is a thin delegator to `catalog_surface.session_table` so a catalog-cached resolved identity scans the held cache view on the second read and every `spark.table` frame is registered under its identity for `isCached`; the temp-view-first resolution and native `SELECT *` path are unchanged, and the orphaned `_sql_table_ref_resolved` helper was removed — the file ratchets DOWN to 2291. pins: catalog-surface-1/C-004 **SESSION-SURFACE-1 (2026-09-15, rebase onto #602):** the nineteen surface names bind as one-line class attributes to `session_surface.py`; `create_namespace`'s signature joins onto one line so the file ratchets DOWN to 2290. pins: session-surface-1/C-001 **ICE-DYN-OVERWRITE-1 (2026-09-17):** the getOrCreate reuse fold forwards `PARTITION_OVERWRITE_MODE_KEY` through `RuntimeConfig.set` (same line, baseline held at 2290). pins: ice-dyn-overwrite-1/C-007 **ICE-DYN-OVERWRITE-1 round 2, ruling Q-20a-6 (2026-09-17):** a `sql()` keyword was considered and reverted — the file holds its 2290 baseline; the static path enters through the native free function called from `writer_readwriter.py`. pins: ice-dyn-overwrite-1/L-001 **IPI-21 (2026-09-20):** the `DROP` expander peels the trailing `PURGE` off the name blob before it resolves and re-emits it after the qualified list, so `DROP TABLE [IF EXISTS] cat.ns.t PURGE` reaches the Rust router with the keyword intact; without it the cell dies in Python and no Rust fix is reachable. Baseline held. pins: ipi-21-25-42-small-parser/C-008. **ICE-VIEWS-1 (2026-09-20):** `_try_expand_create_view_body_sql` expands TEMPORARY bodies only; durable bodies reach the engine verbatim. pins: ice-views-1/C-001, C-006 |
| `session_maintenance.py` | **MAINT-POLICY-1 step 4 (2026-09-10):** `run_maintenance(table, dry_run=True, **overrides)` (D-5 thin wrapper; the `ReparkSession` class body in `session_core.py` declares a thin delegator to it since the audit fix). Resolves the table under the session catalog + namespace, renders one `CALL <catalog>.system.run_maintenance(…)` (`dry_run` always explicit, overrides in call-site order through the shared Spark literal helpers), and returns `self.sql(…)` so engine refusals keep their native class. Non-str table / non-bool dry_run refuse as `PySparkTypeError`, bad override keys or an empty resolution as `PySparkValueError`; unknown keys ride through to the engine's loud refusal. pins: maint-policy-1/C-021, C-022, C-023, C-024, C-025, C-027, C-030 |
| `session_sources.py` | **CFG-2 step 2 (2026-09-13):** the named-source facade (D-7/D-8). `sources(session)` builds `SourceMetadata` rows off `_native.session_sources` in declaration order; `source(session, name)` wraps `_native.session_source` in `NamedSource` (read-only `name` / `kind` / `key_path`, `ping()` routing to `_native.session_source_ping` so the connector-pending refusal keeps `UnsupportedOperationException`). `session_core.py` declares the two thin delegators; `_builder_config_get_master` moved byte-identical to `session_configuration.py` (as a config-dict function, the CFG-1-step-3 shape) and `_promote_active` to `session_state.py` (the `_active_session` owner) to pay for them — both ride `_funcs.py` back into `session_core` globals, so the file holds its exact 2304 baseline. S2-21 step-2 review (Grok 4.6, read-only): no P1/P2, P3 rows in the ledger. pins: cfg-2/C-013, C-014, C-015, C-017 |
| `session_surface.py` | **SESSION-SURFACE-1 step 1 (2026-09-14):** the 19-name SparkSession surface card. Job tags over a per-session set (`add_tag`/`remove_tag`/`get_tags`/`clear_tags` — `remove_tag` on a missing or empty tag is Spark's no-op; non-str raises `NOT_STR`, SES-TAG-1); `interrupt_all`/`interrupt_tag`/`interrupt_operation` answer `[]` (SES-INTERRUPT-1: no cancellable registry, numeric-id validation kept); the five Connect-only names raise `ONLY_SUPPORTED_WITH_SPARK_CONNECT` byte-identical to classic; `session_read_stream`/`session_streams`/`session_data_source` are `NOT_IMPLEMENTED` declared refusals (SES-DECL-*); `add_artifacts` binds both `addArtifact` and `addArtifacts` (driver-local `pyfile` copy into a per-session `mkdtemp` root prepended to `sys.path` once, `INVALID_MULTIPLE_ARGUMENT_CONDITIONS` on two flags, `DUPLICATED_ARTIFACT` on a changed target, `FileNotFoundError` naming a missing source, `archive`/`file` declared, no-flag no-op — SES-ARTIFACT-1); `Profile`/`_ProfilerCollector` answer Spark's method surface with an empty collector and `render` declared (SES-PROFILE-1); `TableValuedFunction` exposes Spark's full method list — `range` delegates to `session.range`, generators select the same-named `repark.spark.functions` call over `range(1)` and ride that function's own refusal when it has one, `sql_keywords`/`collations` try the SQL door then refuse `tvf.<name>`, `python_worker_logs` refuses outright (SES-TVF-1). `session_core.py` binds all 19 names and holds its exact 2304 baseline (five one-line signature joins funded the import plus bindings). **Critic round 1 (2026-09-15):** `cleanup_artifact_dir` removes the session's own dir + `sys.path` entry, called from `stop()` (the `create_dataframe` join pays for the block, baseline stays 2304); `interrupt_operation` validates as `Try(toLong)` (ASCII digits, int64 range) with `NOT_STR` on a non-str id; `add_artifacts` raises Spark's `addPyFile()` arity `TypeError` on a pathless `pyfile=True`; `Profile.render` runs the `VALUE_NOT_ALLOWED` `type` check before its declared refusal; `tvf.json_tuple` unquotes string-literal Columns to `str` fields and refuses a non-literal field `NOT_STR`. pins: session-surface-1/C-001, C-002, C-003, C-004, C-005, C-006, C-007, C-010 |
| `reader.py` | DataFrameReader incl. `smartCsv`. `sep`/`delimiter` resolve with the `is not None` idiom; empty/multi-char/newline/CR/quote values refuse via `_require_single_char_delimiter`; auto-detect stays origin/main agreement-first in `_csv_smart.detect_delimiter`. CSV `inferSchema` finishes through `_finish_csv_infer_schema` (path argument, not a stored `path` option) then the `_cN` rename then the leftover ntz recast. The semantic gate's csv/json-only branch (`_CSV_JSON_ONLY_OPTION_KEYS`: `compression`, `flag_secret_columns`) refuses loud on every other reader (parquet, table, empty format). pins: nullability-2/C-006; csv-infer-perf-1/C-002; torture-1/C-018 |
| `reader.py` | DataFrameReader incl. `smartCsv`. `sep`/`delimiter` resolve with the `is not None` idiom; empty/multi-char/newline/CR/quote values refuse via `_require_single_char_delimiter`; auto-detect stays origin/main agreement-first in `_csv_smart.detect_delimiter`. CSV `inferSchema` finishes through `_finish_csv_infer_schema` (path argument, not a stored `path` option) then the `_cN` rename then the leftover ntz recast. The semantic gate's csv/json-only branch (`_CSV_JSON_ONLY_OPTION_KEYS`: `compression`, `flag_secret_columns`) refuses loud on every other reader (parquet, table, empty format). Per-format notes (moved from the class docstring, IO-TEXT-1): CSV/JSON route through the DataFusion native readers (`read_csv` / `read_json`); `schema` stores a user StructType/DDL for those reads; unknown formats raise `AnalysisException` (Spark's `DATA_SOURCE_NOT_FOUND` class shape); unknown options store (Spark tolerates them), semantic ones fail loud; `excel` is a disclosed repark extension (PySpark has no excel reader), single-sheet v1, `sheet_names` discovers sheets. **IO-TEXT-1 (2026-09-14):** `text` binds as the one-line class attribute `text = _reader_text.text` and `format("text").load` routes to `reader_text.load_text`; the docstring above condenses to fund those lines so the file holds its exact 1022 baseline. **Round 3 (2026-09-15, U-4):** `load()` drops a falsy `recursiveFileLookup` on the `text` door before the semantic gate (helper lives in `reader_text.py`); truthy stays loud. pins: nullability-2/C-006; csv-infer-perf-1/C-002; torture-1/C-018; io-text-1/C-001, U-4 **ICE-CHANGELOG-1 (2026-09-20):** `table()` and the `format('iceberg').load()` arm route to `reader_incremental.load_incremental` when `collect_incremental_window` finds a window bound, passing the time-travel pins along so the engine rules the combination. pins: ice-changelog-1/C-001, C-006 **IPI-23 (2026-09-22):** `_iceberg_time_travel_opts` refuses the legacy `snapshot-id` / `as-of-timestamp` / `tag` pins with Spark 4.1.2's `IllegalArgumentException` texts (precedence in that order) before any other check, on both `load` and `table`; `branch` still parses and the built-ins still forward raw. |
  IO-DECLARED-1 (2026-09-14): `orc`/`xml`/`jdbc` bind on `DataFrameReader` from `dataframe/io_declared.py` (Spark's signatures; registry IO-ORC-1 / IO-XML-1 / IO-JDBC-1). R-3 (2026-09-14 round 2): `jdbc` is main's PostgreSQL read path — the body lives in `io_declared.py` (`reader.py` binds only, 1022 → 954, the ceiling exception retires), non-PostgreSQL driver URLs refuse `NOT_IMPLEMENTED` at the dispatch, and `load()` gains the `format("orc")`/`format("xml")` declared arms before the semantic gate. pins: io-declared-1/C-001, C-002, C-003, C-007
  **IO-ORC-1 (2026-09-16):** `orc` rebinds to `reader_orc.orc` (the read side is a real scan now) and `format("orc").load` routes to `reader_orc.load_orc`; only `xml` keeps the declared `load()` arm, and `orc` skips the blanket semantic gate (it carries its own file options). pins: io-orc-1/C-002, C-005, C-006 |
| `reader_text.py` | **IO-TEXT-1 (2026-09-14):** `DataFrameReader.text` (wholetext always overlays, matching the JVM parameter; other kwargs overlay when set) plus `load_text` for `format("text").load`. Path lists union per-path Rust reads; the engine call is one typed `read_text` (wholetext bool, lineSep) so unknown options cannot slip through. A user schema is honored only as one string field (rename + cast), non-UTF-8 encodings refuse, and the four engine file-scope options ride the shared semantic gate. **Follow-up (2026-09-15):** an explicit falsy `recursiveFileLookup` drops before the gate (the scan never recurses); truthy stays loud. **Round 3 (2026-09-15, U-10):** `_read_one` pins `PATH_NOT_FOUND` plus `42K03` onto the native missing-path refusal through the shared attach helper. **Round 4 (2026-09-15, W-1/W-4):** the user schema passes as data-schema pairs plus `basePath` into the engine (`text_schema.rs` overlays, `basePath` discovers under the base); `_read_one` pins the two runtime conditions (`CONFLICTING_PARTITION_COLUMN_NAMES` `KD009`, `INVALID_PARTITION_VALUE` `42846`). pins: io-text-1/C-001, io-text-1/T-1, U-10, W-1, W-3, W-4 |
| `reader_orc.py` | **IO-ORC-1 (2026-09-16):** `DataFrameReader.orc` (Spark's positional signature; a non-bool positional mergeSchema raises the JVM's `IllegalArgumentException` text, a non-str/non-list path raises `NOT_STR_OR_LIST`) plus `load_orc` for `format("orc").load`. Python holds names, shapes and option plumbing only: every read is one typed `read_orc` over a path list and the scan merges (mergeSchema) or first-schema-projects exactly like a directory, so unknown options are ignored like Spark. The user schema and `basePath` pass through as data-schema pairs; the Rust binding attaches the native file conditions (`PATH_NOT_FOUND` `42K03`, `UNABLE_TO_INFER_SCHEMA` `42KD9`, `FAILED_READ_FILE.CANNOT_READ_FILE_FOOTER` `KD001`, `CANNOT_MERGE_SCHEMAS`) and `load_orc` binds the shared structured-error methods. **Round 3 (R-18b-12):** the scan reads every glob match like Spark (hidden `_`/`.` only) — `m?` carries the merge cell, `m*` over the fixture dir refuses naming map.md. pins: io-orc-1/C-002, C-004, C-005, C-006, C-007 |
| `reader_incremental.py` | **ICE-CHANGELOG-1 (2026-09-20):** the Iceberg incremental-read door — `load_incremental` resolves the table name like `read_iceberg_table` does, then hands the raw window option strings and the time-travel pins beside them to the native `read_iceberg_incremental`. Python holds no bound, no parse and no refusal; the engine owns all three (`repark_core::incremental`). pins: ice-changelog-1/C-001, C-006 |
| `sql_udf.py` | UDFRegistration. |
| `create_dataframe.py` | Region marker + `SparkSession`/`ReParkSession` aliases. |
| `catalog.py` | Re-export-binding region note (no-stub mark). |
| `timestamp_type.py` | `spark.sql.timestampType` facade half: one key spelling, default `TIMESTAMP_LTZ`, parse refuses naming both legal tokens, builder whitespace normalize, runtime `conf.set` is store-only. Engine resolves at `SparkExtension.configure`. **FACADE-4 step 1 (2026-09-14):** `default_timestamp_data_type` / `default_timestamp_arrow_type` read the shared Rust table (`default_timestamp_descriptor` + Arrow capsule); conf parsing stays here. **Round 2:** `default_timestamp_arrow_type` routes the decoded class through `repark_type_to_arrow` — the descriptor's tuple wire shape cannot feed the dict-reading capsule call. pins: facade-4/C-013, C-026 |
| `session_time_zone.py` | `spark.sql.session.timeZone` facade half: one key spelling, `UTC` default, `normalize_session_time_zone_config` (whitespace-only, matching the engine's own trim — the engine stays the sole validator). **SET-ANSI-RUNTIME-1 (2026-09-15):** runtime set/unset validates in Rust and applies to the live session (the warn-and-swallow helper is deleted); fresh queries answer the new zone, pre-SET frames keep their analysis snapshot. pins: set-ansi-runtime-1/C-002, C-003. **R-17c-4:** the canonical companion cache (`active_session_time_zone_canonical`, alive-token, lazy native seed, refreshed on every zone mutation) feeding `_session_tzinfo` (ZoneInfo for IANA, fixed offset otherwise); runtime zone sets store trimmed. pins: set-ansi-runtime-1/C-002. The module docstring carries the user-visible statement of what the zone reaches; it ships in the wheel, so it is a lockstep obligation whenever engine coverage changes. |
| `__init__.py` | Frozen public re-exports and shared facade-class binding. |

## The column-wise createDataFrame path (PERF-FACADE-CDF-1)

`_create_dataframe_from_rows_inner` ends at one dispatcher,
`_arrow_table_from_raw_tuples`: explicit `StructType` / DDL schemas run the legacy row-wise
build (`_arrow_table_from_raw_tuples_legacy`, the moved pre-unit block, kept callable as the
pins' oracle); inferred schemas run the column-wise build. The old-vs-new bench pair and every
equality pin swap the rows-module dispatcher between those two in a `finally`, the way
PERF-FACADE-1 swapped the row converter.

Why the answers are identical by construction, not by reimplementation: every shared rule is
the same function object on both paths (the cell normalizer, the all-null witness, the tuple
converter for nested columns, the decimal-envelope validator, the three helpers tuples.py now
shares). The fast path skips work only where the census proves it a no-op — a `{int}` column
cannot carry a second merge kind, so the refusal walk is skipped; a pure-`None` column is
all-null, so normalization is skipped. Single-kind checks use `type()`, never `isinstance`,
because `bool` subclasses `int` and `datetime` subclasses `date`.

Two error-precedence facts, both multi-failure only (every single-failure input raises the
byte-identical error): the envelope scan reports the row-major-first violation across decimal
columns while slow columns validate inside their own build, and a slow column's inference error
raises in the build phase rather than the inference phase. The ledger names the pairs.

The tuple loop skips `_apply_permutation` when the permutation is the identity (hoisted
once, not per row) — the profiler charged the rebuild ~60 ms at 1e5. Only
namedtuple-plus-reorder takes the permuting arm.

pins: perf-facade-cdf-1/C-002, C-003, C-004

## Pointers

- Up: [../map.md](../map.md)
- Engine half of the session-timezone knob: `crates/repark-core/src/session_time_zone.rs`
- Temp-view home resolution: `session_core.py` `_temp_view_home_ref` calls native
  `resolve_temp_view_home_ref`; every product path emits the home-qualified spelling
  (`"datafusion"."public"."v"`) for a session-local view. `createDataFrame`'s scratch view is
  named through `repark.spark._temp_views.scratch_view_name`.

## Debug

| Symptom | First check |
|---|---|
| `spark.conf.set("spark.sql.session.timeZone", …)` does not move old frames | Intended: the value applies per-query — frames built before the set keep the snapshot they were analysed under; only fresh queries answer the new zone (registry SET-ANSI-RUNTIME-1/2). |
| A garbage zone passed to `conf.set` is not refused | No longer true: the native setter refuses `INVALID_CONF_VALUE.TIME_ZONE` before anything is stored (registry TZ-3 FIXED, 2026-09-15). |
| `spark.conf.get("spark.sql.session.timeZone")` returns `UTC` on a never-set session | Intended: the default is `UTC`, not the host zone — a declared divergence from Spark's JVM-local default. |
| A second `getOrCreate` with a different zone warns and does not apply | Intended: the session zone joins the engine-knob set, so reuse never folds a zone the live engine session does not have into the facade conf. |
| `conf.get` returns a trimmed zone though the builder value was padded | Intended: `normalize_session_time_zone_config` strips whitespace exactly as the engine does; validity stays the engine's. |
| The zone is set but timestamp extraction did not move | The conf surface landed without the extraction fix; the rows in `python/repark/tests/test_session_timezone_parity.py` pin the current divergence honestly. |

ICE-MIXED-CASE-1 (2026-09-17): `spark.sql.caseSensitive` joins the live engine-knob set — default `false` in `_SQLCONF_DEFAULTS`, runtime set/unset routes through the native setter beside the ANSI and zone keys (`builder_conf.py`, `sql_set_statements.py`). pins: ice-mixed-case-1/C-006

IPI-19 + IPI-37 (2026-09-20): `session_configuration.py` holds
`MERGE_SCHEMA_KEY` (`spark.sql.iceberg.merge-schema`) and `builder_conf.py`
forwards `set` of it to the native setter beside the overwrite-mode and WAP
keys. It carries no `_SQLCONF_DEFAULTS` row — Spark answers no default for it —
so `unset` forwards the literal `"false"`, which is the documented default the
Rust carrier starts from. SQL `SET spark.sql.iceberg.merge-schema = true` needs
no new parser: `sql_set_statements.py` already routes a plain assignment through
`RuntimeConfig.set`. pins: ipi-19-56-37-schema-evolution-write/C-004, C-012
