# `python/repark/tests/fixtures/orc` map

Spark-written ORC fixture directories behind `facade_orc_oracle.json`
(`.crc` sidecars not copied). `m1` is the example fixture reused by
`docs/examples/io/orc_read.py`.

pins: io-orc-1/C-002
